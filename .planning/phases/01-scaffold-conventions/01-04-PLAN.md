---
phase: 01-scaffold-conventions
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/copy/governed.ts
  - scripts/check-governed.mjs
  - scripts/check-governed.test.mjs
  - scripts/claims-audit.mjs
  - scripts/claims-audit.test.mjs
autonomous: true
requirements: [REQ-FR-47, REQ-FR-50, REQ-FR-65, REQ-SM-5]
user_setup: []

must_haves:
  truths:
    - "Each of the eight governed sentences is defined exactly once, with its load-bearing clause addressable so a renderer can wrap it in <strong> (D-07)"
    - "A second literal of any governed sentence anywhere in app/, components/ or lib/ fails the build (D-09, REQ-FR-47)"
    - "A ninth governed sentence cannot be added to the module without failing the build (D-09)"
    - "The claims register is the inherited one, verbatim, with its version, inheritance date and owner stated, plus the seed's additions, and it fails the build on the first prohibited claim (D-17, D-18, REQ-FR-50)"
    - "Every register addition has a positive and a negative fixture that the gate runs (D-18, D-23)"
    - "Every string this phase authors obeys the eight voice rules and the PRD §3 vocabulary verbatim, and the only two newly authored sentences — the platform-413 sentence here and the English-only limitation in 01-05 — carry [written here] beside them in code (D-19)"
  artifacts:
    - path: "lib/copy/governed.ts"
      provides: "The eight governed sentences and the platform-413 sentence, defined once"
      exports: ["GovernedKey", "GovernedSentence", "GOVERNED", "PLATFORM_413"]
    - path: "scripts/check-governed.mjs"
      provides: "Duplicate-literal sweep and the closed-set assertion"
    - path: "scripts/claims-audit.mjs"
      provides: "The inherited register plus the seed's additions"
      contains: "inherited from ipv-demo/HANDOVER.md §3 at 8fd097a (2026-09-01)"
    - path: "scripts/claims-audit.test.mjs"
      provides: "A positive and a negative fixture for every D-18 addition"
  key_links:
    - from: "scripts/check-governed.mjs"
      to: "lib/copy/governed.ts"
      via: "source parse — the sentences are read from the module, never restated in the check"
      pattern: "lib/copy/governed\\.ts"
    - from: "scripts/claims-audit.mjs"
      to: "app/, components/, lib/"
      via: "recursive walk + per-line regex sweep"
      pattern: "ROOTS"
---

<objective>
Define the honesty module once and build the two gates that keep it honest: the duplicate-literal
check and the claims audit.

Purpose: AD-12 allows one definition per governed sentence, and AD-22 allows the honesty surface
only to be strengthened. Both are enforceable only if the sentences live in one module and a
build-time sweep fails on a second literal — and if the register that decides what may never be
claimed is inherited verbatim rather than paraphrased.

Output: `lib/copy/governed.ts`, `scripts/check-governed.mjs` + its fixture test, and
`scripts/claims-audit.mjs` + its fixture test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-conventions/01-CONTEXT.md
@.planning/phases/01-scaffold-conventions/01-UI-SPEC.md
@.planning/phases/01-scaffold-conventions/01-PATTERNS.md
@.planning/phases/01-scaffold-conventions/01-VALIDATION.md
@.planning/phases/01-scaffold-conventions/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Created by plan 01-01, imported by both test files here. Use as-is. -->

scripts/lib/fixtures.mjs
- `repoRoot(): string`
- `withFixture(files: Record<string, string>, fn: (dir: string) => Promise<void>): Promise<void>`
- `runCheck(scriptRelPath, { cwd?, args?, env? }): Promise<{ code, stdout, stderr }>`

<!-- Contract this plan creates. Plans 01-05 and every later phase import exactly this. -->

lib/copy/governed.ts
- `export type GovernedKey = "preview" | "authoredVerification" | "authoredProposals" | "noRedaction" | "memoryStore" | "noOfflineInference" | "pendingReconciliation" | "mediaOnDevice"` — a closed union of exactly eight.
- `export type GovernedSentence = { before: string; strong: string; after: string }` — the
  load-bearing clause is the `strong` slot; a renderer emits `before`, then `<strong>{strong}</strong>`,
  then `after`, producing the sentence verbatim. This resolves CONTEXT.md's Claude's Discretion
  on how the clause is represented.
- `export const GOVERNED: Record<GovernedKey, GovernedSentence>` — declared in the locked key
  order; declaration order is the render order on the Limits surface (`Object.entries` preserves it).
- `export const PLATFORM_413: GovernedSentence` — the AD-11 disclosed-exception sentence, under a
  distinct name that is not one of the eight (D-07). Not rendered in Phase 1.
- No other value export exists. Consumers import `GOVERNED` and never restate a sentence.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write lib/copy/governed.ts</name>
  <files>lib/copy/governed.ts</files>
  <read_first>
    - .planning/phases/01-scaffold-conventions/01-UI-SPEC.md — §Copywriting Contract, §The Eight Governed Sentences (verbatim, with their bold clauses), §Voice Rules Every String Obeys
    - .planning/phases/01-scaffold-conventions/01-CONTEXT.md — D-07, D-19
    - .planning/phases/01-scaffold-conventions/01-PATTERNS.md — §`lib/copy/governed.ts` (no functional analog; the structural analog is `../ipv-demo/lib/data/types.ts` lines 19-30, a closed union plus a `Record<Key, V>`)
    - ../ipv-demo/lib/data/types.ts — lines 19-30 only, for the union + `Record` shape
    - tsconfig.json — the `@/*` path alias every consumer imports through
  </read_first>
  <action>
Create `lib/copy/governed.ts` with exactly the exports listed in this plan's `<interfaces>`
block, following the closed-union-plus-`Record` shape of `../ipv-demo/lib/data/types.ts`
lines 19-30.

Split each of the eight sentences into `before`, `strong` and `after` so that concatenating
the three reproduces the sentence character for character, with the `strong` slot holding
exactly the clause 01-UI-SPEC.md §The Eight Governed Sentences bolds. Copy the sentences
verbatim from that section — they are quoted there in full with their bold clauses marked.
The key order is `preview`, `authoredVerification`, `authoredProposals`, `noRedaction`,
`memoryStore`, `noOfflineInference`, `pendingReconciliation`, `mediaOnDevice`, and that
declaration order is the render order on the Limits surface.

Four of the eight open with their clause (`preview`, `authoredProposals`, `noRedaction`,
`noOfflineInference` opens after a short lead-in), so their `before` is the empty string or
the short lead-in; `mediaOnDevice` closes with its clause, so its `after` is the empty string;
`authoredVerification`, `memoryStore` and `pendingReconciliation` carry the clause
mid-sentence. Do not re-punctuate: the trailing full stop that sits inside a bold clause in
01-UI-SPEC.md stays inside `strong`.

`pendingReconciliation` contains the literal placeholder `{time}`. Leave it in the string
exactly as written; Phase 4 substitutes it, and Phase 1 renders the sentence as-is on the
Limits surface.

Add `PLATFORM_413` as a separate `GovernedSentence` const, not a member of `GOVERNED`, holding
the platform-413 sentence quoted verbatim in 01-UI-SPEC.md §Copywriting Contract's "Error
state" row. Its `strong` slot is empty (the sentence carries no bolded clause). Put a
`// [written here] — D-07, D-19: new copy authored for this preview` comment on the line above
it, as D-19 requires for each of this phase's two newly authored sentences.

Add a file header comment stating: this module is the single definition of every governed
sentence (AD-12); a second literal anywhere under `app/`, `components/` or `lib/` fails the
build; the honesty surface may only be strengthened, never softened or paraphrased (AD-22);
and that adding a ninth sentence requires the register and `scripts/check-governed.mjs` to be
changed in the same commit. Do not put the eight sentences in any comment — a commented copy
is still a second literal.

Every string in this file must pass the claims audit built in Task 3: no figure, no
"simulation" in any form, no server-persistence claim, no staffing-reduction framing.
  </action>
  <acceptance_criteria>
    - `lib/copy/governed.ts` exports `GovernedKey`, `GovernedSentence`, `GOVERNED`, `PLATFORM_413` and no other value binding
    - `GOVERNED` declares exactly 8 keys in the order `preview`, `authoredVerification`, `authoredProposals`, `noRedaction`, `memoryStore`, `noOfflineInference`, `pendingReconciliation`, `mediaOnDevice`
    - `lib/copy/governed.ts` contains `Designed preview.` exactly once and `{time}` exactly once
    - `lib/copy/governed.ts` contains `[written here]` on the line above `PLATFORM_413`
    - `lib/copy/governed.ts` contains no exclamation mark and does not contain `simulat`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The eight sentences and the 413 sentence are defined once, each with its load-bearing clause addressable, and the module type-checks under the closed key union.</done>
</task>

<task type="auto">
  <name>Task 2: Write the duplicate-literal check and its fixture test</name>
  <files>scripts/check-governed.mjs, scripts/check-governed.test.mjs</files>
  <read_first>
    - .planning/phases/01-scaffold-conventions/01-CONTEXT.md — D-07, D-09, D-23
    - .planning/phases/01-scaffold-conventions/01-PATTERNS.md — §Shared Patterns → the directory-walk-and-sweep (analog `../ipv-demo/scripts/claims-audit.mjs` lines 140-158 `walk()`, 160-184 the sweep loop, 186-222 the report and `process.exit(1)`), §File Classification (`check-governed.mjs` is a strong match for `claims-audit.mjs` — the same sweep, inverted purpose)
    - ../ipv-demo/scripts/claims-audit.mjs — lines 29-30 (`ROOTS`, `EXT`), 140-158 (`walk()`), 186-222 (report/exit)
    - lib/copy/governed.ts — the module this check parses; the sentences are never restated here
    - scripts/lib/fixtures.mjs — the helpers the test imports
  </read_first>
  <action>
Write `scripts/check-governed.mjs` as the inverted twin of the sibling's claims audit: the
same banner convention, the same `ROOTS = ["app", "components", "lib"]` and extension set, the
same `walk()` copied unchanged from `../ipv-demo/scripts/claims-audit.mjs` lines 140-158, the
same report-then-`process.exit(1)` ending. Where the audit fails on a prohibited string being
present, this fails on a governed string being present in the wrong place.

It reads `lib/copy/governed.ts` as text and extracts the sentences from the module rather than
restating them — restating them in the check would itself be a second definition. Parse the
`before`/`strong`/`after` string literals out of the `GOVERNED` record and out of
`PLATFORM_413`, concatenate each triple, and whitespace-normalise (collapse every run of
whitespace to a single space, trim). These normalised sentences are the needles.

Sweep every file under the three roots except `lib/copy/governed.ts` itself. For each file,
read it, whitespace-normalise the whole text the same way, and push a problem for every needle
the normalised text contains — normalising the whole file, not line by line, so a sentence
split across JSX lines is still caught. Report the file and the sentence key.

Add the closed-set assertion (D-09): push a problem if the keys of `GOVERNED` are not exactly
the eight in the locked order; if any additional exported binding in the module holds an object
with `before`/`strong`/`after` fields other than `PLATFORM_413`; or if `lib/copy/governed.ts`
is missing. A ninth sentence therefore cannot be added without failing the build.

Write `scripts/check-governed.test.mjs` (`node:test`) using `withFixture`/`runCheck`. Each
fixture writes its own `lib/copy/governed.ts` — a small two-key stand-in is enough, since the
check reads its needles from the module — plus whatever surface file the case needs. Cover:
the real repository exits 0; a fixture where `components/shell/Ribbon.tsx` contains one of its
module's sentences as a literal exits non-zero; a fixture where the same sentence is split
across three JSX lines with different indentation exits non-zero (the normalisation case); a
fixture where a sentence appears in `app/page.tsx` inside a comment exits non-zero (a
commented copy is still a second literal); a fixture whose module exports a ninth
`GovernedSentence` exits non-zero; a fixture whose module reorders two keys exits non-zero;
and a fixture where a component imports and renders the sentence rather than restating it
exits 0.
  </action>
  <acceptance_criteria>
    - `scripts/check-governed.mjs` contains `ROOTS` with the three roots and reads `lib/copy/governed.ts`
    - `scripts/check-governed.mjs` does NOT contain the substring `Designed preview.`
    - `node scripts/check-governed.mjs` exits 0 against the real repository
    - `node --test scripts/check-governed.test.mjs` exits 0 and reports at least 7 passing tests
    - The fixture case for a sentence split across three JSX lines exits non-zero
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/check-governed.test.mjs</automated>
  </verify>
  <done>A second literal of any governed sentence — including one split across lines or hidden in a comment — fails the build, and the set of eight is closed.</done>
</task>

<task type="auto">
  <name>Task 3: Copy the claims audit verbatim, extend it, and fixture every addition</name>
  <files>scripts/claims-audit.mjs, scripts/claims-audit.test.mjs</files>
  <read_first>
    - .planning/phases/01-scaffold-conventions/01-CONTEXT.md — D-17, D-18, D-19, D-23
    - .planning/phases/01-scaffold-conventions/01-PATTERNS.md — §The claims audit — copy verbatim, then extend (analog `../ipv-demo/scripts/claims-audit.mjs` lines 1-24 header, 29-30 roots, 44-62 register shape, 122-124 retirement marker, 140-158 walk, 160-184 sweep, 186-222 report; plus the redundancy flags on "simulation" at line 76 and "records never cross the border" at line 53)
    - ../ipv-demo/scripts/claims-audit.mjs — copy this entire file verbatim, then extend
    - ../ipv-demo/HANDOVER.md — §3 CLAIMS DISCIPLINE, lines 110-124, the register's human-readable source
    - .planning/REQUIREMENTS.md — REQ-FR-50's enumerated banned-claim list
    - scripts/lib/fixtures.mjs — the helpers the test imports
  </read_first>
  <action>
Copy `../ipv-demo/scripts/claims-audit.mjs` to `scripts/claims-audit.mjs` in full (D-17):
`ROOTS = ["app", "components", "lib"]`, the extension set, the `kind` taxonomy
(`retired`/`never`/`figure`), the `{ kind, pattern, note, allowQuoted? }` register entries, the
`RETIREMENT_MARKER` mechanism with its three-line context window, `walk()`, the sweep loop, and
the `live`/`quoted` report split ending in `process.exit(1)`. Carry every inherited
`PROHIBITED` entry unchanged — it is the inherited register, not a starting point.

Replace only the header (D-17). The new header states: the register version; `inherited from
ipv-demo/HANDOVER.md §3 at 8fd097a (2026-09-01)`; `owner: the SHEQ manager`; and that it is a
string sweep that cannot detect a prohibited position expressed in new words. Keep its stated
invocation and exit-code contract.

Append the seed's additions as new register entries (D-18), each with a `note` naming why it is
prohibited and the source that named it:
- Rule 1's funding alternation gains `\bfunded by this round\b`, `allowQuoted: true`.
- A findings-verb family for claim-shaped uses:
  `\b(as|writes?|files?|records?|reports?|raises?|surfaces?)\s+(a\s+)?findings?\b`, and extend
  `RETIREMENT_MARKER` by two new alternation branches inside the same regex literal:
  `never as (a )?finding` and `no model`.
- A subject-guarded camera-detects family:
  `\b(camera|model|AI|vision)\b.{0,40}\b(detects|detected|recognis(es|ed)|identif(ies|ied))\b.{0,30}\b(corrosion|leak|damage|asset|unit)\b`.
- Performance figures, kind `figure`: `\d+(\.\d+)?\s*(%|x|×)\s*(faster|fewer|less|reduction|saved)`
  and `\bin under \d+ (seconds|minutes)`.
- Staffing framing: `reduce(s|d)? (reliance on imported|headcount)`.
- The server-persistence family:
  `\bpersisted (to|in|on) (a |the )?(server|database|cloud)\b|\bencrypted at rest\b|\bsecure(ly)? stored\b`.
- A TRL entry named by REQ-FR-50: `\bTRL\s*-?\s*[1-9]\b|\btechnology readiness level\b`,
  `allowQuoted: true`.
- A competitor-name entry named by REQ-FR-50: `\b(Prevu3D|Siemens|Matterport|Hexagon|AVEVA)\b`,
  `allowQuoted: true`, with a note that the SHEQ manager extends the list when a new competitor
  is named. Prevu3D and Siemens are the two the parent's HANDOVER §3 cedes to.
- A modelled-savings-as-cash entry named by REQ-FR-50, kind `figure`:
  `\b(R|ZAR|\$|USD)\s?\d[\d ,.]*\s*(k|m|bn|million|billion)?\b.{0,40}\b(saved|saving|savings|avoided)\b`
  and `\b(saves?|saving|savings)\b.{0,30}\b(R|ZAR|\$|USD)\s?\d`.

Two of D-18's named additions are already in the inherited register and must NOT be added a
second time (01-PATTERNS.md flags both): "simulation" in any form is inherited at line 76, and
"records never cross the border" is inherited at line 53. Leave each inherited entry unchanged
and add a comment beside it recording that it satisfies D-18's requirement, so the redundancy
is documented rather than duplicated into two findings per hit.

Write `scripts/claims-audit.test.mjs` (`node:test`) using `withFixture`/`runCheck`. Every
register addition gets a matched pair: a MUST-TRIP fixture (a surface file containing the
string, asserted non-zero) and a MUST-PASS fixture (a near-miss the entry must not catch,
asserted 0). The near-miss is the load-bearing half — it is what proves the entry is scoped
rather than a word ban — so no entry ships with a positive case alone. Where an entry carries
`allowQuoted`, add a third EXCUSED fixture placing the string within the three-line context
window of a `RETIREMENT_MARKER` word, asserted 0.

Use these exact fixture strings, so the pairs are a worked template rather than invented:

- Baseline — the real repository exits 0 over `app/`, `components/` and `lib/` as they stand.
- Funding alternation — trips: "This work is funded by this round of investment." Passes:
  "This work is funded by the client's own capital budget." Excused: a comment line reading
  "retired claim, never restate: funded by this round".
- Findings-verb family — trips: "The artisan writes a finding against the asset." Passes:
  "The artisan writes an observation against the asset." Excused: "The verification is
  authored, never as a finding." A second excused case: "Matched to record, and no model ran."
- Camera-detects family — trips: "The camera detects corrosion on the unit." Passes: "The
  artisan photographs the unit and the record names the corrosion." (both subject nouns
  present, no detect-verb between them).
- Performance figures, pattern one — trips: "Capture is 40% faster." Passes: "The gauge reads
  40% relative humidity." (a figure and a percent sign, but no achievement verb follows).
- Performance figures, pattern two — trips: "The capture completes in under 30 seconds."
  Passes: "The photograph is taken in under two minutes." (the entry catches numerals, not
  prose — which is the register's stated limitation, and the near-miss says so).
- Staffing framing — trips: "Capture reduces headcount on the plant." Passes: "Capture reduces
  reliance on paper by moving the record to the phone." (same verb, same noun-phrase opening,
  different object).
- Server-persistence family — trips: "The capture is persisted to a database." Passes: "There
  is no database behind this preview."
- TRL claim — trips: "Capture is at TRL 6." Passes: "The asset tag reads TRL-A17, which is a
  plant identifier and not a readiness claim." Excused: a comment line reading "out of scope:
  never claim any TRL for Capture as a product".
- Competitor name — trips: "Prevu3D covers the layout work." Passes: "The hexagonal bolt head
  is 24 mm across." (the word-boundary case: `Hexagon` must not match inside "hexagonal").
  Excused: a comment line reading "out of scope: Prevu3D and Siemens own this".
- Modelled savings as cash — trips on pattern one: "R 1.2m saved on downtime last year."
  Trips on pattern two: "Capture saves R 40 000 a month." Passes: "Work order R 1024 is open."
  (an R-prefixed identifier with no saving verb within reach).
- Inherited entries — "The training module runs a simulation of the plant." is caught exactly
  once, not twice, proving D-18's overlap was documented rather than duplicated; the same for
  "records never cross the border". Each also gets its excused case, since both inherited
  entries carry `allowQuoted`.
  </action>
  <acceptance_criteria>
    - `scripts/claims-audit.mjs` header contains `inherited from ipv-demo/HANDOVER.md §3 at 8fd097a (2026-09-01)` and `owner: the SHEQ manager` and the words `string sweep`
    - `scripts/claims-audit.mjs` contains `ROOTS = ["app", "components", "lib"]` and `RETIREMENT_MARKER` and `never as (a )?finding` and `no model`
    - `scripts/claims-audit.mjs` contains `funded by this round`, `technology readiness level`, `Prevu3D`, `encrypted at rest`, `reduce(s|d)? (reliance on imported|headcount)`
    - `grep -c 'simulat' scripts/claims-audit.mjs` reports exactly 1 register pattern (the inherited entry), not 2
    - `node scripts/claims-audit.mjs` exits 0 against the real repository
    - `node --test scripts/claims-audit.test.mjs` exits 0 and reports at least 20 passing tests, with a positive and a negative case for every D-18 addition
    - Every D-18 register entry has both a must-trip and a must-pass fixture string in `scripts/claims-audit.test.mjs` — including the funding-alternation, staffing, TRL-claim, competitor-name, modelled-savings and `in under N seconds` entries, none of which ships with a positive case alone
    - Every entry carrying `allowQuoted` also has an excused fixture placing the string inside a `RETIREMENT_MARKER` context window, asserted 0
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/claims-audit.test.mjs</automated>
  </verify>
  <done>The inherited register is carried verbatim with its version, inheritance date and owner stated, the seed's additions are appended without duplicating the two already inherited, and every addition is proved by a positive and a negative fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored copy → rendered surface | A claim crosses from source into a public statement here; the audit is the only gate on that crossing |
| parent register (`ipv-demo/HANDOVER.md` §3) → this repository | An inherited prohibition list crosses here once and must not be paraphrased afterwards |
| `lib/copy/governed.ts` → every renderer | The honesty contract crosses here; a second literal would let one copy drift from the other |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | Information disclosure (overclaiming) | `scripts/claims-audit.mjs` register | mitigate | The register is copied verbatim from the parent, its header states version, `inherited from ipv-demo/HANDOVER.md §3 at 8fd097a (2026-09-01)` and `owner: the SHEQ manager`, and every seed addition ships a positive and a negative fixture the gate runs (D-17, D-18, D-23). |
| T-1-06 | Tampering (of the honesty contract) | `lib/copy/governed.ts` consumers | mitigate | `check-governed.mjs` fails on a second literal anywhere in `app/`, `components/` or `lib/`, including one split across lines or placed in a comment, and on a ninth sentence or a reordered key set (D-09, AD-12, AD-22). |
| T-1-13 | Repudiation | the audit's own limits | accept | The audit is a string sweep and cannot detect a prohibited position expressed in new words; this limitation is stated in the script header and the register's owner (the SHEQ manager) is named so the residual risk has an accountable reviewer (D-17, REQ-FR-50's stated limitation). |
| T-1-04 | Tampering | the audit as a skippable step | mitigate | The audit exits 1 on the first live finding with no flag, environment variable or config value that suppresses it; `verify.mjs` runs it and its fixtures in D-20's stated position. |
</threat_model>

<verification>
- `node scripts/check-governed.mjs` exits 0; `node scripts/claims-audit.mjs` exits 0.
- `node --test scripts/` exits 0.
- `npx tsc --noEmit` exits 0 and `npx eslint .` exits 0.
</verification>

<success_criteria>
- Every governed sentence is defined once in `lib/copy/governed.ts` and the duplicate-literal check fails the build on a second literal anywhere in `app/`, `components/` or `lib/` (REQ-FR-47, roadmap success criterion 3).
- The claims audit carries the inherited register with version and inheritance date, its owner named, and its additions tested (REQ-FR-50, REQ-SM-5, roadmap success criterion 4).
- Both checks exit non-zero on a fixture violation and neither warns in place of a failure (D-20, D-23).
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-conventions/01-04-SUMMARY.md` when done
</output>
