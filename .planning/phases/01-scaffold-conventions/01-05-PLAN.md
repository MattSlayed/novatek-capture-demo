---
phase: 01-scaffold-conventions
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - components/shell/Ribbon.tsx
  - components/shell/Ribbon.module.css
  - app/layout.tsx
  - app/page.tsx
  - components/limits/Limits.tsx
  - components/limits/Limits.module.css
autonomous: true
requirements: [REQ-FR-47, REQ-FR-48, REQ-NFR-9]
user_setup: []

must_haves:
  truths:
    - "Every route carries the preview sentence in document flow, above the page content, with no dismissal control (D-08, REQ-FR-48)"
    - "The ribbon link reaches the full preview limits at /?s=limits with a visible 44px target and a non-colour link cue (D-08, D-11)"
    - "The shell renders one labelled main with one screen-title heading and nothing else (D-11)"
    - "The Limits surface renders all eight governed sentences in key order plus the English-only limitation (D-10)"
    - "The three fonts resolve through the inherited alias chain into --font-display, --font-body and --font-mono (D-14)"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Font wiring, viewport, the ribbon above children"
      contains: "--font-syne"
    - path: "components/shell/Ribbon.tsx"
      provides: "The preview disclosure landmark"
      contains: "aria-label=\"Preview disclosure\""
    - path: "app/page.tsx"
      provides: "The statically prerendered shell with the searchParams reader behind Suspense"
      contains: "Suspense"
    - path: "components/limits/Limits.tsx"
      provides: "The minimal Limits surface"
      contains: "GOVERNED"
  key_links:
    - from: "app/layout.tsx"
      to: "components/shell/Ribbon.tsx"
      via: "rendered above {children} in <body>"
      pattern: "<Ribbon\\s*/>"
    - from: "components/shell/Ribbon.tsx"
      to: "lib/copy/governed.ts"
      via: "GOVERNED.preview, never a literal"
      pattern: "GOVERNED"
    - from: "components/limits/Limits.tsx"
      to: "lib/copy/governed.ts"
      via: "Object.entries(GOVERNED) in declaration order"
      pattern: "Object\\.entries\\(GOVERNED\\)"
    - from: "components/shell/Ribbon.tsx"
      to: "/?s=limits"
      via: "plain anchor, no <Link>, no router.push"
      pattern: "href=\"/\\?s=limits\""
---

<objective>
Render the two Phase 1 surfaces: the labelled shell at `/` and the minimal Limits surface at
`/?s=limits`, with the ribbon carried on every route by construction.

Purpose: the first deployment of a new project is a production deployment regardless of flags,
so a public URL must be labelled before it has anything to label. The ribbon is the label; the
Limits surface is where the label is read in full.

Output: `app/layout.tsx`, `app/page.tsx`, `components/shell/Ribbon.tsx` + its CSS Module, and
`components/limits/Limits.tsx` + its CSS Module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold-conventions/01-CONTEXT.md
@.planning/phases/01-scaffold-conventions/01-UI-SPEC.md
@.planning/phases/01-scaffold-conventions/01-RESEARCH.md
@.planning/phases/01-scaffold-conventions/01-PATTERNS.md
@.planning/phases/01-scaffold-conventions/01-03-SUMMARY.md
@.planning/phases/01-scaffold-conventions/01-04-SUMMARY.md
</context>

<interfaces>
<!-- Created by plan 01-04. Import exactly this; never restate a sentence. -->

lib/copy/governed.ts
- `type GovernedKey` — closed union of the eight keys.
- `type GovernedSentence = { before: string; strong: string; after: string }` — render as
  `{before}<strong>{strong}</strong>{after}` to reproduce the sentence verbatim with the
  load-bearing clause bolded at weight 500.
- `const GOVERNED: Record<GovernedKey, GovernedSentence>` — declaration order is render order.
- `const PLATFORM_413: GovernedSentence` — not rendered in Phase 1.

<!-- Created by plan 01-03. These global class names already exist in app/globals.css. -->

app/globals.css
- Type-role classes: `screen-title`, `object-title`, `prose`, `prose-sm`, `control-label`,
  `tag`, `eyebrow`, `label`, `figure`. None sets `text-transform`.
- `strong { font-weight: 500 }`; `:focus-visible { outline: 2px solid var(--cobalt-glow); outline-offset: 2px }`;
  `.sr-only`.
- Tokens available: `--navy-deep`, `--viewer-ink`, `--viewer-ink-dim`, `--viewer-border`,
  `--cobalt-glow`, `--cobalt-glow-ink`, `--control-border`, `--gutter`, `--safe-x`, `--safe-b`,
  `--space-4`, `--space-12`, `--space-16`, `--space-32`, `--space-48`, `--ribbon-min-h`,
  `--ribbon-link-min-h`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write the ribbon</name>
  <files>components/shell/Ribbon.tsx, components/shell/Ribbon.module.css</files>
  <read_first>
    - .planning/phases/01-scaffold-conventions/01-UI-SPEC.md — §Ribbon Contract in full (markup, style, the dot, the sentence, the link, the measured heights, the Never list), §Copywriting Contract (the primary CTA row), §Accessibility Floor
    - .planning/phases/01-scaffold-conventions/01-CONTEXT.md — D-08, D-11, D-15
    - .planning/phases/01-scaffold-conventions/01-PATTERNS.md — §`components/shell/Ribbon.tsx` and `components/limits/Limits.tsx` (analog `../ipv-demo/components/deepdive/PartPanel.tsx` lines 116-130 and 143, the `<section aria-label>` static-content landmark), §Shared Patterns → `<section aria-label>` static-content landmark
    - ../ipv-demo/components/deepdive/PartPanel.tsx — lines 116-130 only, for the landmark + prose-block shape
    - lib/copy/governed.ts — the module the sentence comes from
    - app/styles/tokens.capture.css — the tokens the module CSS references
  </read_first>
  <action>
Create `components/shell/Ribbon.tsx` as a Server Component (no `"use client"`, no state, no
event handler) following the landmark shape of `../ipv-demo/components/deepdive/PartPanel.tsx`
lines 116-130: a named `<section aria-label="Preview disclosure">` containing only static
content. Import `GOVERNED` from `@/lib/copy/governed` and destructure `GOVERNED.preview`;
render `{before}<strong>{strong}</strong>{after}`. Never write the sentence as a literal —
plan 01-04's check fails the build on a second literal (REQ-FR-47).

Structure inside the section: a decorative dot `<span aria-hidden="true">`, then a column
holding the sentence in a `<p>` and the link. The link is a plain
`<a href="/?s=limits">Read the full preview limits</a>` — the visible label is the accessible
name, it is never uppercased in the DOM (CSS does that), and it never reads as a dismiss.
D-11 makes this a plain anchor in this phase; no `<Link>`, no `router.push`. Phase 4 replaces
it with the `history.pushState` switcher without changing the URL contract.

The sentence carries the global `label` role class plus a module class; the link carries the
global `label` role class plus a module class. Compose global and module class names on the
same element rather than redefining the type role in the module.

Create `components/shell/Ribbon.module.css` with exactly the 01-UI-SPEC.md §Ribbon Contract
values: on the section, `background: var(--navy-deep)`,
`border-bottom: 1px solid var(--viewer-border)`, `min-height: var(--ribbon-min-h)`,
`max-height: none`, `position: static`, `padding: var(--space-12) var(--gutter)`,
`display: flex`, `gap: var(--space-12)`, `align-items: flex-start`. The `position: static` and
`max-height: none` are load-bearing and asserted by plan 01-06's WCAG check — the ribbon is
never fixed, never sticky, never capped.

The dot: 5px square, `border-radius: 9999px`, `background: var(--cobalt-glow)`,
`flex: 0 0 5px`, `margin-top: 6px`. The inner text column is the flex remainder, 311px at a
360px viewport (328 − 5 − 12).

The sentence: `color: var(--viewer-ink-dim)` with a descendant rule promoting `strong` to
`color: var(--viewer-ink)`. Explicitly `text-transform: none` — it is a sentence, not a label,
and renders in sentence case even though it uses the `label` type role.

The link: `display: inline-flex`, `align-items: center`,
`min-height: var(--ribbon-link-min-h)`, `margin-top: 2px`, `text-transform: uppercase`,
`color: var(--cobalt-glow-ink)`, `text-decoration: none`,
`border-bottom: 1px solid var(--control-border)` as the non-colour link cue.

Extend the link's hit area to fill the band with a `::after` pseudo-element positioned
absolutely against the text column (give the column `position: relative`, never the section —
the section must compute to `position: static`). Inset the pseudo-element by the negative of
the ribbon's own padding plus the dot column, so it covers the whole band:
top and bottom `calc(-1 * var(--space-12))`, right `calc(-1 * var(--gutter))`, left
`calc(-1 * (var(--gutter) + 17px))`.

Never add a dismiss control, a `✕`, a chevron, a collapse affordance, `aria-hidden` on the
section, truncation, a `max-height`, or an icon.
  </action>
  <acceptance_criteria>
    - `components/shell/Ribbon.tsx` contains `aria-label="Preview disclosure"` and `GOVERNED` and `href="/?s=limits"` and the visible label `Read the full preview limits`
    - `components/shell/Ribbon.tsx` does NOT contain `Designed preview.`, `use client`, `next/link`, `router.push`, `aria-hidden` on the section, `dismiss`, `✕` or `onClick`
    - `components/shell/Ribbon.module.css` contains `position: static`, `max-height: none`, `min-height: var(--ribbon-min-h)`, `border-bottom: 1px solid var(--viewer-border)`, `background: var(--navy-deep)`
    - `components/shell/Ribbon.module.css` contains `border-radius: 9999px`, `flex: 0 0 5px`, `text-transform: uppercase` on the link and `text-transform: none` on the sentence
    - `node scripts/check-governed.mjs` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-governed.mjs</automated>
  </verify>
  <done>The ribbon renders the preview sentence from the module in a named landmark, in document flow, with a 44px link and no dismissal affordance.</done>
</task>

<task type="auto">
  <name>Task 2: Write the root layout and the shell route</name>
  <files>app/layout.tsx, app/page.tsx</files>
  <read_first>
    - .planning/phases/01-scaffold-conventions/01-CONTEXT.md — D-08, D-11, D-14
    - .planning/phases/01-scaffold-conventions/01-UI-SPEC.md — §Phase 1 Surfaces (global constraints and Surface 1), §Copywriting Contract (the shell heading and empty-state rows)
    - .planning/phases/01-scaffold-conventions/01-RESEARCH.md — Pattern 1 (static shell with a streamed dynamic island), §Code Examples
    - .planning/phases/01-scaffold-conventions/01-PATTERNS.md — §`app/layout.tsx` (analog `../ipv-demo/app/layout.tsx` lines 10-29 font loading, 70-87 viewport and RootLayout) and its naming-deviation flag, §`next/font` → CSS custom property wiring
    - ../ipv-demo/app/layout.tsx — lines 10-29 and 70-87 only
    - app/styles/tokens.inherited.css — read the three `--font-*` alias declarations; they are what fixes the variable names below
    - components/shell/Ribbon.tsx — the component this layout renders
  </read_first>
  <action>
Create `app/layout.tsx` following `../ipv-demo/app/layout.tsx` lines 10-29 and 70-87. Import
`"./globals.css"` first, then `Syne`, `DM_Sans` and `JetBrains_Mono` from `next/font/google`,
then `Ribbon` from `@/components/shell/Ribbon`.

The three `next/font` calls MUST use `variable: "--font-syne"`, `variable: "--font-dm-sans"`
and `variable: "--font-jetbrains"` — the sibling's names, not `--font-display`/`--font-body`/
`--font-mono`. `app/styles/tokens.inherited.css` is a byte-identical copy of the sibling's
token file (D-12) and already declares `--font-display: var(--font-syne), …`,
`--font-body: var(--font-dm-sans), …` and `--font-mono: var(--font-jetbrains), …`. Loading the
fonts into the sibling's variable names is therefore how D-14's "into `--font-display`,
`--font-body` and `--font-mono`" is satisfied; any other variable name silently breaks the
alias chain and every type role falls back to system fonts with no build error.

Load exactly one weight per family, matching the locked single-weight-per-family rule
(01-UI-SPEC.md §Typography): Syne `600`, DM Sans `400`, JetBrains Mono `500`. Use
`subsets: ["latin"]` and `display: "swap"` on all three, as the sibling does.

Export `viewport` with `themeColor: "#0c1e35"`, `width: "device-width"`, `initialScale: 1`
and `viewportFit: "cover"` (D-14 — the sibling has no `viewportFit`, add it).
`metadata.appleWebApp` and the manifest are Phase 7; do not add them.

`RootLayout` renders `<html lang="en-ZA" className={...three .variable strings...}>` with
`<body><Ribbon />{children}</body>`, so every route carries the ribbon by construction
(D-08, REQ-FR-48). Do not wrap `{children}` in any element.

Create `app/page.tsx` per RESEARCH.md Pattern 1: a plain, non-async default-exported Server
Component that returns a `<Suspense fallback={null}>` wrapping an async `Screen` child which
awaits `searchParams`. Type the props explicitly as
`{ searchParams: Promise<{ s?: string }> }` rather than the generated `PageProps<"/">`, so the
route type-checks without a prior `next typegen` run; the shape is equivalent and this keeps
`tsc --noEmit` runnable at any point. Do not render the ribbon here — the layout already does.

When `s === "limits"` the `Screen` returns `<Limits />` (imported from
`@/components/limits/Limits`, written in Task 3). Otherwise it returns
`<main aria-labelledby="screen-title">` containing one
`<h1 id="screen-title" className="screen-title">NOVATEK Capture</h1>` and nothing else. The
`<h1>` carries both the class (the type-role hook) and the id (the `aria-labelledby` target).
This supersedes the shorthand `aria-label="screen"` in the RESEARCH.md sample; the locked
contract is `aria-labelledby`, per 01-UI-SPEC.md §Phase 1 Surfaces.

No body copy, no wordmark, no header bar, no navigation on the shell surface. The emptiness is
D-11 and D-19 working together — the reader's next step is the ribbon link — and is a locked
decision, not an omission. Do not add `components/brand/`; 01-UI-SPEC.md §Phase 1 Surfaces
locks the shell to heading-only.

`next.config.ts` already sets `cacheComponents: true` (plan 01-01), which is what keeps the
shell statically prerendered while the `searchParams` reader streams; plan 01-02's structure
check asserts the static marker for `/` from the build output.
  </action>
  <acceptance_criteria>
    - `app/layout.tsx` contains `variable: "--font-syne"`, `variable: "--font-dm-sans"`, `variable: "--font-jetbrains"` and does NOT contain `variable: "--font-display"`
    - `app/layout.tsx` contains `weight: ["600"]`, `weight: ["400"]`, `weight: ["500"]`, `display: "swap"`, `subsets: ["latin"]`
    - `app/layout.tsx` contains `themeColor: "#0c1e35"`, `viewportFit: "cover"`, `lang="en-ZA"` and `<Ribbon />` immediately before `{children}`
    - `app/page.tsx` contains `Suspense`, `aria-labelledby="screen-title"`, `id="screen-title"`, `className="screen-title"`, `NOVATEK Capture`
    - `app/page.tsx` does NOT contain `next/link`, `router.push`, `use client` or `useSearchParams`
    - `npx tsc --noEmit` exits 0
    - `npx eslint .` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Every route carries the ribbon above its content, the three fonts resolve through the inherited alias chain, and `/` renders one labelled main with one heading.</done>
</task>

<task type="auto">
  <name>Task 3: Write the minimal Limits surface</name>
  <files>components/limits/Limits.tsx, components/limits/Limits.module.css</files>
  <read_first>
    - .planning/phases/01-scaffold-conventions/01-UI-SPEC.md — §Phase 1 Surfaces → Surface 2, §Copywriting Contract (the Limits heading, Limits body and English-only rows), §The Eight Governed Sentences, §Spacing Scale
    - .planning/phases/01-scaffold-conventions/01-CONTEXT.md — D-10, D-19
    - .planning/phases/01-scaffold-conventions/01-PATTERNS.md — §`components/shell/Ribbon.tsx` and `components/limits/Limits.tsx` (analog `../ipv-demo/components/deepdive/PartPanel.tsx` lines 116-130, the same static-content-in-a-landmark shape; the prose-block half is the closer match for this file)
    - ../ipv-demo/components/deepdive/PartPanel.tsx — lines 116-130 only
    - lib/copy/governed.ts — the module every sentence comes from
    - app/globals.css — the `prose` and `screen-title` role classes this file uses
  </read_first>
  <action>
Create `components/limits/Limits.tsx` as a Server Component. It owns its own landmark:
`<main aria-labelledby="screen-title">` containing
`<h1 id="screen-title" className="screen-title">Preview limits</h1>`, then the sentence list.

Render the eight sentences by mapping `Object.entries(GOVERNED)` imported from
`@/lib/copy/governed` — declaration order in the module is the locked key order
(`preview`, `authoredVerification`, `authoredProposals`, `noRedaction`, `memoryStore`,
`noOfflineInference`, `pendingReconciliation`, `mediaOnDevice`) and `Object.entries` preserves
it, so no separate order array is needed. Each sentence is one `<p className="prose">` keyed by
its governed key, rendering `{before}<strong>{strong}</strong>{after}` — 16px DM Sans 400 in
`--viewer-ink`, with the load-bearing clause at weight 500 (globals.css already sets
`strong { font-weight: 500 }`). Every sentence renders in full: never suppressed, softened,
paraphrased, or reduced to a chip.

`pendingReconciliation` contains the literal `{time}` placeholder; render it as-is in this
phase. Do not substitute, hide or reword it.

Last in the list, render the English-only limitation as its own
`<p className="prose">` with no `<strong>`, using this sentence verbatim from
01-UI-SPEC.md §Copywriting Contract: "This preview is in English only because no other
language has been checked for it, and the product's language support is undecided." Put a
`{/* [written here] — D-10, D-19: new copy authored for this preview */}` comment beside it.
It is a preview limitation, not a product position, and it is one of only two sentences this
phase authors.

Do not render `PLATFORM_413` — it first renders in Phase 3+. Do not build the disposition
table, the live `X-CAP-Instance` value, the storage tri-state or any per-phase stated
limitation, and do not leave placeholders for them: they arrive with Phases 3, 5, 7 and 8.

Create `components/limits/Limits.module.css`: the main is a column at
`padding: var(--space-16) var(--safe-x) var(--space-48)`; the heading has
`margin-bottom: var(--space-32)` (the locked gap between the heading block and the sentence
list); the sentence list is `display: flex; flex-direction: column; gap: var(--space-16)`.
No cards, no panels, no rules between sentences in this phase — the panel primitive is
Phase 4/5. Both surfaces sit directly on the `--navy-deep` page ground.
  </action>
  <acceptance_criteria>
    - `components/limits/Limits.tsx` contains `Object.entries(GOVERNED)`, `aria-labelledby="screen-title"`, `id="screen-title"`, `Preview limits`
    - `components/limits/Limits.tsx` contains the English-only sentence verbatim and `[written here]` beside it
    - `components/limits/Limits.tsx` does NOT contain `PLATFORM_413`, `use client`, `X-CAP-Instance` or any governed sentence as a literal
    - `components/limits/Limits.module.css` contains `var(--space-16)`, `var(--space-32)`, `var(--space-48)`, `var(--safe-x)` and does NOT contain `box-shadow` or `border-radius`
    - `node scripts/check-governed.mjs` exits 0
    - `node scripts/claims-audit.mjs` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-governed.mjs</automated>
  </verify>
  <done>All eight governed sentences render in full in key order from the single module, followed by the English-only limitation, under one `Preview limits` heading.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated phone → the rendered page | The first thing a stranger sees crosses here; the ribbon is the only claim-bearing element in Phase 1 |
| `lib/copy/governed.ts` → JSX | The honesty contract crosses into markup here; a literal instead of an import would let the two copies drift |
| URL query (`?s=`) → rendered surface | The only untrusted input in Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-06 | Tampering (of the honesty contract) | `Ribbon.tsx`, `Limits.tsx` | mitigate | Both render from `GOVERNED`; `check-governed.mjs` fails the build on any literal copy of a governed sentence in `components/` (D-09, REQ-FR-47). |
| T-1-14 | Denial of the disclosure | ribbon styling and placement | mitigate | The ribbon is rendered by `app/layout.tsx` above `{children}` so it cannot be omitted per-route; `position: static`, `max-height: none`, no `aria-hidden` and no dismiss control, each asserted by plan 01-06's WCAG check on both surfaces (D-08, REQ-FR-48). |
| T-1-15 | Information disclosure | `searchParams.s` | mitigate | `s` is read for a two-way branch only and is never interpolated into markup, a URL, a header or a query; any value other than `limits` renders the shell. No other input surface exists in Phase 1 (ASVS V5, RESEARCH.md §Security Domain). |
| T-1-07 | Usability denial | rendered inks and target sizes | mitigate | Every colour comes from the token layers and every size from the type roles; plan 01-06's contrast check and axe scan gate both surfaces at 7:1 text, 3:1 non-text and WCAG 2.2 A/AA (REQ-NFR-5, REQ-NFR-9). |
</threat_model>

<verification>
- `node scripts/check-governed.mjs` exits 0 — no governed sentence appears as a literal in `app/` or `components/`.
- `node scripts/claims-audit.mjs` exits 0 over the newly copied surfaces.
- `npx tsc --noEmit` exits 0; `npx eslint .` exits 0.
- `node scripts/check-structure.mjs` exits 0.
</verification>

<success_criteria>
- The ribbon renders on every route, in document flow, with the `preview` sentence from the module and no dismissal control (REQ-FR-48, D-08).
- `/` renders one labelled `<main>` with one `screen-title` heading and nothing else (D-11).
- `/?s=limits` renders the eight governed sentences in key order plus the English-only limitation (D-10).
- The three fonts load into `--font-syne`, `--font-dm-sans` and `--font-jetbrains` so the inherited aliases resolve (D-14).
</success_criteria>

<output>
Create `.planning/phases/01-scaffold-conventions/01-05-SUMMARY.md` when done
</output>
