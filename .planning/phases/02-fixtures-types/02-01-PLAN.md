---
phase: 02-fixtures-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/data/types.ts
autonomous: true
requirements: [REQ-FR-21a]

must_haves:
  truths:
    - "`npx tsc --noEmit` is clean over `lib/data/types.ts` (roadmap success criterion 1)"
    - "The grade meaning *the record states this* is unconstructible on an authored observation: `AuthoredObservation.grade` is its own two-member union and `EXTRACTED` is not one of its members (D-12, REQ-FR-21a, roadmap success criterion 2)"
    - "Every closed set the later phases need is defined exactly once, here, as a string-literal union with a typed member array beside it (D-19)"
    - "The eight governed sentences are not restated: `types.ts` imports `GovernedKey` and declares no sentence text (D-19, Phase 1 D-07/D-09)"
    - "D-05: the observation kind set is the seed's eight plus `isolation_present` and `gauge_obscured`, one closed string-literal union with its typed member array, so the three seed rows that fit none of the eight have a kind and the set stays defined once"
    - "No TypeScript `enum` appears anywhere in `lib/data`, so Node 24 strip-only loading works for every later fixture test (RESEARCH Pattern 2 nuance 3)"
  artifacts:
    - path: "lib/data/types.ts"
      provides: "The fourteen copied sibling names, the eleven closed sets and the seed's eighteen new entity types (D-18, D-19)"
      contains: "export type ObservationGrade"
      min_lines: 300
  key_links:
    - from: "lib/data/types.ts"
      to: "lib/copy/governed.ts"
      via: "type-only import of GovernedKey (never a second definition)"
      pattern: "import type \\{[^}]*GovernedKey"
    - from: "lib/data/types.ts AuthoredObservation.grade"
      to: "ObservationGrade"
      via: "its own two-member union, never ExtractionGrade"
      pattern: "grade:\\s*ObservationGrade"
---

<objective>
Create `lib/data/types.ts`: the fourteen names copied verbatim from the sibling at `8fd097a`, the
eleven closed sets this project's later phases key off, and the seed's eighteen new entity types.

Purpose: this file is the single definition site. Every later phase imports its closed sets rather
than restating them, and the one type decision the roadmap turns on — that `EXTRACTED` is
unconstructible on an authored observation — is made here, in the type system, not in a runtime
check. This plan serves REQ-FR-21a's grade-unconstructibility half and roadmap success criterion 1.

Output: one file, `tsc`-clean, importing `GovernedKey` and defining no governed sentence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fixtures-types/02-CONTEXT.md
@.planning/phases/02-fixtures-types/02-PATTERNS.md
@.planning/phases/02-fixtures-types/02-VALIDATION.md
</context>

<interfaces>
<!-- Already in this repository. `types.ts` imports the first name and must never redeclare it. -->

lib/copy/governed.ts
- `export type GovernedKey = "preview" | "authoredVerification" | "authoredProposals" | "noRedaction" | "memoryStore" | "noOfflineInference" | "pendingReconciliation" | "mediaOnDevice";`
- `export type GovernedSentence = { before: string; strong: string; after: string };`
- `export const GOVERNED: Record<GovernedKey, GovernedSentence>`
- `export const PLATFORM_413` — a ninth export that is deliberately not one of the eight keys.
- The module has zero imports of its own, so importing from it creates no cycle.

Closed-set shape already established in this repository (`lib/copy/governed.ts`) and in the sibling
(`RbacTier` / `RBAC_ORDER` / `RBAC_LABEL`): a string-literal union, plus a typed array or
`Record<Key, …>` beside it. Never a TypeScript `enum`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Copy the sibling's fourteen names verbatim and define the eleven closed sets</name>
  <files>lib/data/types.ts</files>
  <read_first>
    - ../ipv-demo/lib/data/types.ts — the copy source at commit 8fd097a. Read the whole file once. The names to copy and their line numbers: `RbacTier` 19, `RBAC_ORDER` 21, `RBAC_LABEL` 27, `ExtractionGrade` 51, `Provenance` 53, `SystemOfRecord` 71, `EvidenceChain` 87, `CitedFact` 107, `Zone` 132, `AssetClass` 173, `ASSET_CLASS_LABEL` 189, `Machinery` 222, `SetpointBlock` 248, `RootCause5M` 270, `Deviation` 272, `GoverningDoc` 299
    - lib/copy/governed.ts — the `GovernedKey` union this file imports and must never redeclare
    - .planning/phases/02-fixtures-types/02-RESEARCH.md — §Closed Sets (the exact member list for every set below, plus the two flagged discrepancies) and §Common Pitfalls Pitfall 3
    - .planning/phases/02-fixtures-types/02-PATTERNS.md — §`lib/data/types.ts` (the `RbacTier` shape to copy; the do-not-copy list; the enum caution)
    - .planning/phases/02-fixtures-types/02-CONTEXT.md — D-05, D-12, D-18, D-19
  </read_first>
  <action>
Create `lib/data/types.ts` with a header comment naming the copy source (`../ipv-demo/lib/data/types.ts`), the commit `8fd097a` and the date `2026-09-01`, in the style of `scripts/check-tokens.mjs`'s pinned-source header.

Copy verbatim, byte-faithful including their doc comments, exactly these sixteen exported names (D-18's fourteen entries, two of which name a type plus its companion const): `RbacTier`, `RBAC_ORDER`, `RBAC_LABEL`, `ExtractionGrade`, `Provenance`, `SystemOfRecord`, `EvidenceChain`, `CitedFact`, `Zone`, `AssetClass`, `ASSET_CLASS_LABEL`, `Machinery`, `SetpointBlock`, `RootCause5M`, `Deviation`, `GoverningDoc`.

Copy nothing else from the sibling. In particular do not copy `Anchor`, `CaptureSession`, `DecisionState`, `DecisionRecord`, `SceneManifest`, `ManifestZoneEntry` or `ManifestModelEntry` — the seed defines its own `Decision`, `Referral` and `SyncItem` shapes and this project has no 3D scene.

`ExtractionGrade` keeps all three members including `EXTRACTED` (D-12): the copied plant facts carry `EXTRACTED` provenance legitimately, from the ERP and NCR systems of record. It is the authored observation's own grade, defined below, that excludes it.

Then define the eleven closed sets (D-19), each as a string-literal union with a typed member array beside it following the `RbacTier` / `RBAC_ORDER` shape. Members exactly as listed, no additions, no TypeScript `enum` anywhere:

1. `ObservationKind` / `OBSERVATION_KINDS` — the seed's eight `corrosion_visible`, `gland_weep`, `guard_damaged`, `seal_absent`, `leak_evidence`, `label_illegible`, `fixing_missing`, `discolouration`, plus the two D-05 additions `isolation_present` and `gauge_obscured`. Ten members. Add a comment naming D-05 and stating that the two additions exist because the ap003 and gs001 isolation rows and the as001 fogged-gauge row have no honest fit among the eight.
2. `ObservationGrade` / `OBSERVATION_GRADES` — `INFERRED`, `AMBIGUOUS`. Two members. Add a comment naming D-12 and stating that this union is deliberately not `ExtractionGrade`, so the grade meaning *the record states this* cannot be constructed on an authored observation.
3. `ObservationRelation` / `OBSERVATION_RELATIONS` — `evidence`, `context`.
4. `ArtisanTrade` / `ARTISAN_TRADES` — `millwright`, `electrician`, `boilermaker`.
5. `ProposalState` / `PROPOSAL_STATES` — `open`, `accepted`, `rejected`, `superseded`. Four members. Add a comment naming RESEARCH Pitfall 3: EXPERIENCE.md's `decided, pending` row is a rendering state derived from an open proposal with a pending decision, never a fifth member here.
6. `ReconciledState` / `RECONCILED_STATES` — `recorded`, `pending`, `conflict`, `rejected`.
7. `QueueItemState` / `QUEUE_ITEM_STATES` — `queued`, `sending`, `recorded`, `conflict`, `rejected`, `discarded`.
8. `ConflictCode` / `CONFLICT_CODES` — `order_not_found`, `order_closed`, `asset_not_in_order`, `account_mismatch`, `proposal_superseded`, `already_recorded_differently`, `clock_skew`, `referral_evidence_missing`. Eight members. Add a comment stating that `referral_evidence_missing` has no sentence yet and that P9 supplies it, and that AD-9's `already_open` and `not_open` join in P4.
9. `RejectCode` / `REJECT_CODES` — `bad_shape`, `media_too_large`, `unknown_kind`, `unknown_proposal`, `unknown_referral`, `store_evicted`. Six members, with the same note for `unknown_referral`.
10. `ReferralResolution` / `REFERRAL_RESOLUTIONS` — `pending`, `resolved`, `unresolved`.
11. `SyncItemKind` / `SYNC_ITEM_KINDS` — `order_open`, `order_close`, `capture`, `decision`, `referral`. Beside it, per AD-18, export `SYNC_ITEM_SCHEMA_VERSIONS: Record<SyncItemKind, readonly number[]>` with the value `[1]` for each of the five kinds, and a comment stating that no breaking change has shipped, so each kind emits exactly one schema version so far.

Import `GovernedKey` from `lib/copy/governed.ts` with a **type-only** import (`import type { GovernedKey } from "../copy/governed";`). Type-only matters twice: D-19 forbids a second definition of the governed set, and a type-only import is erased by Node's strip-only loader so the later `.mjs` fixture tests can load `lib/data/*.ts` without `lib/copy/governed.ts` being present in their temp trees. Never write a governed sentence's text in this file.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `lib/data/types.ts` contains `export type ExtractionGrade` with all three of `EXTRACTED`, `INFERRED`, `AMBIGUOUS`
    - `lib/data/types.ts` contains `export type ObservationGrade` and the string `EXTRACTED` does not appear on that union's own lines
    - `lib/data/types.ts` contains `import type { GovernedKey }` and no line declaring a `GovernedKey` union
    - `grep -c "^export type\|^export const\|^export interface" lib/data/types.ts` reports at least 30
    - `grep -n "enum " lib/data/types.ts` returns no match
    - `node scripts/check-governed.mjs` exits 0 — no governed sentence has been restated
    - `node scripts/claims-audit.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; node scripts/check-governed.mjs &amp;&amp; node scripts/claims-audit.mjs</automated>
  </verify>
  <done>The fourteen copied names and the eleven closed sets exist in one file, `tsc` is clean, `EXTRACTED` is unconstructible on `ObservationGrade`, and the governed set is imported rather than restated.</done>
</task>

<task type="auto">
  <name>Task 2: Add the seed's eighteen new entity types with the AD narrowings</name>
  <files>lib/data/types.ts</files>
  <read_first>
    - lib/data/types.ts — the file as Task 1 left it; the closed sets below are referenced by name, never re-inlined
    - docs/CAPTURE-PLAN-SEED.md lines 198–216 — §Data model, the shape of every type below stated verbatim by the seed
    - .planning/phases/02-fixtures-types/02-CONTEXT.md — D-18 (the eighteen names and the AD-3/AD-5/AD-6 narrowings), D-19, D-20
    - .planning/phases/02-fixtures-types/02-RESEARCH.md — §Closed Sets (the sets these types reference), §Id Shapes (`wo-NNNN` internal id versus `WO-2026-NNNN` display number)
    - docs/planning-artifacts/epics.md lines 872–884 — Story 2.1's first acceptance criterion, which names the fields `AuthoredObservation`, `VerificationResult`, `Proposal`, `Decision` and `Capture` must carry
  </read_first>
  <action>
Append the seed's new entity types to `lib/data/types.ts`, following the seed's §Data model shape and referencing the closed sets defined in Task 1 by name. Every one of these eighteen names is exported:

`Artisan` — `id`, `name`, `trade: ArtisanTrade`, `competency`, `employee_no`, `rbac_tier: RbacTier`. Add a comment naming D-20 and AD-2: `rbac_tier` is a display-only attribute; no function under `lib/data` reads it and no later access decision may, because authorisation is decided by work-order assignment alone (FR-57).

`Session` — `sid`, `account_id`, `issued_at`, `expires_at`.

`WorkOrder` — `id` (the internal `wo-NNNN` id), `number` (the display `WO-2026-NNNN` string), `title`, `description`, `assigned_to`, `zone_id`, `asset_ids: string[]`, `governing_docs: string[]`, `priority`, `raised_on`, `due_by`, `status: "assigned" | "in_progress" | "closed"`, `provenance: Provenance`. Comment that `id` and `number` are two different strings and are never conflated.

`OrderAsset` — `Machinery & { observation_ids: string[] }`, with a comment that it is server-only and `observation_ids` is stripped from responses.

`AuthoredObservation` — `id`, `asset_id`, `kind: ObservationKind`, `wording: string`, `grade: ObservationGrade`, `drawn_from: string`, `relation: ObservationRelation`. `drawn_from` is a bare record id, not a `Provenance` tuple: Claude's Discretion, resolved to the bare id because the resolver test checks one string and because the full `ObservationProvenance` tuple is composed from the cited record's own provenance by the P3/P5 module. State that in a comment, and state that the id space is a `CitedFact` id or a `Deviation` `id` and nothing else (D-09).

`ObservationProvenance` — `Omit<Provenance, "confidence" | "extractor_hash"> & { confidence: null; extractor: "authored" }` (AD-8).

`Capture` — `id`, `order_id`, `asset_id`, `kind: "photo" | "voice"`, `purpose: "verify" | "evidence"`, `captured_at`, `mime`, `bytes`, `sha256`, `duration_ms?`, `thumb?`, `captured_by: string | null`, `recorded_at`. Comment that the thumbnail's byte cap is a bounded quantity that lives in `lib/limits` in P3 (AD-13), so no number appears here.

`VerificationResult` — `capture_id`, `asset_id`, `outcome: "matched" | "pending"`, `matched_tag`, `matched_serial`, `method: "authored"`, `confidence: null`, `label: GovernedKey`, `verified_at`. Typing `label` as `GovernedKey` is what makes the `GovernedKey` import load-bearing: the sentence is resolved through `GOVERNED` at render time and never restated here.

`Proposal` — `id`, `capture_id`, `asset_id`, `order_id`, `observation`, `provenance: ObservationProvenance`, `issued_at`, `state: ProposalState`.

`Decision` — `id`, `proposal_id`, `outcome: "accept" | "reject"`, `decided_at`, `decided_where_claimed: "online" | "on_device"`, `arrived_via: "immediate" | "queued"`, `decided_by: string | null`, `note?`, `recorded_at`, `device_offset_s`, `reconciled: ReconciledState`, `conflict?: ConflictCode`. Comment the AD-3 narrowing: `arrived_via` is server-derived and the client's `decided_where_claimed` is shown as a claim, never as fact; `decided_by` is server-stamped and any body value is ignored. Comment the AD-5 narrowing: decision identity is `capture_client_id` plus `observation_id`.

`Referral` — `id`, `order_id`, `typed_tag: string | null`, `plate_capture_id`, `capture_ids: string[]`, `note_capture_id?`, `observed_at`, `resolution: ReferralResolution`, `resolved_asset_id: string | null`, `flag_id: string | null`, `raised_by: string | null`, `recorded_at`, `reconciled: ReconciledState`. Comment that a referral has no proposals, no verification, no decision and no `drawn_from`, and that `typed_tag` is the artisan's own claim, recorded as typed and never overwritten.

`Flag` — `id`, `asset_id`, `referral_id`, `raised_by`, `raised_at`, `state: "raised"`. Comment that a flag records that a person raised something, not that a condition obtains; it carries no grade, no wording about the plant's condition and no provenance tuple, and no operation promotes one (AD-19).

`OrderClock` — `order_id`, `account_id`, `segments: { opened_at: string; closed_at: string | null; source: "server" | "device_reconciled" }[]`, `elapsed_s`.

`SyncItem<K>` — generic over the payload: `client_id`, `kind: SyncItemKind`, `schema_version: number`, `order_id`, `created_at`, `attempts`, `state: QueueItemState`, `claimed_account_id`, `payload: K`, `last_result?: SyncItemResult`. Comment AD-18: the emitted set is enumerated beside the type in `SYNC_ITEM_SCHEMA_VERSIONS`, and `claimed_account_id` is compared, never trusted.

`SyncItemResult` — `client_id`, `status: "recorded" | "duplicate" | "conflict" | "rejected"`, `code?: ConflictCode | RejectCode`, `detail: string`, `server?: { verification?: VerificationResult; proposals?: Proposal[]; decision?: Decision; clock?: OrderClock }`.

`WalkPayload` — the seed's shape at line 216, with `schema: "novatek.capture.walk/1"`, `issued_at`, `store`, `account`, the order-and-clock block, `assets[]`, `referrals[]` as their own set never folded into `candidate_facts`, and `redaction: { ran: false; statement: string }`. Keep it as one exported interface; the fields the P8 route actually populates are P8's concern.

Also export `ConflictCode`, `RejectCode` and the remaining closed sets from Task 1 — they are already exported there; do not redeclare them.

Add nothing that reads from `lib/store`, `lib/reconcile` or `lib/access`: this file imports only from `lib/copy/governed.ts` (D-DEP, D-20).
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `lib/data/types.ts` contains all eighteen names: `Artisan`, `Session`, `WorkOrder`, `OrderAsset`, `AuthoredObservation`, `ObservationProvenance`, `Capture`, `VerificationResult`, `Proposal`, `Decision`, `Referral`, `Flag`, `OrderClock`, `SyncItem`, `SyncItemResult`, `WalkPayload` plus the already-defined `ConflictCode` and `RejectCode`
    - `AuthoredObservation` declares `grade: ObservationGrade` and does not mention `ExtractionGrade`
    - `WorkOrder` declares both an `id` field and a separate `number` field
    - `VerificationResult` declares `confidence: null` and `label: GovernedKey`
    - `grep -n "from \"\\.\\./store\\|from \"\\.\\./reconcile\\|from \"\\.\\./access" lib/data/types.ts` returns no match
    - `grep -n "enum " lib/data/types.ts` returns no match
    - `node scripts/check-governed.mjs` exits 0 and `node scripts/claims-audit.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; node scripts/check-governed.mjs &amp;&amp; node scripts/claims-audit.mjs &amp;&amp; npx eslint lib/data/types.ts</automated>
  </verify>
  <done>Every entity type the later phases need is declared once, referencing the closed sets by name, with the AD-3, AD-5, AD-6, AD-8, AD-13, AD-18, AD-19 and D-20 narrowings recorded in comments beside the fields they constrain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the type system → every later phase's runtime | Every closed set defined here is the only thing standing between a later phase and an invented state value; a widened union is a silently accepted illegal state |
| authored plane → extracted plane | `AuthoredObservation.grade` and `ExtractionGrade` describe two different kinds of claim; a single shared union would let an authored sentence carry the ERP's authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Spoofing | `AuthoredObservation.grade` | mitigate | Task 1 defines `ObservationGrade` as its own two-member union over `INFERRED` and `AMBIGUOUS`; `ExtractionGrade` keeps `EXTRACTED` for the copied plant facts only. Task 2 types the field as `ObservationGrade`, never as `ExtractionGrade`, so a grade meaning *the record states this* cannot be constructed on an authored observation at all (D-12, REQ-FR-21a, roadmap success criterion 2). |
| T-2-02 | Tampering | the governed-sentence set | mitigate | Task 1 imports `GovernedKey` type-only from `lib/copy/governed.ts` and declares no sentence text; `node scripts/check-governed.mjs` runs in both tasks' verify and fails on any second literal or any change to `governed.ts`'s export set (D-19, Phase 1 D-07/D-09). |
| T-2-03 | Tampering | the eleven closed sets | mitigate | Each union ships with a typed member array beside it (the sibling's `RBAC_ORDER` pattern), so a member added to the union without the array, or to the array without the union, is a `tsc` failure rather than a silent widening; plan 02-02 adds a runtime membership assertion over the same arrays (D-19). |
| T-2-04 | Repudiation | the loading contract for later fixture tests | mitigate | No TypeScript `enum` is written anywhere in `lib/data`; a `grep -n "enum "` acceptance criterion in both tasks makes the omission checkable. An `enum` would break Node 24's strip-only loading, which is the mechanism every later provenance and hash check depends on to read the real evaluated records (RESEARCH Pattern 2, nuance 3). |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx eslint .` exits 0.
- `node scripts/check-governed.mjs` exits 0 — the eight sentences still have exactly one definition site.
- `node scripts/claims-audit.mjs` exits 0 over the new file's comments and strings.
- `npm run verify` exits 0.
</verification>

<success_criteria>
- `tsc` is clean over `lib/data/types.ts` and every closed set the later phases need — proposal states, conflict and reject codes, referral resolution states, queue item kinds with their emitted schema versions, the observation kind and grade sets — is defined exactly once here (roadmap success criterion 1, D-19).
- No grade meaning *the record states this* is constructible on an authored observation (roadmap success criterion 2, D-12, REQ-FR-21a).
- The eight governed sentences are imported, never restated (D-19).
</success_criteria>

<output>
Create `.planning/phases/02-fixtures-types/02-01-SUMMARY.md` when done
</output>
