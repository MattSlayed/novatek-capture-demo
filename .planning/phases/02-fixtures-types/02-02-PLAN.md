---
phase: 02-fixtures-types
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/data/plant.ts
  - scripts/check-fixture-shape.test.mjs
autonomous: true
requirements: [REQ-FR-21a]

must_haves:
  truths:
    - "Every record an observation or an order will cite exists in this repository verbatim, because the sibling is not present on Vercel and what is copied must be complete here"
    - "The eleven assets on the five orders plus `m-aa605` are present and no other machinery record is (D-08's asset set; the referral target is on nobody's order and is still in the register)"
    - "`ANCHORS`, `CAPTURE_SESSIONS` and the 3D placement table are gone, and the file's own `import type` line no longer names `Anchor` or `CaptureSession` (RESEARCH Pitfall 1)"
    - "The claims audit is clean over the copied surface (roadmap success criterion 4)"
    - "Every closed set defined in 02-01 has a runtime membership assertion, so a member cannot be added or dropped without a red test (D-19)"
  artifacts:
    - path: "lib/data/plant.ts"
      provides: "The trimmed ipv-demo plant subset: 11 machinery records, 4 zones, 6 docs, 3 deviations, 4 people, the prov/erpFact helpers and the *_BY_ID maps"
      contains: "SCENE_VERSION"
      min_lines: 400
    - path: "scripts/check-fixture-shape.test.mjs"
      provides: "Runtime membership assertions over the closed sets and the copied plant subset"
      min_lines: 80
  key_links:
    - from: "lib/data/plant.ts"
      to: "lib/data/types.ts"
      via: "import type of the copied names, with Anchor and CaptureSession removed"
      pattern: "import type \\{[\\s\\S]*?\\} from \"\\./types\""
    - from: "scripts/check-fixture-shape.test.mjs"
      to: "lib/data/plant.ts"
      via: "Node 24 native TypeScript stripping, .ts extension at the .mjs entry point"
      pattern: "from \"\\.\\./lib/data/plant\\.ts\""
---

<objective>
Copy the ipv-demo plant subset into `lib/data/plant.ts` at commit `8fd097a`, trimmed by whole-record
deletion to the eleven assets the five work orders touch plus `m-aa605`, and add the fixture-shape
test that asserts both the copied subset's membership and the closed sets 02-01 defined.

Purpose: every `drawn_from` a Phase 2 observation names has to resolve to a record that is present
in *this* repository — the sibling is not on Vercel. This plan is what makes roadmap success
criterion 2 possible and criterion 4's "claims audit clean over the copied surfaces" true.

Output: one copied and trimmed data module, one test file the existing `fixture-suite` step picks up
with no wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fixtures-types/02-CONTEXT.md
@.planning/phases/02-fixtures-types/02-PATTERNS.md
@.planning/phases/02-fixtures-types/02-VALIDATION.md
@.planning/phases/02-fixtures-types/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Created by plan 02-01. plant.ts imports the first group; the shape test asserts the second. -->

lib/data/types.ts
- Copied from the sibling: `RbacTier`, `RBAC_ORDER`, `RBAC_LABEL`, `ExtractionGrade`, `Provenance`,
  `SystemOfRecord`, `EvidenceChain`, `CitedFact`, `Zone`, `AssetClass`, `ASSET_CLASS_LABEL`,
  `Machinery`, `SetpointBlock`, `RootCause5M`, `Deviation`, `GoverningDoc`.
  `Anchor` and `CaptureSession` are deliberately **not** among them.
- Closed-set member arrays, each beside its union: `OBSERVATION_KINDS` (10), `OBSERVATION_GRADES` (2),
  `OBSERVATION_RELATIONS` (2), `ARTISAN_TRADES` (3), `PROPOSAL_STATES` (4), `RECONCILED_STATES` (4),
  `QUEUE_ITEM_STATES` (6), `CONFLICT_CODES` (8), `REJECT_CODES` (6), `REFERRAL_RESOLUTIONS` (3),
  `SYNC_ITEM_KINDS` (5), `SYNC_ITEM_SCHEMA_VERSIONS`.

scripts/lib/fixtures.mjs
- `repoRoot()`, `withFixture(files, fn)`, `runCheck(scriptRelPath, opts)`, `runCommand(...)`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Copy plant.ts and trim it by whole-record deletion</name>
  <files>lib/data/plant.ts</files>
  <read_first>
    - ../ipv-demo/lib/data/plant.ts — the copy source at commit 8fd097a, read in full (897 lines). Structure: `SCENE_VERSION` 25, `PLANT_NAME` 26, `DOC` 29, `prov()` 37, `ZONES` 64 (z01 66, z02 78, z03 90, z04 101, z05 115), `ZONE_BY_ID` 130, `PEOPLE` 137, `DOCS` 155, `DOC_BY_CODE` 225, `DEVIATIONS` 231 (ncr-0118 233, ncr-0104 255, ncr-0091 278), `DEVIATION_BY_ID` 301, `erpFact()` 307, `MACHINERY` 330, `MACHINERY_BY_ID`/`_BY_TAG` 825, `machineryInZone` 828, `ANCHOR_POSITIONS` 842, `ANCHORS` 865, `ANCHOR_BY_MACHINERY` 876, `anchorsInZone` 878, `CAPTURE_SESSIONS` 888
    - lib/data/types.ts — the sixteen names available to import; confirm `Anchor` and `CaptureSession` are absent before trimming the import line
    - .planning/phases/02-fixtures-types/02-RESEARCH.md — §Copying `plant.ts` (the exact per-record fact-id map and the dependency analysis), §Common Pitfalls Pitfall 1
    - .planning/phases/02-fixtures-types/02-PATTERNS.md — §`lib/data/plant.ts` (the import line, the `DOC` constant, `prov()`, `erpFact()`, the `m-ap003` record and the `ncr-0118` deviation quoted verbatim; the exact trim instruction)
    - .planning/phases/02-fixtures-types/02-CONTEXT.md — the Claude's Discretion list, §Existing Code Insights
  </read_first>
  <action>
Copy `../ipv-demo/lib/data/plant.ts` to `lib/data/plant.ts` and trim it by **deletion of whole records only** — never re-type, never reformat, never re-indent a kept record. A record that is present must be byte-faithful to its sibling at `8fd097a`, so that the claims audit and any later reader are reading the source's own words.

Keep verbatim: the `SCENE_VERSION` line (`"ref-plant/2026.07.3"` — the copied plant's own version, not this project's `FIXTURE_VERSION`), `PLANT_NAME`, the `DOC` constant, `provSeq` and `prov()`, `erpFact()`, all six `DOCS` entries and `DOC_BY_CODE`, all three `DEVIATIONS` (`ncr-0118`, `ncr-0104`, `ncr-0091`) and `DEVIATION_BY_ID`, `ZONE_BY_ID`, `MACHINERY_BY_ID`, `MACHINERY_BY_TAG` and `machineryInZone`.

Delete these nine `MACHINERY` records whole, leaving eleven: `m-ap001`, `m-ap002`, `m-ap004`, `m-aa210`, `m-aa211`, `m-aa301`, `m-cf101`, `m-ap010`, `m-bb002`. The eleven that remain are `m-ap003`, `m-aa101`, `m-aa102`, `m-aa601`, `m-aa602`, `m-aa605`, `m-as001`, `m-bb001`, `m-ac001`, `m-gs001`, `m-an001`. `m-aa605` is on no work order and stays: it is the referral fixture's resolved target, and a referral fixture whose target is absent from the register would exercise only the unresolved path.

Delete the `z05` entry (Reagent Dosing) from `ZONES` whole, leaving `z01` through `z04`. `ZONE_BY_ID` is a derived `.map()` and regenerates correctly with no edit.

Trim `PEOPLE` to the four keys the kept records and the three deviations actually reference — `millwright`, `ndt`, `test`, `qc` — and delete `reliability`, `supervisor` and `planner`, which were referenced only by the excluded `ANCHORS` and `CAPTURE_SESSIONS` blocks. This is the Claude's Discretion call recorded here: trim to four, consistent with the whole-unit deletion applied to `MACHINERY` and `ZONES`. Prove it with the grep in the acceptance criteria rather than trusting the analysis.

Delete in the same pass, not as an afterthought (RESEARCH Pitfall 1): the names `Anchor,` and `CaptureSession,` from the file's own `import type { … } from "./types";` line at the top, and the `ANCHOR_POSITIONS` const, the `ANCHORS` export, the `ANCHOR_BY_MACHINERY` export, the `anchorsInZone` function and the `CAPTURE_SESSIONS` export at the bottom. Leaving those two names on the import line is a hard `tsc` failure (`TS2305: Module './types' has no exported member 'Anchor'`), not a lint nit, because 02-01 deliberately did not copy them.

Leave every kept record's `import`-free internal references alone: `plant.ts` imports its types extension-free (`from "./types"`) and that resolves unchanged both under `tsc` and under Node 24's native TypeScript loading, so no import specifier in this file gains a `.ts` suffix.

Add a header comment above the existing file header naming this repository's copy: the source path `../ipv-demo/lib/data/plant.ts`, the commit `8fd097a`, the date `2026-09-01`, the eleven kept record ids, and the six things deleted (nine machinery records, `z05`, three `PEOPLE` keys, the anchor block, `CAPTURE_SESSIONS`, and the two names on the import line). Follow `scripts/check-tokens.mjs`'s pinned-source header style. Do not use the word "simulation" in any form.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npx eslint lib/data/plant.ts` exits 0
    - `node -e "import('./lib/data/plant.ts').then(m=>{const ids=[...m.MACHINERY_BY_ID.keys()].sort();console.log(ids.length, ids.join(','))})"` prints `11` followed by `m-aa101,m-aa102,m-aa601,m-aa602,m-aa605,m-an001,m-ac001,m-ap003,m-as001,m-bb001,m-gs001`
    - `grep -c "id: \"m-" lib/data/plant.ts` reports 11
    - `grep -n "Anchor\|CaptureSession\|ANCHOR_POSITIONS\|anchorsInZone\|CAPTURE_SESSIONS" lib/data/plant.ts` returns no match
    - `grep -n "PEOPLE\.\(reliability\|supervisor\|planner\)" lib/data/plant.ts` returns no match and `grep -n "  reliability:\|  supervisor:\|  planner:" lib/data/plant.ts` returns no match
    - `grep -n "id: \"z05\"" lib/data/plant.ts` returns no match; `grep -c "id: \"z0" lib/data/plant.ts` reports 4
    - `grep -c "id: \"ncr-" lib/data/plant.ts` reports 3
    - `lib/data/plant.ts` contains `export const SCENE_VERSION = "ref-plant/2026.07.3";`
    - `node scripts/claims-audit.mjs` exits 0 over the copied surface
    - `node scripts/check-governed.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; node scripts/claims-audit.mjs &amp;&amp; node scripts/check-governed.mjs</automated>
  </verify>
  <done>The eleven records the five orders and the referral fixture need are present verbatim, the anchor and capture-session surface is gone along with the two names it orphaned on the import line, and the claims audit is clean over the copy.</done>
</task>

<task type="auto">
  <name>Task 2: Add scripts/check-fixture-shape.test.mjs with the closed-set and plant-subset assertions</name>
  <files>scripts/check-fixture-shape.test.mjs</files>
  <read_first>
    - scripts/check-tokens.test.mjs — the `node:test` file shape this repository uses (imports, one behaviour per `test()`, `assert` from `node:assert/strict`)
    - scripts/lib/fixtures.mjs — `repoRoot()`, `withFixture`, `runCheck`; this test needs none of them yet, but later plans extend this file and will
    - lib/data/types.ts — the closed-set member arrays being asserted
    - lib/data/plant.ts — the subset being asserted, as Task 1 left it
    - .planning/phases/02-fixtures-types/02-RESEARCH.md — §Closed Sets (the exact member lists), §Copying `plant.ts` (the per-record fact-id table and `f-aa605-cert`'s confirmed value)
    - .planning/phases/02-fixtures-types/02-PATTERNS.md — §Shared Patterns, "Node native TypeScript loading — `.ts` extension only at the `.mjs` entry point"
  </read_first>
  <action>
Create `scripts/check-fixture-shape.test.mjs`. It is a plain `node:test` file, not a check script, so it needs no `verify.mjs` entry: the existing `fixture-suite` step already runs `node --test scripts/**/*.test.mjs`. Later plans in this phase extend the same file.

Import the modules under test with the explicit `.ts` extension, which Node 24 requires only at this `.mjs` entry point: the closed-set arrays from `../lib/data/types.ts`, and `MACHINERY_BY_ID`, `MACHINERY_BY_TAG`, `ZONE_BY_ID`, `DEVIATION_BY_ID`, `PEOPLE`, `DOC_BY_CODE`, `SCENE_VERSION` and the module namespace from `../lib/data/plant.ts`.

Closed-set assertions (D-19), one `test()` per set, each asserting the array's exact contents in order:
`OBSERVATION_KINDS` is the ten `corrosion_visible`, `gland_weep`, `guard_damaged`, `seal_absent`, `leak_evidence`, `label_illegible`, `fixing_missing`, `discolouration`, `isolation_present`, `gauge_obscured`; `OBSERVATION_GRADES` is `INFERRED`, `AMBIGUOUS` and — asserted separately and named for D-12 — does not include `EXTRACTED`; `OBSERVATION_RELATIONS` is `evidence`, `context`; `ARTISAN_TRADES` is `millwright`, `electrician`, `boilermaker`; `PROPOSAL_STATES` is the four `open`, `accepted`, `rejected`, `superseded` with a comment naming RESEARCH Pitfall 3 (there is no fifth member); `RECONCILED_STATES` is `recorded`, `pending`, `conflict`, `rejected`; `QUEUE_ITEM_STATES` is the six `queued`, `sending`, `recorded`, `conflict`, `rejected`, `discarded`; `CONFLICT_CODES` is the eight ending in `referral_evidence_missing`; `REJECT_CODES` is the six including `unknown_referral`; `REFERRAL_RESOLUTIONS` is `pending`, `resolved`, `unresolved`; `SYNC_ITEM_KINDS` is `order_open`, `order_close`, `capture`, `decision`, `referral`; and `SYNC_ITEM_SCHEMA_VERSIONS` maps each of those five kinds to `[1]`.

Plant-subset assertions, one `test()` per group:
the sorted `MACHINERY_BY_ID` key list is exactly the eleven ids `m-aa101`, `m-aa102`, `m-aa601`, `m-aa602`, `m-aa605`, `m-an001`, `m-ac001`, `m-ap003`, `m-as001`, `m-bb001`, `m-gs001`; `MACHINERY_BY_TAG` resolves the eleven tags `20LAC10AP003`, `20LAC10AA101`, `20LAC10AA102`, `20HAD10AA601`, `20HAD10AA602`, `20LBA10AA605`, `20LAC30AS001`, `20GHC10BB001`, `20GHC20AC001`, `20BFA10GS001`, `20LAC10AN001` to those same records; `ZONE_BY_ID` has exactly `z01`, `z02`, `z03`, `z04` and no `z05`; `DEVIATION_BY_ID` has exactly `ncr-0118`, `ncr-0104`, `ncr-0091`; `PEOPLE` has exactly the four keys `millwright`, `ndt`, `test`, `qc`; `DOC_BY_CODE` has six entries; `SCENE_VERSION` equals `ref-plant/2026.07.3`.

Fact-id assertions: for each of the eleven records, the sorted `facts.map(f => f.id)` equals the list RESEARCH §Copying `plant.ts` gives for that record — `m-ap003` five ids, `m-aa101` three, `m-aa102` two, `m-aa601` four, `m-aa602` four, `m-aa605` two, `m-as001` two, `m-bb001` three, `m-ac001` two, `m-gs001` three, `m-an001` two, thirty-two facts across the subset. Assert additionally that `f-aa605-cert`'s `value` is exactly `Certified`, because the referral fixture's comment quotes it and a drift there would falsify the row the whole UJ-5 story turns on.

Exclusion assertions: importing the plant module namespace, assert that `ANCHORS`, `ANCHOR_BY_MACHINERY`, `anchorsInZone` and `CAPTURE_SESSIONS` are all `undefined` on it, naming Pitfall 1 in the message.

Referential-integrity assertions over the trimmed subset: every kept record's `zone_id` is a key of `ZONE_BY_ID`; every id in every kept record's `deviations` array is a key of `DEVIATION_BY_ID`; every code in every kept record's `governing_docs` array is a key of `DOC_BY_CODE`. These are what prove the trim left no dangling reference.

Every assertion message names the record or set it is about, so a red test says which one moved.
  </action>
  <acceptance_criteria>
    - `node --test scripts/check-fixture-shape.test.mjs` exits 0
    - The file contains `from "../lib/data/plant.ts"` and `from "../lib/data/types.ts"` — the `.ts` extension is present at this `.mjs` entry point
    - The file asserts `OBSERVATION_GRADES` does not include the string `EXTRACTED`
    - The file asserts `SCENE_VERSION` equals `ref-plant/2026.07.3`
    - Deleting a member from any closed-set array in `lib/data/types.ts` makes this test exit non-zero (demonstrate once locally, then restore)
    - `npm run verify` exits 0 — the new test is picked up by the existing `fixture-suite` step with no `verify.mjs` change
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/check-fixture-shape.test.mjs</automated>
  </verify>
  <done>The closed sets and the copied subset both have a runtime assertion, so a member or a record cannot move without a red test, and the trim is proved to have left no dangling zone, deviation or document reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the sibling repository → this repository | The sibling is the system of record for every copied fact and is not present on Vercel; once copied, this repository is the only source, and drift here is invisible from the source side |
| the copied subset → every later citation | A `drawn_from` naming a record that was trimmed away, or a record whose value was edited after copying, makes an audited claim unverifiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-05 | Tampering | copied plant records diverging from their source of record | mitigate | Task 1 trims by whole-record deletion only, so a record that is present is byte-faithful to `8fd097a`; the header records the source path, commit and date; plan 02-07's content hash (D-14) pins the file so any later edit fails `verify` until it is re-pinned and the fixture version bumped. |
| T-2-06 | Repudiation | dangling references left by the trim | mitigate | Task 2 asserts every kept record's `zone_id`, `deviations[]` and `governing_docs[]` resolve inside the trimmed subset, so a citation cannot later resolve to a record the trim removed. |
| T-2-07 | Spoofing | a prohibited claim entering `lib/` through copied text | mitigate | `node scripts/claims-audit.mjs` is an acceptance criterion of Task 1 and a `verify` step; the orchestrator's pre-run of the 27-rule register over verbatim copies of `plant.ts` and `types.ts` returned zero hits, so any hit here means the copy was edited rather than copied (Phase 1 D-17, D-18). |
| T-2-08 | Denial of service | a TypeScript construct that breaks strip-only loading | accept | The copied file uses only interfaces, `as const` objects and functions — no `enum`, no decorator, no parameter property. Accepted rather than mitigated because Task 2's own test is the detector: it loads `plant.ts` through Node's native stripping, so an unsupported construct fails the fixture suite immediately and loudly. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 with `plant.ts` present.
- `node --test scripts/check-fixture-shape.test.mjs` exits 0.
- `node scripts/claims-audit.mjs` exits 0 over `lib/data/plant.ts`.
- `npm run verify` exits 0.
</verification>

<success_criteria>
- The eleven assets the five work orders touch, plus `m-aa605`, are present in `lib/data/plant.ts` verbatim with the zones, people, docs and deviations they reference (phase boundary, roadmap success criterion 2's precondition).
- The claims audit is clean over the copied surface (roadmap success criterion 4).
- `tsc` is clean over `lib/data/plant.ts` with `Anchor` and `CaptureSession` removed from its own import line (roadmap success criterion 1, RESEARCH Pitfall 1).
</success_criteria>

<output>
Create `.planning/phases/02-fixtures-types/02-02-SUMMARY.md` when done
</output>
