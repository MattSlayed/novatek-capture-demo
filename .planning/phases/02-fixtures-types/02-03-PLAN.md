---
phase: 02-fixtures-types
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - lib/data/artisans.ts
  - lib/data/orders.ts
  - scripts/check-fixture-shape.test.mjs
autonomous: true
requirements: [REQ-FR-21a]

must_haves:
  truths:
    - "The three doors exist as accounts: S. Mabaso, K. Naidoo and J. van Wyk, each with their assigned order ids and a display-only `rbac_tier` (D-20)"
    - "K. Naidoo carries `site_supervisor` and the other two carry `field_technician`, and no function under `lib/data` reads the field (D-20, AD-2, FR-57)"
    - "The five work orders WO-2026-0142, 0151, 0137, 0129 and 0133 exist, each carrying its internal `wo-NNNN` id and its display number as two separate strings that are never conflated (D-CONV)"
    - "Every `asset_ids` entry resolves in the copied plant subset and every `governing_docs` code resolves in `DOC_BY_CODE`, so an order can never name an asset or a document that is not there"
    - "`lib/data` imports nothing from `lib/store`, `lib/reconcile` or `lib/access` (D-20, D-DEP)"
  artifacts:
    - path: "lib/data/artisans.ts"
      provides: "The three Artisan accounts with their assigned order ids"
      contains: "acc-mabaso"
    - path: "lib/data/orders.ts"
      provides: "The five WorkOrder records with their asset sets"
      contains: "WO-2026-0142"
    - path: "scripts/check-fixture-shape.test.mjs"
      provides: "Extended with the artisan and order integrity assertions"
      contains: "ORDERS_BY_ID"
  key_links:
    - from: "lib/data/orders.ts asset_ids"
      to: "lib/data/plant.ts MACHINERY_BY_ID"
      via: "every asset id resolves in the copied subset, asserted in the shape test"
      pattern: "MACHINERY_BY_ID"
    - from: "lib/data/artisans.ts"
      to: "lib/data/orders.ts assigned_to"
      via: "every order's assigned_to is an artisan id and every artisan's order ids are real orders"
      pattern: "assigned_to"
---

<objective>
Author `lib/data/artisans.ts` and `lib/data/orders.ts`: the three doors and the five work orders the
preview's walkthrough runs on, and extend the fixture-shape test so an order can never name an asset,
a document or an account that is not there.

Purpose: these two files are the assignment plane. P3's `lib/access/scope.ts` decides authorisation by
work-order assignment alone, so the shape of `assigned_to` and `asset_ids` here is the shape that
decision will read. `rbac_tier` is carried and displayed and read by nothing (D-20) — this plan is
where that discipline starts, and where it is asserted.

Output: two small data modules and an extended shape test. This plan serves the phase's REQ-FR-21a by
laying the order set every authored observation's asset must belong to.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fixtures-types/02-CONTEXT.md
@.planning/phases/02-fixtures-types/02-PATTERNS.md
@.planning/phases/02-fixtures-types/02-VALIDATION.md
@.planning/phases/02-fixtures-types/02-02-SUMMARY.md
</context>

<interfaces>
<!-- Created by plans 02-01 and 02-02. -->

lib/data/types.ts
- `Artisan { id, name, trade: ArtisanTrade, competency, employee_no, rbac_tier: RbacTier }`
- `WorkOrder { id, number, title, description, assigned_to, zone_id, asset_ids: string[],
   governing_docs: string[], priority, raised_on, due_by, status, provenance: Provenance }`
- `Provenance { source_uri, source_version, extracted_at, extractor_hash, confidence, grade,
   source_label, system_of_record }`
- `SystemOfRecord = "ERP" | "Document Control" | "NCR Register" | "Calibration Register" | "Capture Session"`
- `RbacTier = "field_technician" | "site_supervisor" | "management"`
- `ArtisanTrade = "millwright" | "electrician" | "boilermaker"`

lib/data/plant.ts
- `MACHINERY_BY_ID`, `MACHINERY_BY_TAG`, `ZONE_BY_ID`, `DOC_BY_CODE`, `DEVIATION_BY_ID`, `PEOPLE`
- `prov()` and `erpFact()` are module-private in the copied file and are **not** exported. Order
  provenance is written as a literal `Provenance` object in `orders.ts`; do not edit `plant.ts` to
  export its helpers.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author the three artisan accounts and the five work orders</name>
  <files>lib/data/artisans.ts, lib/data/orders.ts</files>
  <read_first>
    - lib/data/types.ts — `Artisan`, `WorkOrder`, `Provenance`, `SystemOfRecord`, `RbacTier`, `ArtisanTrade`
    - lib/data/plant.ts — `PEOPLE.millwright` (the name and competency the Mabaso account must match), `MACHINERY_BY_ID` (the eleven asset ids), `ZONE_BY_ID` (`z01`–`z04`), each kept record's own `governing_docs` array, and `DOC_BY_CODE`'s six codes
    - docs/CAPTURE-PLAN-SEED.md lines 217–226 — the Fixtures table: the three doors, their tiers, their orders and their assets, stated verbatim
    - .planning/phases/02-fixtures-types/02-RESEARCH.md — §Id Shapes (account `acc-<surname>`; order internal `wo-NNNN` with a separate display `WO-2026-NNNN`)
    - .planning/phases/02-fixtures-types/02-PATTERNS.md — §`lib/data/artisans.ts` and `lib/data/orders.ts` (the array-plus-`_BY_ID`-map convention)
    - .planning/phases/02-fixtures-types/02-CONTEXT.md — D-20 and the Claude's Discretion entry on persona ids, employee numbers and order id shapes
  </read_first>
  <action>
Create `lib/data/artisans.ts` exporting `ARTISANS: Artisan[]` and `ARTISAN_BY_ID = new Map(ARTISANS.map(a => [a.id, a]))`, following `plant.ts`'s array-plus-derived-map convention. Three records, in this order:

`acc-mabaso` — name `S. Mabaso`, trade `millwright`, competency `Millwright, Red Seal`, employee_no `EMP-4471`, rbac_tier `field_technician`. The name and competency are the same strings as `PEOPLE.millwright` in `plant.ts`: this is the same person who raised NCR-2026-0118. Write them as literals here rather than importing `PEOPLE`, so the account's own content is inside the D-14 hash, and let Task 2's test assert the two agree.

`acc-naidoo` — name `K. Naidoo`, trade `electrician`, competency `Electrician, Trade Tested`, employee_no `EMP-4208`, rbac_tier `site_supervisor`.

`acc-vanwyk` — name `J. van Wyk`, trade `boilermaker`, competency `Boilermaker, Red Seal`, employee_no `EMP-4635`, rbac_tier `field_technician`.

Above `ARTISANS`, write a comment naming D-20, AD-2 and FR-57: `rbac_tier` is carried so a surface can display which tier a person holds, and is read by nothing. Authorisation is decided by work-order assignment alone. K. Naidoo carries `site_supervisor` because Z-04 is supervisor-restricted in the inherited plant, and that fact must not become an access rule here or in any later phase.

Also export `ORDER_IDS_BY_ARTISAN: Record<string, readonly string[]>` mapping `acc-mabaso` to `["wo-0142", "wo-0151"]`, `acc-naidoo` to `["wo-0137"]` and `acc-vanwyk` to `["wo-0129", "wo-0133"]` — the assigned order ids D-CONV's boundary calls for, kept beside the accounts rather than derived, so the assignment is readable in one place.

Create `lib/data/orders.ts` exporting `ORDERS: WorkOrder[]` and `ORDER_BY_ID = new Map(ORDERS.map(o => [o.id, o]))`. Five records, in this order, each with `id` and `number` as two distinct strings — the internal id is what every route path and every `SyncItem.order_id` uses, the display number is what a surface renders, and they are never conflated:

`wo-0142` / `WO-2026-0142` — title `Strip and assess transfer set C`, assigned_to `acc-mabaso`, zone_id `z01`, asset_ids `["m-ap003", "m-aa101", "m-aa102"]`, priority `High`, raised_on `2026-07-20`, due_by `2026-07-31`, status `assigned`.

`wo-0151` / `WO-2026-0151` — title `Duplex strainer element clean`, assigned_to `acc-mabaso`, zone_id `z02`, asset_ids `["m-as001"]`, priority `Medium`, raised_on `2026-07-22`, due_by `2026-08-05`, status `assigned`.

`wo-0137` / `WO-2026-0137` — title `Protection test — set C feeder`, assigned_to `acc-naidoo`, zone_id `z04`, asset_ids `["m-gs001", "m-an001"]`, priority `High`, raised_on `2026-07-18`, due_by `2026-07-30`, status `assigned`.

`wo-0129` / `WO-2026-0129` — title `Relief valve re-cert support`, assigned_to `acc-vanwyk`, zone_id `z02`, asset_ids `["m-aa601", "m-aa602"]`, priority `High`, raised_on `2026-07-15`, due_by `2026-07-29`, status `assigned`.

`wo-0133` / `WO-2026-0133` — title `Surge vessel inspection prep`, assigned_to `acc-vanwyk`, zone_id `z03`, asset_ids `["m-bb001", "m-ac001"]`, priority `Medium`, raised_on `2026-07-17`, due_by `2026-08-07`, status `assigned`.

Every order's `description` is one plain sentence naming the work and where, under the eight voice rules and the PRD §3 vocabulary: no cause, no severity claim, no figure, no verb implying a model looked. `m-aa605` appears in no order's `asset_ids` — it is deliberately on nobody's order and reaches the fixtures only through the register and the referral.

Each order's `governing_docs` is the deduplicated, sorted union of its own assets' `governing_docs` codes as they appear in `plant.ts`, so every code resolves in `DOC_BY_CODE`. Derive the list by reading the assets' arrays; do not invent a code.

Each order's `provenance` is a literal `Provenance` object with `system_of_record: "ERP"`, `source_label: "Maintenance management system"`, `source_uri` of the form `erp://work-order/<display number>`, `source_version: "read 2026-07-26"` (matching the read date the copied plant's ERP facts carry), `extracted_at: "2026-07-26T06:12:41+02:00"`, `confidence: 0.99`, `grade: "EXTRACTED"` and a stable `extractor_hash` string. `EXTRACTED` is correct and legitimate here: a work order is a record the ERP states, not an authored observation. Do not call `plant.ts`'s `prov()` — it is module-private and `plant.ts` stays untouched.

Neither file imports from `lib/store`, `lib/reconcile` or `lib/access` (D-20, D-DEP). Neither file contains a function that reads `rbac_tier`.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 and `npx eslint lib/data/artisans.ts lib/data/orders.ts` exits 0
    - `node -e "import('./lib/data/artisans.ts').then(m=>console.log(m.ARTISANS.map(a=>a.id+':'+a.rbac_tier).join(' ')))"` prints `acc-mabaso:field_technician acc-naidoo:site_supervisor acc-vanwyk:field_technician`
    - `node -e "import('./lib/data/orders.ts').then(m=>console.log(m.ORDERS.map(o=>o.id+'/'+o.number).join(' ')))"` prints `wo-0142/WO-2026-0142 wo-0151/WO-2026-0151 wo-0137/WO-2026-0137 wo-0129/WO-2026-0129 wo-0133/WO-2026-0133`
    - `grep -n "m-aa605" lib/data/orders.ts` returns no match
    - `grep -n "rbac_tier" lib/data/orders.ts` returns no match, and `grep -n "rbac_tier" lib/data/artisans.ts` matches only the three record fields and the comment above `ARTISANS`
    - `grep -n "from \"\\.\\./store\|from \"\\.\\./reconcile\|from \"\\.\\./access" lib/data/artisans.ts lib/data/orders.ts` returns no match
    - `node scripts/claims-audit.mjs` exits 0 and `node scripts/check-governed.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; node scripts/claims-audit.mjs &amp;&amp; node scripts/check-governed.mjs</automated>
  </verify>
  <done>The three doors and the five orders exist with the seed's numbers and asset sets, the internal id and the display number are two fields, and `rbac_tier` is carried and read by nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the fixture-shape test with the artisan and order integrity assertions</name>
  <files>scripts/check-fixture-shape.test.mjs</files>
  <read_first>
    - scripts/check-fixture-shape.test.mjs — the file as plan 02-02 left it; add to it, do not restructure it
    - lib/data/artisans.ts and lib/data/orders.ts — the records being asserted, as Task 1 left them
    - lib/data/plant.ts — `MACHINERY_BY_ID`, `ZONE_BY_ID`, `DOC_BY_CODE`, `PEOPLE`
    - .planning/phases/02-fixtures-types/02-PATTERNS.md — §Shared Patterns, the `.ts`-extension-at-the-entry-point rule
  </read_first>
  <action>
Append tests to `scripts/check-fixture-shape.test.mjs`, importing `ARTISANS`, `ARTISAN_BY_ID` and `ORDER_IDS_BY_ARTISAN` from `../lib/data/artisans.ts` and `ORDERS`, `ORDER_BY_ID` from `../lib/data/orders.ts`, both with the explicit `.ts` extension.

Assert, one `test()` per group, each message naming the record it is about:

Accounts — `ARTISANS` has exactly three records with ids `acc-mabaso`, `acc-naidoo`, `acc-vanwyk`; the tiers are `field_technician`, `site_supervisor`, `field_technician` in that order (D-20); every `trade` is a member of `ARTISAN_TRADES`; every `rbac_tier` is a member of `RBAC_ORDER`; `ARTISAN_BY_ID.get("acc-mabaso").name` equals `PEOPLE.millwright.name` and its `competency` equals `PEOPLE.millwright.competency`, so the account and the plant record cannot drift apart silently.

Orders — `ORDERS` has exactly five records; the id list is `wo-0142`, `wo-0151`, `wo-0137`, `wo-0129`, `wo-0133`; the number list is the five `WO-2026-NNNN` strings; no order's `id` equals its own `number` and no order's `number` appears as a key of `ORDER_BY_ID`, which is what makes the two-field discipline checkable rather than asserted; every `status` is `assigned`; every `provenance.system_of_record` is `ERP`.

Referential integrity — every order's `assigned_to` is a key of `ARTISAN_BY_ID`; every order's `zone_id` is a key of `ZONE_BY_ID`; every id in every order's `asset_ids` is a key of `MACHINERY_BY_ID`; every code in every order's `governing_docs` is a key of `DOC_BY_CODE`; the union of all five orders' `asset_ids` is exactly the ten ids `m-ap003`, `m-aa101`, `m-aa102`, `m-as001`, `m-gs001`, `m-an001`, `m-aa601`, `m-aa602`, `m-bb001`, `m-ac001`, and `m-aa605` is in `MACHINERY_BY_ID` but in no order's `asset_ids` — the assertion that carries the referral fixture's whole point.

Assignment symmetry — for each artisan id, `ORDER_IDS_BY_ARTISAN[id]` equals the sorted ids of the orders whose `assigned_to` is that artisan, so the two directions of the assignment cannot disagree.

Display-only tier — assert that the text of `lib/data/artisans.ts` and `lib/data/orders.ts`, read as source, contains no `function` or arrow-function body that reads `rbac_tier`: a source-text assertion that the only occurrences of `rbac_tier` in `artisans.ts` are the three record fields and the comment, and that `orders.ts` does not mention it at all (D-20, AD-2, FR-57). Read the files with `node:fs/promises` relative to `repoRoot()` from `scripts/lib/fixtures.mjs`, and strip `//` and block comments before counting so the comment above `ARTISANS` cannot make the assertion self-defeating.
  </action>
  <acceptance_criteria>
    - `node --test scripts/check-fixture-shape.test.mjs` exits 0
    - The file asserts `m-aa605` is a key of `MACHINERY_BY_ID` and appears in no order's `asset_ids`
    - The file asserts `ARTISAN_BY_ID.get("acc-mabaso").name === PEOPLE.millwright.name`
    - Changing any order's `assigned_to` to an unknown account id makes this test exit non-zero (demonstrate once locally, then restore)
    - Adding a sixth entry to `ORDERS` without updating `ORDER_IDS_BY_ARTISAN` makes this test exit non-zero (demonstrate once locally, then restore)
    - `npm run verify` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/check-fixture-shape.test.mjs</automated>
  </verify>
  <done>Every order resolves to real accounts, zones, assets and documents; the internal id and the display number are proved to be two different strings; `m-aa605` is proved to be in the register and on nobody's order; and `rbac_tier` is proved to be read by nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the assignment plane → P3's authorisation decision | `assigned_to` and `asset_ids` are the only inputs the single named accessor will read; anything else that looks authoritative here invites a second, wrong access rule |
| the display plane → the decision plane | `rbac_tier` is rendered to a person and must never reach a code path that decides what a person may reach |
| the order record → the route path | The internal `wo-NNNN` id and the display `WO-2026-NNNN` number are two different strings; conflating them would make a rendered label a usable identity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-09 | Elevation of privilege | `Artisan.rbac_tier` | mitigate | Task 1 carries the field with a comment naming D-20, AD-2 and FR-57 and writes no function that reads it; Task 2 asserts from source text that `artisans.ts` mentions it only as a record field and `orders.ts` never mentions it, with comments stripped before counting so the warning comment cannot satisfy its own assertion. |
| T-2-10 | Spoofing | the internal order id versus the display number | mitigate | Task 1 declares `id` and `number` as separate fields; Task 2 asserts no order's `id` equals its `number` and that no display number is a key of `ORDER_BY_ID`, so a rendered label cannot be replayed as an identity in a route path or a `SyncItem.order_id` (D-CONV). |
| T-2-11 | Repudiation | an order naming an asset, document or account that is not there | mitigate | Task 2 asserts every `assigned_to`, `zone_id`, `asset_ids` entry and `governing_docs` code resolves in the copied plant subset and the account map, and that the two directions of the assignment agree. |
| T-2-12 | Tampering | the account's identity drifting from the plant record | mitigate | Task 2 asserts `acc-mabaso`'s name and competency equal `PEOPLE.millwright`'s, so the millwright who raised NCR-2026-0118 and the millwright who holds `wo-0142` cannot become two different people through an unnoticed edit. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 with all four `lib/data` modules present.
- `node --test scripts/check-fixture-shape.test.mjs` exits 0.
- `node scripts/claims-audit.mjs` exits 0 over the two new files.
- `npm run verify` exits 0.
</verification>

<success_criteria>
- The three artisans exist as accounts with their assigned order ids and a display-only `rbac_tier`, and the five orders WO-2026-0142, 0151, 0137, 0129 and 0133 exist (phase boundary; D-20).
- `tsc` is clean over `lib/data/artisans.ts` and `lib/data/orders.ts` (roadmap success criterion 1).
- No function under `lib/data` reads `rbac_tier`, proved from source rather than asserted (D-20, AD-2, FR-57).
</success_criteria>

<output>
Create `.planning/phases/02-fixtures-types/02-03-SUMMARY.md` when done
</output>
