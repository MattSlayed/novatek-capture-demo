---
phase: 03-server-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/limits/index.ts
  - lib/limits/index.test.mjs
  - lib/session/key.ts
  - lib/session/key.test.mjs
  - lib/data/types.ts
  - lib/copy/conflicts.ts
  - lib/copy/conflicts.test.mjs
  - scripts/check-fixture-shape.test.mjs
  - scripts/verify.mjs
  - scripts/verify.test.mjs
  - package.json
autonomous: true
requirements: [REQ-FR-8, REQ-FR-11, REQ-FR-19]

must_haves:
  truths:
    - "D-06: `not_open` joins `ConflictCode` and `CONFLICT_CODES` in this phase with its sentence and next act written here; `already_open` stays in P4 and no code is introduced for it"
    - "D-02: the sync route's item ceiling and encoded-byte ceiling are named exports of `lib/limits`, and the client's outbound budget is not decided here"
    - "D-07: the queued clamp's tolerance bounds (future tolerance and the slack below `issued_at`) are named exports of `lib/limits`, so the clamp reads them rather than restating them"
    - "Every bounded quantity this phase needs is one named export in `lib/limits`, read by both sides, and no later module restates a number (AD-13)"
    - "`OrderClock.segments` retains the device-claimed start and the server-measured offset as two optional fields, so neither silently replaces the other (REQ-FR-11)"
    - "`Proposal` carries `observation_id`, so an issued proposal hands back the half of AD-5's identity pair a client cannot know on its own and a decision can name the triple its id was derived from (AD-5)"
    - "The session signing key is resolved lazily on first use by one module shared by the cookie and the proposal-id derivation, and production refuses to serve without `CAPTURE_SESSION_KEY`"
    - "A `lib/**/*.test.mjs` unit suite runs inside `npm run verify`, and the gate fails rather than passing vacuously when that glob matches nothing"
  artifacts:
    - path: "lib/limits/index.ts"
      provides: "Every bounded quantity first needed by the server seam, one export each"
      min_lines: 60
    - path: "lib/session/key.ts"
      provides: "The one lazily-resolved HMAC signing key, shared by the cookie and the proposal-id derivation"
      min_lines: 30
    - path: "lib/copy/conflicts.ts"
      provides: "One sentence and one next-act list per ConflictCode, RejectCode and transport error code, including not_open"
      contains: "not_open"
    - path: "lib/data/types.ts"
      provides: "not_open in the conflict closed set; the retained segment fields; the proposal's observation id"
      contains: "device_claimed_opened_at"
  key_links:
    - from: "scripts/verify.mjs STEPS"
      to: "lib/**/*.test.mjs"
      via: "a unit-suite step immediately after fixture-suite, before next-build"
      pattern: "unit-suite"
    - from: "lib/copy/conflicts.ts"
      to: "lib/data/types.ts"
      via: "keying both records by the ConflictCode and RejectCode closed sets so a new code cannot ship without a sentence"
      pattern: "Record<ConflictCode"
---

<objective>
Lay the four leaves every later plan in this phase imports: the bounded quantities, the one signing key,
the three additive type changes, and one sentence per refusal code. Wire a `lib/**/*.test.mjs` unit suite
into the gate so every plan after this one has somewhere to prove pure logic without a build or a server.

Purpose: AD-13 says a bounded value is code with one name, read by both sides. AD-5 and FR-1 both sign
with the same secret, so the resolver is one module or it is two subtly different ones. D-06 says
`not_open` is a code this phase must emit, and a code without a sentence is half a refusal. Nothing
downstream can be written deterministically until all four exist.

Output: two leaf modules with their unit tests, two additive edits to `types.ts` with the closed-set test
updated in lockstep, one copy module with its unit test, and a nineteenth gate step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-server-seam/03-CONTEXT.md
@.planning/phases/03-server-seam/03-RESEARCH.md
@.planning/phases/03-server-seam/03-PATTERNS.md
@.planning/phases/03-server-seam/03-VALIDATION.md
</context>

<interfaces>
<!-- Already in this repository. Read the files themselves; these are the contracts this plan extends. -->

lib/data/types.ts
- `ConflictCode` (line 367) — a string-literal union of eight members, with `CONFLICT_CODES` (line 377)
  as a same-order array beside it. Never a TypeScript enum (the file header states why).
- `RejectCode` (line 392) — six members, with `REJECT_CODES` (line 400) beside it.
- `OrderClock` (line 632) — `segments: { opened_at; closed_at; source: "server" | "device_reconciled" }[]`.
- `Decision` (line 581) — carries `device_offset_s: number`, the naming precedent the new segment field
  mirrors.
- `Capture` (line 530) — already carries a comment saying the thumbnail's byte cap lives in `lib/limits`.

scripts/check-fixture-shape.test.mjs
- Line 127: `test("CONFLICT_CODES is the closed set of eight, ending in referral_evidence_missing", ...)`
  with an exact `assert.deepStrictEqual` over the membership array. Adding a member without editing this
  test turns the addition into a red build.

scripts/verify.mjs
- `STEPS` — eighteen entries in D-20's fixed order. `fixture-suite` is the eighth,
  `{ id: "fixture-suite", command: process.execPath, args: ["--test", "scripts/**/*.test.mjs"] }`.
  Verified this session: that glob DOES match nested directories (it already collects
  `scripts/lib/server.test.mjs` and `scripts/lib/support.test.mjs`).

scripts/verify.test.mjs
- `EXPECTED_ORDER` (line 29) — the eighteen ids; the order test at line 51 asserts `deepEqual` against it.
- Line 287 `assert.equal(kept.length, 16)` for the `VERCEL: "1"` case; lines 295, 310, 321
  `assert.equal(kept.length, 18)` for the unset and distractor cases.
- The header comment (line 6) and the order test's name both carry the word "eighteen".

package.json
- `"test": "node --test \"scripts/**/*.test.mjs\""` — does not reach `lib/`.

Environment, confirmed present: `CAPTURE_SESSION_KEY` is already set in this repository's `.env.local`.
No new environment variable name is invented by this plan.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write lib/limits and lib/session/key with their unit tests</name>
  <files>lib/limits/index.ts, lib/limits/index.test.mjs, lib/session/key.ts, lib/session/key.test.mjs</files>
  <read_first>
    - lib/data/fixtures.ts — the whole file: a single-purpose module of named constant exports, each with its own rationale comment and no grouping object. `lib/limits/index.ts` follows this shape exactly (03-PATTERNS.md §`lib/limits/index.ts`).
    - ../ipv-demo/lib/rbac/manifest.ts lines 42–81 — the lazy-key-resolution and refuse-in-production pattern `lib/session/key.ts` copies in shape
    - .planning/phases/03-server-seam/03-RESEARCH.md — §Code Examples 1 (the key resolver, already written out); §Architecture Patterns "Lazy HMAC key resolution"
    - .planning/phases/03-server-seam/03-CONTEXT.md — the `### Claude's Discretion` bullet "The bounded values first needed here", which names every quantity and its value, and the bullet on the development session key. That first bullet is the source; this plan does not restate the numbers (AD-13).
    - next.config.ts — the build-id gate, which is this repository's own proof that `next build` and `next typegen` load module scope under a production-like `NODE_ENV`
    - .planning/phases/03-server-seam/03-PATTERNS.md — §`lib/limits/index.ts` (config, static exports)
  </read_first>
  <action>
Create `lib/limits/index.ts`. One `export const` per bounded quantity, each with its own short comment
naming the decision that set it and the requirement that reads it. No grouping object, no default read
from an environment variable, no computed value derived from another export. Import nothing.

State the unit convention once, in the file header: byte ceilings are binary (1 KB = 1024 bytes,
1 MB = 1024 x 1024 bytes), durations are in seconds unless the name ends `_MS`, and every comment beside
a byte export shows the arithmetic that produced the literal.

Export these names, each taking the value CONTEXT.md's Claude's-Discretion bullet gives for it:

| Export | The CONTEXT.md quantity it carries |
|---|---|
| `THUMB_MAX_ENCODED_BYTES` | thumbnail server hard cap, in base64-encoded bytes |
| `THUMB_CLIENT_TARGET_ENCODED_BYTES` | thumbnail client target, in base64-encoded bytes |
| `CAPTURES_PER_ACCOUNT_MAX` | per-account capture cap |
| `DECISIONS_PER_ACCOUNT_MAX` | per-account decision cap |
| `CLOCK_SEGMENTS_PER_ACCOUNT_MAX` | per-account clock-segment cap |
| `SEEN_ENTRIES_PER_ACCOUNT_MAX` | per-account `seen` cap |
| `STORE_GLOBAL_OBJECT_MAX` | global object cap |
| `STORE_TTL_SECONDS` | store TTL |
| `SESSION_COOKIE_MAX_AGE_SECONDS` | cookie `Max-Age` |
| `ONLINE_CLOCK_OFFSET_WINDOW_SECONDS` | the online clock-offset window |
| `QUEUED_CLOCK_FUTURE_TOLERANCE_SECONDS` | the queued-item skew tolerance, future bound |
| `QUEUED_CLOCK_FLOOR_SLACK_SECONDS` | the queued-item skew tolerance, the slack below `issued_at` |
| `SYNC_MAX_ITEMS` | the sync route's item ceiling (D-02) |
| `SYNC_MAX_ENCODED_BYTES` | the sync route's encoded-byte ceiling (D-02) |

Add three exports CONTEXT.md leaves to this plan, each with the reason in its comment:

- `CAPTURE_MAX_DECLARED_BYTES = 33554432` (32 MiB) — the ceiling on a capture's declared original
  `bytes`. The thumbnail cap bounds what the server stores; this bounds what a client may claim the
  original was, so an absurd declared size is refused `media_too_large` rather than recorded. P5 may
  re-tune it against a real re-encode.
- `VOICE_MAX_DURATION_MS = 120000` (120 s) — the bounded note duration FR-14 names. It lands here
  because `lib/reconcile/validate.ts` needs a ceiling to refuse against in this phase; P5 owns the
  recorder and may re-tune it.
- `EVICTION_RECORD_PER_ACCOUNT_MAX = 100` — how many evicted proposal ids the store remembers per
  account so `store_evicted` is distinguishable from `unknown_proposal` after a sweep. Bounded so the
  eviction record cannot itself become unbounded growth.

Add a closing comment recording two decisions taken here, each with its reason:
- `FIXTURE_VERSION` is **not** re-exported from `lib/limits` (Phase 2 D-15 left this open). It is not a
  bounded quantity, no client import needs it in this phase, and one definition is worth more than one
  import site.
- The client's outbound batch budget is **not** declared here. D-02 puts it in P6 as a separate named
  export strictly below `SYNC_MAX_ENCODED_BYTES`; declaring it now would be deciding P6's open question.

Create `lib/limits/index.test.mjs` — the project's first `lib/**/*.test.mjs` file. Follow the `node:test`
conventions the existing `scripts/*.test.mjs` files use: `import test from "node:test"`,
`import assert from "node:assert/strict"`, flat `test("plain-English description", () => { ... })` calls,
no `describe` blocks. Import the module as `./index.ts` (Node 24 strips the types; `tsconfig.json`
already carries `allowImportingTsExtensions` for exactly this). Assert:
- every export is a positive, finite, integer `Number`, enumerated by name so a new export cannot be
  added without appearing in this list;
- `THUMB_CLIENT_TARGET_ENCODED_BYTES` is strictly less than `THUMB_MAX_ENCODED_BYTES` — AD-13's
  client-target-below-server-ceiling rule, asserted rather than described;
- `QUEUED_CLOCK_FLOOR_SLACK_SECONDS` is strictly less than `QUEUED_CLOCK_FUTURE_TOLERANCE_SECONDS`;
- `SEEN_ENTRIES_PER_ACCOUNT_MAX` is strictly less than `STORE_GLOBAL_OBJECT_MAX`;
- the module exports nothing whose name is not in the enumerated list.

Create `lib/session/key.ts`. It exists because two modules sign with the same secret —
`lib/session/cookie.ts` (plan 03-03) and `lib/proposals/derive.ts` (plan 03-04) — and one resolver is
the difference between one key and two subtly different ones. Export one function,
`signingKey(): string`, resolving into a module-level `let cached: string | null = null` on first call
and never at module load. Write the reason in the comment, in this repository's own terms: `next build`
and `next typegen` both import every route module under a production-like `NODE_ENV` — this
repository's `next.config.ts` build-id gate already depends on exactly that — so a throw at module scope
would fail the build on a machine with no business holding the production key.

Resolution order, matching the sibling's `resolveSigningKey`: take `process.env.CAPTURE_SESSION_KEY`
when it is set and at least 16 characters; otherwise, when `process.env.NODE_ENV === "production"`,
throw an `Error` whose message names the variable and says the server refuses to serve without it;
otherwise return a committed development fallback string. Mark the fallback with a comment saying it is
a development value, that it is deliberately committed so a local `verify` run needs no secret, and that
production refuses rather than falling back. Export nothing else — no raw key constant, no getter that
returns the fallback unconditionally.

Create `lib/session/key.test.mjs` asserting:
- importing the module with `CAPTURE_SESSION_KEY` unset and `NODE_ENV` unset does not throw — the key is
  not resolved at module load;
- `signingKey()` returns the environment value when one at least 16 characters long is set;
- `signingKey()` returns a non-empty string when neither is set and `NODE_ENV` is not `production`;
- with `NODE_ENV` set to `production` and `CAPTURE_SESSION_KEY` absent, `signingKey()` throws and the
  message names `CAPTURE_SESSION_KEY`. Run each environment case in a child process spawned with
  `node:child_process` so the module-level cache from an earlier assertion cannot mask a later one, and
  comment why.
  </action>
  <acceptance_criteria>
    - `grep -c "^export const" lib/limits/index.ts` returns 17
    - `grep -n "process.env\|^import " lib/limits/index.ts` returns no match
    - `node --test lib/limits/index.test.mjs` exits 0 and `node --test lib/session/key.test.mjs` exits 0
    - `node -e "import('./lib/limits/index.ts').then(m=>{if(!(m.THUMB_CLIENT_TARGET_ENCODED_BYTES < m.THUMB_MAX_ENCODED_BYTES))process.exit(1)})"` exits 0
    - `node -e "import('./lib/session/key.ts')"` exits 0 with `CAPTURE_SESSION_KEY` unset and `NODE_ENV` unset
    - A child process run with `NODE_ENV=production` and no `CAPTURE_SESSION_KEY` throws from `signingKey()` with a message containing `CAPTURE_SESSION_KEY`
    - `grep -c "^export " lib/session/key.ts` returns 1
    - `npx tsc --noEmit` exits 0 and `node scripts/claims-audit.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test lib/limits/index.test.mjs &amp;&amp; node --test lib/session/key.test.mjs &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Every bounded quantity the seam needs has one name and the two relationships AD-13 cares about are asserted rather than described; the signing key has one resolver, resolved on first use, refused in production when absent.</done>
</task>

<task type="auto">
  <name>Task 2: Add not_open, the retained segment fields and the proposal's observation id to types.ts, update the closed-set test, and wire the unit-suite gate step</name>
  <files>lib/data/types.ts, scripts/check-fixture-shape.test.mjs, scripts/verify.mjs, scripts/verify.test.mjs, package.json</files>
  <read_first>
    - lib/data/types.ts lines 363–408 — `ConflictCode`, `CONFLICT_CODES`, `RejectCode`, `REJECT_CODES` and the JSDoc above `ConflictCode` that currently says `already_open` and `not_open` join the set in P4
    - lib/data/types.ts lines 632–641 — `OrderClock` and its `segments` shape
    - lib/data/types.ts lines 574–594 — `Decision`, specifically `device_offset_s: number`, and the JSDoc above it that already states AD-5's identity pair: `capture_client_id` plus `observation_id`
    - lib/data/types.ts lines 561–572 — `Proposal`, which today carries no observation id at all, and lines 496–497 — `OrderAsset.observation_ids`, the server-only plural field this edit must leave alone
    - scripts/check-fixture-shape.test.mjs lines 120–150 — the `CONFLICT_CODES` closed-set test and its exact membership array
    - scripts/check-fixture-hash.mjs lines 35–50 — `FIXTURE_PATHS`, the four hashed files, which do not include `types.ts`
    - scripts/verify.mjs — the `STEPS` array in full, the `fixture-suite` entry and its comment about why the glob is passed unshelled as a single argv entry
    - scripts/verify.test.mjs — `EXPECTED_ORDER`, the order test, the four `resolveSteps` length assertions, and the header comment's step-count phrase
    - package.json — the `test` script's current glob and its quoting
    - .planning/phases/03-server-seam/03-PATTERNS.md — §`lib/data/types.ts` (modified) and §`scripts/verify.mjs` (modified), which spell out both edits and the insertion contract
  </read_first>
  <action>
Three additive edits to `lib/data/types.ts`. No restructuring, no reordering of existing members.

First: add `"not_open"` to the `ConflictCode` union and to `CONFLICT_CODES`, in the same position in
both so the union and the array stay in the same order. Put it immediately after `"order_closed"`,
because the two are the clock's pair and a reader looking for one will look for the other. Amend the
JSDoc above `ConflictCode` to record what actually happened: `not_open` landed in P3 under D-06 with its
sentence and next act in `lib/copy/conflicts.ts`; only `already_open` remains on AD-9's P4 schedule;
`referral_evidence_missing` still has no sentence until P9. Do not add `already_open` — no code for it
is introduced in this phase.

Second: add two optional fields to `OrderClock.segments`, after `source`:
`device_claimed_opened_at?: string` and `device_offset_s?: number`. Comment both: present only when
`source` is `"device_reconciled"`; `device_offset_s` mirrors `Decision.device_offset_s` deliberately so
the same quantity carries the same name on both records; both are retained together because FR-11 says
neither silently replaces the other. Both optional, so every `source: "server"` segment on the online
path needs no change and no existing type-check breaks.

Third: add one required field to `Proposal`, `observation_id: string`, immediately after `order_id`, and
put a JSDoc above the interface naming AD-5. Write the reason plainly: `Proposal.id` is the HMAC over the
account, the capture envelope's client id and the observation id, and AD-5 requires the decision item to
carry `capture_client_id` and `observation_id` so any instance can reconstruct that triple and re-derive
without reading the store. A client can only send back an observation id the server told it, and this
field is where it is told. Comment the two things this deliberately does not do: it does not widen
`OrderAsset.observation_ids`, which stays server-only and stripped from responses, because that field
names observations the artisan has not been shown, whereas an issued proposal already carries its
observation's full wording and naming that observation's id discloses nothing further; and it adds
nothing to `Decision`, whose two identity fields are request fields consumed at the ownership check and
never stored on the record. Grep first and confirm it: `Proposal` is constructed nowhere in the
repository yet, so the field is required rather than optional and no existing type-check breaks.

Record in a comment beside the segment change that `types.ts` is outside `check-fixture-hash.mjs`'s
`FIXTURE_PATHS` (`plant.ts`, `artisans.ts`, `orders.ts`, `observations.ts`), so this edit does not move
the pinned fixture hash — shape is not content (Phase 2 D-14). The same holds for the `Proposal` field.

Update `scripts/check-fixture-shape.test.mjs`'s `CONFLICT_CODES` test in the same commit: nine members in
the new order, and the test's own name changed from "the closed set of eight, ending in
referral_evidence_missing" to a description that matches the new membership. Leave every other closed-set
test in that file untouched — none of them moved.

Then wire the unit suite into the gate. Insert into `scripts/verify.mjs`'s `STEPS`, immediately after
`fixture-suite` and before `check-headers`:
`{ id: "unit-suite", command: process.execPath, args: ["--test", "lib/**/*.test.mjs"] }`.
No `shell: true`, no `vercelExcluded: true`. Write a comment above it saying what it is for: the pure
`lib/` logic this phase adds is provable without a build or a server, so it runs early and cheaply,
before `next-build`.

Update `scripts/verify.test.mjs` in the same commit:
- insert `"unit-suite"` into `EXPECTED_ORDER` immediately after `"fixture-suite"` — nineteen ids;
- change the order test's name and the header comment from "eighteen" to "nineteen";
- change `assert.equal(kept.length, 16)` to `17` for the `VERCEL: "1"` case, and every
  `assert.equal(kept.length, 18)` to `19` for the unset and distractor cases, including any test name
  that carries the word;
- add one new test proving the step is not a vacuous pass: assert that the `unit-suite` step's `args`
  are exactly `["--test", "lib/**/*.test.mjs"]`, and that `globSync("lib/**/*.test.mjs")` from
  `node:fs` currently matches at least one file. Write the limitation in a comment beside it, in this
  repository's own honest-caveat voice: an empty glob exits 0 under `node --test` — verified — so
  without this assertion the step would pass while proving nothing; this test proves the glob is
  non-empty, not that any particular module is covered.
- leave the "two check-wcag steps are the last two", "no step id is duplicated", "check-structure
  appears twice" and "check-register-isolation appears twice" tests alone — all four still hold.

Update `package.json`'s `test` script to `node --test "scripts/**/*.test.mjs" "lib/**/*.test.mjs"` so a
developer running `npm test` runs the same two globs the gate does.

If `STEPS.length` is not 18 when you start (a prior plan slipped), reconcile to the value you find plus
one rather than to the literal 19, and record the discrepancy in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `node -e "import('./lib/data/types.ts').then(m=>{const c=m.CONFLICT_CODES;if(c.length!==9||!c.includes('not_open')||c.includes('already_open'))process.exit(1)})"` exits 0
    - `node -e "import('./lib/data/types.ts').then(m=>{const c=m.CONFLICT_CODES;if(c[c.indexOf('order_closed')+1]!=='not_open')process.exit(1)})"` exits 0
    - `grep -n "device_claimed_opened_at" lib/data/types.ts` returns a match inside the `OrderClock` block
    - `grep -c "device_offset_s" lib/data/types.ts` returns 2 — one on `Decision`, one on `OrderClock.segments`
    - `grep -n "observation_id: string" lib/data/types.ts` returns a match inside the `Proposal` block
    - `grep -n "observation_ids" lib/data/types.ts` still shows only the `OrderAsset` type and its server-only comment — the plural field was not widened
    - `grep -v '^\s*\*' lib/data/types.ts | grep -c "already_open"` returns 0
    - `node --test scripts/check-fixture-shape.test.mjs` exits 0
    - `node scripts/check-fixture-hash.mjs` exits 0 and prints `Problems: 0` — the pin did not move
    - `node -e "import('./scripts/verify.mjs').then(m=>console.log(m.STEPS.length))"` prints `19`
    - `node -e "import('./scripts/verify.mjs').then(m=>{const i=m.STEPS.map(s=>s.id);console.log(i[i.indexOf('fixture-suite')+1])})"` prints `unit-suite`
    - `node --test scripts/verify.test.mjs` exits 0 and `grep -n "eighteen" scripts/verify.test.mjs` returns no match
    - `grep -n "lib/\*\*/\*.test.mjs" package.json` returns a match
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/check-fixture-shape.test.mjs &amp;&amp; node scripts/check-fixture-hash.mjs &amp;&amp; node --test scripts/verify.test.mjs &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>The conflict closed set carries the code this phase must emit, the clock segment can retain both the device's claim and the server's measurement, a proposal names the observation it was drawn from so AD-5's triple can be reconstructed from a decision request, the test that guards the closed sets moved in the same commit, and the gate runs a pure-logic unit suite that cannot pass while matching nothing.</done>
</task>

<task type="auto">
  <name>Task 3: Write lib/copy/conflicts.ts — one sentence and one next act per refusal code</name>
  <files>lib/copy/conflicts.ts, lib/copy/conflicts.test.mjs</files>
  <read_first>
    - docs/planning-artifacts/ux-designs/ux-novatek-capture-demo-2026-09-01/EXPERIENCE.md lines 218–245 — the full conflict-and-reject table: every code, its sentence, its `[seed]`/`[written here]` marking and its actions. Every conflict and reject sentence in this module is copied from that table character for character.
    - docs/planning-artifacts/ux-designs/ux-novatek-capture-demo-2026-09-01/EXPERIENCE.md lines 92–112 — §Voice and Tone, the eight rules `not_open`'s new sentence must obey
    - lib/copy/governed.ts — the whole file: the `GovernedKey`/`GOVERNED` closed-record shape this module mirrors, the `[written here]` comment convention above `PLATFORM_413`, and the header's instruction never to put a governed sentence in a comment
    - lib/data/types.ts lines 363–408 — `ConflictCode` and `RejectCode` as Task 2 left them
    - scripts/check-governed.mjs — what it asserts about `governed.ts`'s export set, so this plan stays clear of it
    - scripts/claims-audit.mjs — the register this module's text must pass; `lib/` is one of its swept roots
  </read_first>
  <action>
Create `lib/copy/conflicts.ts`. It is not a governed module: D-06 says a conflict sentence is not a
governed sentence, `check-governed` does not govern it, and `lib/copy/governed.ts` is **not** edited by
this plan or any plan in this phase.

Export `export interface RefusalCopy { sentence: string; actions: string[] }` and three closed records,
typed so a code cannot ship without a sentence:

- `CONFLICT_COPY: Record<ConflictCode, RefusalCopy>`
- `REJECT_COPY: Record<RejectCode, RefusalCopy>`
- `TRANSPORT_COPY: Record<TransportErrorCode, RefusalCopy>`, with
  `export type TransportErrorCode = "no_session" | "unknown_persona" | "bad_request" | "method_not_allowed" | "batch_too_large"`

`actions` is the next-act list EXPERIENCE.md's table gives for that row, in that order, as plain strings
— voice rule 3 says every refusal carries a next act and there are none without one.

Copy every conflict and reject sentence from EXPERIENCE.md's table character for character. For
`order_not_found` use the `[written here, otherwise]` form — the persona-switch case is the
`account_mismatch` card, not a second `order_not_found` string, and FR-6 requires one and only one
not-found sentence so an unowned id and a fabricated id cannot be told apart by their prose.
`account_mismatch` has no quoted sentence in the table (the row describes the card); write one here to
the same rules, mark it `[written here]`, and name the mechanism without naming a persona, since the
acting account is a runtime value and a sentence that interpolated one would be a second literal.

`referral_evidence_missing` and `unknown_referral` have no sentence until P9. Give each an explicit
placeholder entry whose `sentence` is the empty string and whose comment says P9 supplies it, so the
`Record<>` type stays total and a reader can see the two gaps rather than infer them.

All five `TRANSPORT_COPY` sentences are new. Mark each `[written here]`, write each to the same eight
rules, and use exactly these strings:

- `no_session`: "This request carried no session, so the server has no account to act as and nothing was bound. Choose an artisan at the gate, then try again." — actions `["Choose an artisan"]`
- `unknown_persona`: "That persona is not one of the three this preview carries, so no session was minted." — actions `["Choose an artisan"]`
- `bad_request`: "The server could not read this request as JSON, so nothing was bound. Send it again as JSON." — actions `["Discard", "Send it again"]`
- `method_not_allowed`: "This route does not accept that method, so nothing was bound and nothing was changed. Use the method this route documents." — actions `["Read it instead"]`
- `batch_too_large`: "This batch is larger than the demo server accepts, so nothing in it was bound. The demo server caps how many items and how many bytes it will take at once — send fewer items." — actions `["Discard", "Send fewer items"]`

The `batch_too_large` sentence is the app-level case and is distinct from `lib/copy/governed.ts`'s
`PLATFORM_413`, which is the platform refusing before the preview server ever saw the body. Say so in a
comment beside it: two different refusals, two different sentences, and neither is imported in place of
the other.

Write `not_open`'s sentence here, marked `[written here] — D-06` in a comment above it, in the
conflict-table voice: name the mechanism, say plainly that nothing was bound, and give the next act. Use
exactly:

  `This order is not open, so there was nothing to close and nothing was bound. Open the order, then close it.`

with `actions: ["Discard", "Open the order"]`. Check it against all eight voice rules before committing:
it states the mechanism rather than a mood, it says what was bound rather than apologising, it carries a
next act, it claims neither more nor less than is true, it carries no exclamation and no praise, it uses
no euphemism, it puts no words in the artisan's mouth, and it trips no register entry.

Say in the file header that every refusal sentence the server sends resolves through these three records
and that no module in this phase may write one as a literal at its call site, with the reason: the curl
suite's expected string and the server's actual string are then the same string.

Create `lib/copy/conflicts.test.mjs` asserting:
- `Object.keys(CONFLICT_COPY)` equals `CONFLICT_CODES` as a set, `Object.keys(REJECT_COPY)` equals
  `REJECT_CODES` as a set, and `Object.keys(TRANSPORT_COPY)` is exactly the five `TransportErrorCode`
  members — a new code without a sentence is a red test;
- every `sentence` other than the two P9 placeholders is non-empty, begins with a capital letter and
  ends with a full stop (the envelope rule: the code is for the reviewer, the sentence is for the
  artisan, and both always ship);
- every `actions` array is non-empty for every code with a sentence;
- no sentence contains an exclamation mark (voice rule 6);
- `CONFLICT_COPY.not_open.sentence` is exactly the string above and
  `CONFLICT_COPY.not_open.actions` deep-equals `["Discard", "Open the order"]`;
- no sentence in any of the three records is a substring of any sentence assembled from
  `lib/copy/governed.ts`'s `GOVERNED` or `PLATFORM_413` — the two vocabularies stay separate and
  `check-governed` stays green.
  </action>
  <acceptance_criteria>
    - `node --test lib/copy/conflicts.test.mjs` exits 0
    - `node -e "import('./lib/copy/conflicts.ts').then(m=>{if(m.CONFLICT_COPY.not_open.sentence!=='This order is not open, so there was nothing to close and nothing was bound. Open the order, then close it.')process.exit(1)})"` exits 0
    - `node -e "Promise.all([import('./lib/copy/conflicts.ts'),import('./lib/data/types.ts')]).then(([c,t])=>{if(Object.keys(c.CONFLICT_COPY).sort().join()!==[...t.CONFLICT_CODES].sort().join())process.exit(1)})"` exits 0
    - `node -e "import('./lib/copy/conflicts.ts').then(m=>{if(Object.keys(m.TRANSPORT_COPY).length!==5)process.exit(1)})"` exits 0
    - `grep -c "written here" lib/copy/conflicts.ts` returns at least 6
    - `node scripts/check-governed.mjs` exits 0 — `governed.ts` is unchanged and no governed sentence leaked into this module
    - `node scripts/claims-audit.mjs` exits 0
    - `git diff --name-only HEAD -- lib/copy/governed.ts` returns nothing
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test lib/copy/conflicts.test.mjs &amp;&amp; node scripts/check-governed.mjs &amp;&amp; node scripts/claims-audit.mjs &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>Every code the server can emit has exactly one sentence and one next act, defined once, and the code this phase added has a sentence written to the same standard as the ones it sits beside.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a bounded quantity → the two sides that read it | A cap restated on one side and not the other is a cap that does not hold; AD-13 exists because the client and the server must refuse at the same number |
| the signing secret → module load | A key resolved at module load throws during `next build` on a machine that has no business holding it; a key with no production refusal ships a public fallback |
| a refusal code → the artisan reading it | A code the server can emit with no sentence behind it reaches the artisan as an unexplained failure, and a sentence written twice drifts between the suite's expectation and the server's answer |
| the gate → the unit suite | A test-runner glob that matches nothing exits 0, so a step added without a guard would report a passing suite that ran no tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Denial of Service | unbounded store growth and unbounded batch size | mitigate | Task 1 exports every per-account cap, the global object cap, the TTL and the sync item and byte ceilings as named constants; the store and the sync route in later plans read them rather than restating a number, and the unit test asserts every one is a positive finite integer. |
| T-3-02 | Information Disclosure | a not-found sentence that differs between an unowned id and a fabricated one | mitigate | Task 3 defines exactly one `order_not_found` sentence, in one record, with the reason stated in the header; FR-6's byte-identity comparator in plan 03-13 proves the bodies match. |
| T-3-03 | Tampering | a client thumbnail target at or above the server's hard cap, so the server never actually refuses | mitigate | Task 1's unit test asserts `THUMB_CLIENT_TARGET_ENCODED_BYTES < THUMB_MAX_ENCODED_BYTES` and `QUEUED_CLOCK_FLOOR_SLACK_SECONDS < QUEUED_CLOCK_FUTURE_TOLERANCE_SECONDS` as executable relationships, not prose. |
| T-3-04 | Repudiation | a gate step that passes while running nothing | mitigate | Task 2 adds a `verify.test.mjs` assertion that the `unit-suite` step's args are exact and that the glob currently matches at least one file, with the limitation stated in a comment beside it. |
| T-3-05 | Spoofing | a new conflict code shipping with no sentence, so a refusal reaches the artisan as a bare code | mitigate | Task 3 types all three copy records as `Record<Code, RefusalCopy>` so the compiler refuses an incomplete set, and the unit test asserts key-set equality against the closed-set arrays. |
| T-3-16 | Information Disclosure | the committed development signing key being used in production | mitigate | Task 1's `signingKey()` throws under `NODE_ENV === "production"` when `CAPTURE_SESSION_KEY` is absent or shorter than 16 characters; an acceptance criterion runs exactly that case in a child process and requires a throw naming the variable. |
</threat_model>

<verification>
- `node --test lib/limits/index.test.mjs`, `node --test lib/session/key.test.mjs`, `node --test lib/copy/conflicts.test.mjs`, `node --test scripts/check-fixture-shape.test.mjs` and `node --test scripts/verify.test.mjs` all exit 0.
- `node scripts/check-fixture-hash.mjs`, `node scripts/check-governed.mjs` and `node scripts/claims-audit.mjs` all exit 0.
- `npm run verify` exits 0 end to end with nineteen steps.
- Each task is one commit; after `gsd-sdk query commit`, confirm with `git log -1 --format=%B` that the `Co-Authored-By` trailer survived, and amend if it did not. Edit files under `.planning/` with the Edit tool or a Node script, never `perl -pi`.
</verification>

<success_criteria>
- Media and thumbnail caps, store caps and TTL are exported from `lib/limits` and read by name nowhere else (roadmap success criterion 5, AD-13).
- `not_open` is in the conflict closed set with a sentence and a next act written to EXPERIENCE.md's rules (D-06, REQ-FR-8).
- The clock segment can retain both the device's claim and the server's measured offset (REQ-FR-11).
- The gate runs a pure-logic unit suite that cannot pass vacuously.
</success_criteria>

<output>
Create `.planning/phases/03-server-seam/03-01-SUMMARY.md` when done
</output>
