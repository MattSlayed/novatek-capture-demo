---
phase: 03-server-seam
plan: 05
type: execute
wave: 3
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - lib/reconcile/validate.ts
  - lib/reconcile/validate.test.mjs
  - lib/reconcile/apply.ts
  - lib/reconcile/apply.test.mjs
autonomous: true
requirements: [REQ-FR-7, REQ-FR-8, REQ-FR-9, REQ-FR-11, REQ-FR-15, REQ-FR-16, REQ-FR-19, REQ-FR-24, REQ-FR-61]

must_haves:
  truths:
    - "D-10: shape validation is strict per D-CONV — 64 lowercase hex `sha256`, UUID-shaped `client_id`, ISO-8601 UTC `Z` timestamps, positive-integer `bytes`, a small MIME allowlist, `duration_ms` only on a voice capture, `schema_version` in the emitted set — and a refusal is `422 bad_shape` whose sentence names the offending field"
    - "D-10: every write route is enumerated against its accepted fields, and a field outside the enumeration — an actor field above all — is dropped at parse and appears nowhere (AD-3, AD-20, REQ-FR-61)"
    - "AD-5: the decision item's `capture_client_id` and `observation_id` are enumerated by name as proposal identity — excluded from FR-61's client-content ban for that reason — and are used only to re-derive the proposal id at the ownership position, never written onto a record"
    - "D-05: the clock gates the record — a verify, an evidence capture or a decision against an order whose clock has no running segment is refused `409 order_closed` with EXPERIENCE.md's sentence and Discard as the only act"
    - "D-06: closing a closed or never-opened order returns `409 not_open`; a second open of a running order creates no second segment and is `duplicate`, whichever `client_id` it arrives under, and no code for `already_open` is introduced"
    - "D-04: FR-11's clamp is proved by a direct unit test of `applyItem` with a queued-flagged `order_open`, and again end to end through `/api/sync` and `/api/hours` in the route suite"
    - "D-07: the clamp floor is the later of the session's `issued_at` and the account's last server contact; when the contact value is absent the floor is `issued_at` alone, silently"
    - "`applyItem` is the sole writer for the online routes and `/api/sync` alike, with the check order session → ownership → idempotency → shape → state, and a refusal is written as a retained attempt by the same function (AD-1, REQ-FR-24)"
    - "Every write route's resulting record state is drawn from {open, accepted, rejected, superseded} and no operation creates a finding (REQ-FR-24, AD-19)"
  artifacts:
    - path: "lib/reconcile/validate.ts"
      provides: "The strict shape guards, the accepted-field enumerations and the idempotency canonicaliser"
      min_lines: 200
    - path: "lib/reconcile/apply.ts"
      provides: "applyItem and noteContact — the only writers in the repository"
      min_lines: 280
  key_links:
    - from: "lib/reconcile/apply.ts"
      to: "lib/store/memory.ts"
      via: "the store's mutating exports, imported by this module and by no other"
      pattern: "writeAttempt"
    - from: "lib/reconcile/apply.ts"
      to: "lib/access/scope.ts"
      via: "orderOwned, called before any state is read"
      pattern: "orderOwned"
    - from: "lib/reconcile/validate.ts"
      to: "lib/limits/index.ts"
      via: "the media and duration ceilings it refuses against"
      pattern: "THUMB_MAX_ENCODED_BYTES"
---

<objective>
Build the one writer and the one validator behind it: the fixed check order, the strict shapes, the
accepted-field enumerations, the idempotency canonicaliser, and every state transition the clock, the
capture and the decision can make.

Purpose: AD-1 is the load-bearing claim of the whole phase — every write, online or reconciled, passes
through one function in one order, and a refusal is retained by that same function rather than escaping
it. AD-20 and FR-61 are enforced at parse, which is the only place they can be enforced structurally
rather than by review.

Output: two modules and two unit tests. No route exists yet; everything here is provable with
`node --test lib/**/*.test.mjs` and nothing else.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-server-seam/03-CONTEXT.md
@.planning/phases/03-server-seam/03-RESEARCH.md
@.planning/phases/03-server-seam/03-PATTERNS.md
@.planning/phases/03-server-seam/03-VALIDATION.md
@.planning/phases/03-server-seam/03-02-SUMMARY.md
@.planning/phases/03-server-seam/03-03-SUMMARY.md
@.planning/phases/03-server-seam/03-04-SUMMARY.md
</context>

<interfaces>
<!-- What plans 03-01 to 03-04 created. Read the modules themselves before writing against them. -->

lib/limits/index.ts (03-01)
- `THUMB_MAX_ENCODED_BYTES`, `CAPTURE_MAX_DECLARED_BYTES`, `VOICE_MAX_DURATION_MS`,
  `ONLINE_CLOCK_OFFSET_WINDOW_SECONDS`, `QUEUED_CLOCK_FUTURE_TOLERANCE_SECONDS`,
  `QUEUED_CLOCK_FLOOR_SLACK_SECONDS`, `SYNC_MAX_ITEMS`, `SYNC_MAX_ENCODED_BYTES`

lib/copy/conflicts.ts (03-01)
- `CONFLICT_COPY`, `REJECT_COPY`, `TRANSPORT_COPY`, `RefusalCopy`, `TransportErrorCode`

lib/store/memory.ts (03-02)
- mutating: `writeCapture`, `writeProposals`, `writeDecision`, `writeClockSegment`,
  `closeClockSegment`, `writeSeen`, `writeAttempt`, `stampLastContact`, `recordEviction`, `sweep`
- read-only: `readClock`, `readClocksForAccount`, `readCaptures`, `readProposal`,
  `readProposalsForAsset`, `readDecisions`, `readSeen`, `readAttempts`, `readLastContact`,
  `wasEvicted`, `BOOT_ID`, `storeStats`, `uptimeSeconds`
- `AttemptEntry { at; account_id; event; outcome; code; client_id; order_id }`

lib/http/contract.ts (03-02)
- `WireErrorCode`, `STATUS_BY_CODE`, `detailFor`, `errorBody`

lib/session/cookie.ts (03-03)
- `SessionResult`, `readSession`, `verifySessionValue`, `mintSession`

lib/attribution/index.ts (03-03)
- `ActingAccount { account_id; artisan; session }`, `deriveAccount(result): ActingAccount | null`

lib/access/scope.ts (03-03)
- `ordersFor`, `orderOwned`, `assetInOrder`, `assetsForOrder`

lib/verify/authored.ts (03-04)
- `AuthoredMatch`, `authoredMatch(assetId, fixtureSet)`

lib/proposals/derive.ts (03-04)
- `AuthoredProposal`, `authoredProposals(assetId, fixtureSet)`,
  `deriveProposalId(accountId, clientId, observationId)` — one bare base64url HMAC digest —
  `proposalIdMatches(candidate, accountId, clientId, observationId)`, `UnknownCitedRecordError`.
  There is no `parseProposalId`: the id encodes nothing, so the three derivation inputs come from the
  session's account and the decision item's two identity fields (AD-5)

lib/data/types.ts (03-01)
- `SyncItem<K>`, `SyncItemResult`, `SyncItemKind`, `SYNC_ITEM_SCHEMA_VERSIONS`, `OrderClock`
  (segments now carry `device_claimed_opened_at?` and `device_offset_s?`), `Capture`, `Decision`,
  `Proposal` (now carries `observation_id`), `ProposalState`, `ConflictCode` (nine members),
  `RejectCode`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write lib/reconcile/validate.ts — strict shapes, accepted fields and the canonicaliser</name>
  <files>lib/reconcile/validate.ts, lib/reconcile/validate.test.mjs</files>
  <read_first>
    - .planning/phases/03-server-seam/03-RESEARCH.md — §Code Examples 7 (the canonicaliser, with its `undefined`-versus-missing, number-formatting and Unicode-normalisation notes) and §Code Examples 8 (the UUID and hex guards, written out); §Don't Hand-Roll's canonicalisation row
    - .planning/phases/03-server-seam/03-CONTEXT.md — D-10 in full
    - docs/CAPTURE-PLAN-SEED.md lines 238–254 — the route table's Body column (the accepted fields per route) and the idempotency paragraph (`seen` keyed per account; same key + same hash → duplicate; same key + different hash → conflict)
    - scripts/check-governed.mjs lines 140–170 — the "one small anchored regex per shape, named predicate function" style this project already uses
    - lib/limits/index.ts — every ceiling this module refuses against
    - lib/copy/conflicts.ts — `REJECT_COPY.bad_shape` and `REJECT_COPY.media_too_large`, the two sentences this module composes details from
    - lib/data/types.ts lines 417–443 and 526–672 — `SyncItemKind`, `SYNC_ITEM_SCHEMA_VERSIONS`, `Capture`, `Decision`, `SyncItem`, `SyncItemResult`
    - .planning/REQUIREMENTS.md — REQ-FR-15, REQ-FR-16, REQ-FR-61 and the FR-10 clause "none accepts an artisan-supplied duration or hour value"
  </read_first>
  <action>
Create `lib/reconcile/validate.ts`. It reads nothing from the store — AD-5 requires that, and it is what
makes the proposal-id check a pure re-derivation. Say so in the header.

**Shape predicates**, one small anchored regex each with a named predicate beside it, following
`check-governed.mjs`'s style:
- `isUuidShaped` — `/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i`. Comment
  that it is deliberately strict to `crypto.randomUUID()`'s v4/variant-1 shape rather than a generic
  any-version matcher, because D-10 says strict.
- `isSha256Hex` — `/^[0-9a-f]{64}$/`, lowercase only.
- `isIsoUtcZ` — anchored ISO-8601 with a literal `Z`, and a `Date.parse` round-trip so `2026-02-31` is
  refused rather than accepted by the regex alone.
- `isPositiveInt` — `Number.isSafeInteger` and greater than zero.

**MIME allowlists**, one export each:
`PHOTO_MIMES = ["image/jpeg"]` and `VOICE_MIMES = ["audio/webm", "audio/mp4", "audio/ogg"]`. Compare the
value's type/subtype before any `;` parameter, so `audio/webm;codecs=opus` passes. Comment that P5 owns
the recorder and may extend `VOICE_MIMES` when the real capability probe lands, and that extending it is
a deliberate edit here rather than a loosened check.

**Accepted-field enumerations** (AD-20, FR-61, FR-10). Export two records and treat them as the single
in-code statement of the enumeration:

  `export const ACCEPTED_BODY_FIELDS: Record<string, readonly string[]>` keyed by route id —
  `session: ["persona_id"]`, `orders_open: ["client_id"]`, `orders_close: ["client_id"]`,
  `verify: ["client_id","order_id","asset_id","kind","purpose","captured_at","sha256","bytes","mime","duration_ms","thumb"]`,
  `captures: <the same list as verify>`,
  `decisions: ["client_id","proposal_id","capture_client_id","observation_id","outcome","decided_at","decided_where_claimed","note"]`,
  `sync: ["items"]`

  `export const ACCEPTED_PAYLOAD_FIELDS: Record<SyncItemKind, readonly string[]>` —
  `order_open: ["device_claimed_opened_at"]`, `order_close: ["device_claimed_closed_at"]`,
  `capture: ["asset_id","kind","purpose","captured_at","sha256","bytes","mime","duration_ms","thumb"]`,
  `decision: ["proposal_id","capture_client_id","observation_id","outcome","decided_at","decided_where_claimed","note"]`,
  `referral: []`

Export `pick(body, fields)` which returns a new object carrying only the enumerated keys and **drops
everything else without reporting it** — AD-20's "a field outside the enumeration is dropped at parse
and appears nowhere". Comment the two consequences: an actor field (`captured_by`, `decided_by`,
`raised_by`, `account_id`) is never in any enumeration so it cannot survive the parse, and an
observation, grade or provenance field is never in any enumeration either, which is FR-61 enforced
structurally. Note the one deliberate exception with its reason: `SyncItem.claimed_account_id` is read
off the envelope because `types.ts` says it is compared and never trusted; it is never copied onto a
record and never becomes an actor field.

Note AD-5's two identity fields in a comment beside the `decisions` and `decision` entries, because they
are the one place where a field naming something the client sent is deliberately accepted:
`capture_client_id` is the capture envelope's own client id and `observation_id` names the authored
observation, and the two exist only so the server can reconstruct the triple `Proposal.id` was derived
from. AD-5 says it in those words — "These two fields are proposal identity, not observation content, so
FR-61's accepted-field enumeration excludes them by name" — so write that sentence's substance into the
comment. Note the consequence for every check that enforces the ban: FR-61 forbids `observation`,
`grade` and `provenance` by exact name, and `observation_id` is not `observation`, so field names are
compared whole and never as substrings. Plan 03-12's two rules say the same thing and ship a fixture
proving it.

Note the FR-10 distinction explicitly in a comment: `duration_ms` is a media duration on a voice capture
and is accepted; no enumeration anywhere carries `elapsed_s`, `hours`, `duration_s` or any other
accrued-time value, because hours are computed by the server from segments it stamped.

**Refusals.** Export
`export type ShapeRefusal = { code: "bad_shape" | "media_too_large" | "unknown_kind"; field: string; detail: string }`
and `export function badShapeDetail(field: string): string` returning
`` `${REJECT_COPY.bad_shape.sentence} The field was ${field}.` `` — the canonical sentence, imported not
restated, with one clause naming the offending field as D-10 requires. Do the same for
`mediaTooLargeDetail(field)` over `REJECT_COPY.media_too_large.sentence`.

**Validators**, one per kind, each returning `ShapeRefusal | null`:
- `validateCapturePayload` — `asset_id` a non-empty string; `kind` in `["photo","voice"]`; `purpose` in
  `["verify","evidence"]`; `captured_at` ISO UTC Z; `sha256` 64 lowercase hex; `bytes` a positive
  integer at or below `CAPTURE_MAX_DECLARED_BYTES` (`media_too_large` above it, naming `bytes`); `mime`
  in the allowlist for the declared `kind`; `duration_ms` **present only when** `kind === "voice"`,
  positive-integer, at or below `VOICE_MAX_DURATION_MS` (`media_too_large` above it); `thumb` optional,
  a string whose length is at or below `THUMB_MAX_ENCODED_BYTES` (`media_too_large` above it, naming
  `thumb`). A `duration_ms` on a photo is `bad_shape` naming `duration_ms`.
- `validateDecisionPayload` — `proposal_id` a non-empty string; `capture_client_id` UUID-shaped (it is
  the capture envelope's own client id, minted by `crypto.randomUUID()` at enqueue under AD-16);
  `observation_id` a non-empty string; `outcome` in `["accept","reject"]`; `decided_at` ISO UTC Z;
  `decided_where_claimed` in `["online","on_device"]`; `note` optional string. Write the honest note
  beside the two identity rules: AD-1's order puts the proposal-id re-derivation at the ownership
  position, which runs before shape, so in practice a missing or mis-shaped identity field is refused
  `unknown_proposal` by the digest comparison before this validator sees it. The two rules exist so the
  enumeration and the validator state the same field set in one place, and so a later reordering cannot
  silently accept an unshaped value — not because a `bad_shape` refusal for them is reachable today. Say
  that plainly rather than implying the check is load-bearing.
- `validateClockPayload` — the one optional ISO UTC Z timestamp for its kind, absent or valid.
- `validateEnvelopeItem(item)` — `client_id` UUID-shaped; `kind` in `SYNC_ITEM_KINDS`;
  `schema_version` a member of `SYNC_ITEM_SCHEMA_VERSIONS[kind]`; `order_id` a non-empty string except
  for a `decision` item, where it may be empty because a decision's authorisation comes from the
  proposal id (comment that); `created_at` ISO UTC Z. A `kind` outside the closed set is
  `unknown_kind`, not `bad_shape`.

**Canonicaliser and idempotency.** Implement `canonicalize(value)` as RESEARCH's Code Example 7 shows:
recursive, sorted keys, `JSON.stringify` for primitives. Carry its three notes into comments in your own
words — number formatting, `undefined` versus a missing key, and Unicode normalisation — each said
plainly as a limitation rather than glossed. Then
`export function idempotencyHash(kind: SyncItemKind, payload: unknown): string` projecting the payload
onto `ACCEPTED_PAYLOAD_FIELDS[kind]` **before** hashing, so the hash is over the enumerated subset and
never over the raw attacker-shaped body, and `export function seenKey(accountId, clientId)` returning
`` `${accountId}:${clientId}` `` — keyed per account, so one persona's retry can never short-circuit into
another's stored result.

Create `lib/reconcile/validate.test.mjs` asserting at least:
- each predicate accepts one valid example and refuses at least three shaped-but-wrong ones
  (uppercase hex, 63 hex characters, a v1 UUID, a timestamp with an offset instead of `Z`,
  `2026-02-31`, `0`, `-1`, `1.5`);
- `pick` drops `captured_by`, `decided_by`, `raised_by` and `account_id` from every route's body, for
  every key in `ACCEPTED_BODY_FIELDS` — iterate the record rather than writing one test per route, and
  name the test so FR-23's negative set is visible in the output;
- `pick` drops `observation`, `grade` and `provenance` from every enumeration — FR-61;
- `ACCEPTED_BODY_FIELDS.decisions` and `ACCEPTED_PAYLOAD_FIELDS.decision` both contain
  `capture_client_id` and `observation_id`, and `pick` keeps both on a decision body while dropping
  `observation` from that same body — AD-5's identity pair survives the parse and FR-61's ban is on
  whole names, asserted in one test so the distinction is visible in the output;
- no array in either enumeration contains `elapsed_s`, `hours` or `duration_s` — FR-10;
- a photo payload carrying `duration_ms` is `bad_shape` and the detail names `duration_ms`;
- a voice payload with no `duration_ms` is `bad_shape` naming `duration_ms`;
- a thumbnail one character above `THUMB_MAX_ENCODED_BYTES` is `media_too_large` naming `thumb`, and one
  character below it passes;
- `idempotencyHash` is identical for two payloads whose keys were inserted in different orders, and
  identical when an unenumerated field is added to one of them, and different when an enumerated value
  changes;
- `seenKey` differs for the same `client_id` under two accounts.
  </action>
  <acceptance_criteria>
    - `node --test lib/reconcile/validate.test.mjs` exits 0
    - `node -e "import('./lib/reconcile/validate.ts').then(m=>{const all=[...Object.values(m.ACCEPTED_BODY_FIELDS),...Object.values(m.ACCEPTED_PAYLOAD_FIELDS)].flat();for(const f of ['captured_by','decided_by','raised_by','account_id','observation','grade','provenance','elapsed_s','hours','duration_s'])if(all.includes(f))process.exit(1)})"` exits 0
    - `node -e "import('./lib/reconcile/validate.ts').then(m=>{const a=m.idempotencyHash('capture',{sha256:'a',bytes:1});const b=m.idempotencyHash('capture',{bytes:1,sha256:'a',captured_by:'x'});if(a!==b)process.exit(1)})"` exits 0
    - `node -e "import('./lib/reconcile/validate.ts').then(m=>{for(const f of ['capture_client_id','observation_id']){if(!m.ACCEPTED_BODY_FIELDS.decisions.includes(f))process.exit(1);if(!m.ACCEPTED_PAYLOAD_FIELDS.decision.includes(f))process.exit(1)}})"` exits 0 — AD-5's identity pair is enumerated on both surfaces
    - `grep -n "from \"../store" lib/reconcile/validate.ts` returns no match — validation reads nothing from the store (AD-5)
    - `grep -c "REJECT_COPY" lib/reconcile/validate.ts` returns at least 2 — the two sentences are imported, never restated
    - `npx tsc --noEmit` exits 0 and `node scripts/claims-audit.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test lib/reconcile/validate.test.mjs &amp;&amp; npx tsc --noEmit &amp;&amp; node scripts/claims-audit.mjs</automated>
  </verify>
  <done>Every shape the wire can carry is checked strictly against D-CONV, every write route has its accepted-field list in code, a field outside it is dropped at parse rather than reported, and the idempotency hash is over the enumerated subset so key order and stray fields cannot change it.</done>
</task>

<task type="auto">
  <name>Task 2: Write lib/reconcile/apply.ts — the fixed check order and the retained attempt</name>
  <files>lib/reconcile/apply.ts, lib/reconcile/apply.test.mjs</files>
  <read_first>
    - ../ipv-demo/lib/decision/store.ts lines 125–200 — the discriminated-union result type at 125–134, the fixed check order at 152–186 (each check returns immediately, nothing falls through), the ownership-before-state comment at 160–171, and `sameToken` at 136–142
    - .planning/phases/03-server-seam/03-RESEARCH.md — §Architecture Patterns Pattern 1 (the `applyItem` skeleton, written out) and Pattern 2
    - .planning/phases/03-server-seam/03-PATTERNS.md — §`lib/reconcile/apply.ts` (service, single writer)
    - .planning/phases/03-server-seam/03-CONTEXT.md — D-01, D-05, D-07, the `<domain>` bullet on `lib/reconcile`, and the Claude's-Discretion bullet on where a session-level refusal is recorded
    - lib/store/memory.ts — every mutating and read-only export, and the `AttemptEntry` shape
    - lib/access/scope.ts, lib/attribution/index.ts, lib/reconcile/validate.ts — everything this module composes
    - lib/data/types.ts lines 643–672 — `SyncItem` and `SyncItemResult`, including the note that `claimed_account_id` is compared and never trusted
    - docs/CAPTURE-PLAN-SEED.md line 254 — the check order sentence and the idempotency rule, verbatim
  </read_first>
  <action>
Create `lib/reconcile/apply.ts`. Header comment: this is the only module in the repository that imports
`lib/store/memory.ts`'s mutating exports, and plan 03-12's `scripts/check-single-writer.mjs` asserts it
over the import graph. AD-1's order is fixed and is stated in the header so a later reader cannot
reorder it by accident: **session → ownership → idempotency → shape → state**.

Export two functions and one type:

  `export type ApplyOutcome = { result: SyncItemResult; code: WireErrorCode | null }`
  `export async function applyItem(sessionResult: SessionResult, item: SyncItem<unknown>): Promise<ApplyOutcome>`
  `export function noteContact(account: ActingAccount): void`

`result` is always a well-formed `SyncItemResult`, so `/api/sync` can put it straight into its
`results[]`. `code` is the wire error code an online route maps to a status through
`lib/http/respond.ts`'s `fail`, and is `null` on `recorded` and `duplicate`. Comment why the two are
separate: the sync route returns HTTP 200 whenever the envelope parsed (D-01), so the transport status
belongs to the caller, not to the writer.

`noteContact` exists here and not in `lib/store` or `lib/attribution` because it is a write, and AD-1
says every write goes through this module. It calls `stampLastContact(account.account_id)` and nothing
else. Every route calls it immediately after attribution succeeds, so D-07's `last_contact` is stamped
by every authenticated request from the server's own clock.

Implement `applyItem` as a straight-line sequence of guarded returns — check, return, never fall
through, never accumulate refusals and pick one at the end. Write a one-line comment above each step
naming the step and the decision that put it there:

1. **Session.** `deriveAccount(sessionResult)`; when it is `null`, write an unattributed attempt
   (`writeAttempt` with `account_id: null`, the item's `client_id` and `order_id`, outcome `rejected`)
   and return `{ result: { client_id, status: "rejected", detail: TRANSPORT_COPY.no_session.sentence },
   code: "no_session" }`. The refusal is written here rather than at the route, because AD-1 says
   refusals do not escape the writer. Leave `result.code` unset: `no_session` is not a member of either
   fixture closed set, and the closed sets are not widened to make a transport refusal fit.
2. **Ownership.** For every kind except `decision`, `orderOwned(account, item.order_id)`; when it is
   `null`, write the attempt and return `order_not_found` with `CONFLICT_COPY.order_not_found.sentence`.
   For `kind === "decision"`, ownership is the proposal's and is decided at this same position in Task 3
   by `proposalIdMatches(payload.proposal_id, account.account_id, payload.capture_client_id,
   payload.observation_id)` — all three derivation inputs supplied by the caller, none parsed out of the
   submitted id (AD-5). Write that in a comment and route the branch accordingly, so the order of checks
   is identical for both and an unowned proposal fails before any state is read.
   Also compare `item.claimed_account_id` against `account.account_id` when the field is non-empty and
   return `account_mismatch` when they differ; comment that the field is compared and never trusted,
   and that a mismatch is a conflict rather than a silent reattribution.
3. **Idempotency.** `seenKey(account.account_id, item.client_id)`; `readSeen`. Same key and the same
   `idempotencyHash` → return the stored result with `status: "duplicate"` and `code: null`. Same key,
   different hash → `conflict` with code `already_recorded_differently`. Comment the per-account keying
   and why it matters: one persona's retry can never short-circuit into another's stored result, and two
   accounts submitting the same client id produce no cross-account result (FR-34's server half).
4. **Shape.** Call the validator for the item's kind; a `ShapeRefusal` becomes a `rejected` result with
   that code and that detail. A kind outside the closed set, and `referral` specifically, returns
   `unknown_kind` with `REJECT_COPY.unknown_kind.sentence` — comment that P9 supplies the `referral`
   behaviour and that until then the refusal is honest rather than a silent drop.
5. **State.** Delegate to `applyByKind` (Task 3).

On every path, before returning, call `writeAttempt` with a server-stamped ISO-8601 UTC `at`, the acting
account (or `null`), the kind as the `event`, the outcome, the code and the two ids — success and
failure alike, because FR-60 says every act produces an entry, not every failure. Write `writeSeen` for
every terminal result other than `duplicate`, so a retry of a refusal is also idempotent.

Guarantee the closed result-state set in one place: add
`const TERMINAL_STATES: ProposalState[] = ["open", "accepted", "rejected", "superseded"]` and a small
internal assertion that every record state this module writes is a member, with a comment naming FR-24
and AD-19: no operation here creates a finding, and there is no fifth state to create one with.

Create `lib/reconcile/apply.test.mjs` covering Task 2's half only (Task 3 extends the same file). Pin
`process.env.CAPTURE_SESSION_KEY`. Build a valid `SessionResult` by calling `mintSession` and
`verifySessionValue` rather than hand-building one, so the test exercises the real path. Assert:
- an `{ ok: false }` session yields `code: "no_session"`, a `rejected` result, and one entry in the
  unattributed attempt ring — read it back with `readAttempts`;
- an item naming an order the account does not own and an item naming a fabricated order id yield
  **deep-equal** results and the same `code` — the unit-level half of FR-6 at the writer;
- an item whose `claimed_account_id` names another account yields `account_mismatch`;
- the same `client_id` with the same payload twice yields `recorded` then `duplicate`, and the second
  call writes no second record;
- the same `client_id` with a changed enumerated field yields `already_recorded_differently`;
- the same `client_id` under two different accounts yields two independent `recorded` results;
- a `referral` item yields `unknown_kind`;
- an attempt entry is written on the success path as well as on every refusal path;
- `noteContact` moves `readLastContact` from `null` to a number and takes exactly one parameter.
  </action>
  <acceptance_criteria>
    - `node --test lib/reconcile/apply.test.mjs` exits 0
    - `grep -n "session\|ownership\|idempotency\|shape\|state" lib/reconcile/apply.ts | head -20` shows the five step comments in that order
    - `grep -rn "writeCapture\|writeDecision\|writeClockSegment\|closeClockSegment\|writeSeen\|writeAttempt\|stampLastContact\|recordEviction" lib app --include=*.ts | grep -v "^lib/store/memory.ts" | grep -v "^lib/reconcile/apply.ts"` returns no match
    - `node -e "import('./lib/reconcile/apply.ts').then(m=>{if(m.noteContact.length!==1)process.exit(1)})"` exits 0
    - The test file contains a test asserting the unowned-order and fabricated-order results are deep-equal
    - `npx tsc --noEmit` exits 0 and `node scripts/claims-audit.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test lib/reconcile/apply.test.mjs &amp;&amp; npx tsc --noEmit &amp;&amp; node scripts/claims-audit.mjs</automated>
  </verify>
  <done>One function writes, in one order, and it writes the refusals too; an unowned id and a fabricated id come out of it identical; and a retry under one account cannot reach another account's stored result.</done>
</task>

<task type="auto">
  <name>Task 3: Write applyByKind — the clock, the capture and the decision state transitions</name>
  <files>lib/reconcile/apply.ts, lib/reconcile/apply.test.mjs</files>
  <read_first>
    - lib/reconcile/apply.ts — everything Task 2 wrote, especially the step comments and `ApplyOutcome`
    - .planning/phases/03-server-seam/03-CONTEXT.md — D-04, D-05, D-06, D-07, D-08 in full; the Claude's-Discretion bullet on `store_evicted` detection
    - .planning/phases/03-server-seam/03-PATTERNS.md — §`app/api/orders/[id]/open/route.ts` and `close`, which spells out the D-06 delta from the sibling's binary ok/fail shape
    - docs/planning-artifacts/ux-designs/ux-novatek-capture-demo-2026-09-01/EXPERIENCE.md lines 218–245 — the sentences for `order_closed`, `not_open`, `asset_not_in_order`, `proposal_superseded`, `store_evicted`, `clock_skew`; they are imported from `lib/copy/conflicts.ts`, never quoted
    - lib/data/types.ts lines 632–641 — `OrderClock` with the two new optional segment fields, and lines 574–594 — `Decision` with `device_offset_s`, `arrived_via` and `decided_where_claimed`
    - lib/limits/index.ts — `ONLINE_CLOCK_OFFSET_WINDOW_SECONDS`, `QUEUED_CLOCK_FUTURE_TOLERANCE_SECONDS`, `QUEUED_CLOCK_FLOOR_SLACK_SECONDS`
    - lib/proposals/derive.ts and lib/verify/authored.ts — the functions the capture and decision kinds call
    - lib/store/memory.ts — `wasEvicted`, `readProposal`, `readClock`
  </read_first>
  <action>
Extend `lib/reconcile/apply.ts` with `applyByKind`, reached only after the four earlier checks passed.
`arrived_via` is derived here from the item, never read from a body: an item whose `state` field marks it
as having been queued is `"queued"`, everything else is `"immediate"`. Comment that.

**`order_open`.**
- Read the clock for `(account, order_id)`. If a segment is open (`closed_at === null`), return
  `status: "duplicate"` with `code: null` and the current clock in `result.server.clock`, writing **no**
  second segment. Comment D-06 and FR-7: the online route renders that as `200 { clock }` and the sync
  route counts it under `X-CAP-Sync-Duplicate`; one behaviour, two renderings, whichever `client_id` it
  arrived under. Do not introduce `already_open` and do not reference it.
- Otherwise append a segment. On the immediate path: `opened_at` is the server's own `Date.now()` as
  ISO-8601 UTC `Z`, `source: "server"`, and neither new optional field is set.
- On the queued path, implement FR-11's clamp and write each line's reason beside it:
  `claimed` is the payload's `device_claimed_opened_at`; `floor` is the later of
  `Date.parse(account.session.issued_at)` and `readLastContact(account.account_id)`, and is
  `issued_at` alone when the contact value is `null` — silently, because AD-10 already permits a cold
  start to be silent and no message exists for it. Refuse `clock_skew` when `claimed` is earlier than
  `floor` minus `QUEUED_CLOCK_FLOOR_SLACK_SECONDS`, or later than the server's now plus
  `QUEUED_CLOCK_FUTURE_TOLERANCE_SECONDS`. Otherwise set `opened_at` to the later of `claimed` and
  `floor`, `source: "device_reconciled"`, `device_claimed_opened_at: claimed` and `device_offset_s` to
  the whole-second difference between the server's now and `claimed`. Both retained; neither replaces
  the other (FR-11).

**`order_close`.**
- No open segment — closed already, or never opened — returns `conflict` with code `not_open` and
  `CONFLICT_COPY.not_open.sentence` (D-06).
- Otherwise close the open segment with a server-stamped `closed_at`, recompute `elapsed_s` as the sum
  over every segment, and return `recorded` with the clock. Prior segments are retained untouched, so a
  later open appends a new one (FR-9).

**`capture`.**
- `assetInOrder(order, payload.asset_id)` false → `conflict` `asset_not_in_order` (FR-18).
- No running segment on that order's clock → `conflict` `order_closed` with EXPERIENCE.md's sentence
  (D-05). Comment that this is the clock gating the record, and that `WorkOrder.status` is fixture text
  and is consulted by nothing.
- Otherwise write the capture with `captured_by` assigned **only** from `account.account_id`, a
  server-stamped `recorded_at`, and `thumb` carried through as given. For `purpose: "verify"`, call
  `authoredMatch(payload.asset_id, FIXTURE_VERSION)` and `authoredProposals(payload.asset_id,
  FIXTURE_VERSION)`, attach `capture_id` and `verified_at`/`issued_at` here, derive each proposal's id
  with `deriveProposalId(account.account_id, item.client_id, observation_id)`, set each proposal's
  `state: "open"`, `order_id` and `observation_id` — the last carried straight through from the authored
  entry, because AD-5 needs the client to hold that id before it can name the triple in a decision —
  write them, and return them in `result.server.verification` and `result.server.proposals`. For
  `purpose: "evidence"`, return the capture alone and call neither authored function.

**`decision`.**
- At the ownership position (Task 2 step 2),
  `proposalIdMatches(payload.proposal_id, account.account_id, payload.capture_client_id, payload.observation_id)`
  false → `rejected` `unknown_proposal`, before any state read. A fabricated id, another account's id,
  and an id submitted with a missing or wrong identity field all reach this identically (FR-27), because
  all of them are the same digest comparison failing.
- After the match, the client id and the observation id are simply the ones the item supplied: nothing is
  parsed out of the id, because AD-5's id is a digest and the decision item carries its own identity
  fields. Comment that. `wasEvicted` true → `rejected` `store_evicted` with the seed's sentence. Comment
  that this consult happens only after the HMAC has validated, so AD-5's "validation reads nothing from
  the store" holds.
- `readProposal` missing → the proposal was issued by an instance this one never shared state with;
  record the decision anyway, exactly as EXPERIENCE.md requires, and add the comment forbidding any
  "server restarted" wording here or anywhere.
- Present but `state !== "open"` → `conflict` `proposal_superseded`.
- No running segment on the proposal's order → `conflict` `order_closed` (D-05).
- `decided_at` more than `ONLINE_CLOCK_OFFSET_WINDOW_SECONDS` from the server's clock on the immediate
  path → `conflict` `clock_skew`; on the queued path use the same tolerance bounds as `order_open`.
- Otherwise write the `Decision` with `decided_by` assigned only from `account.account_id`,
  `arrived_via` derived here, `decided_where_claimed` carried through as the client's claim, and
  `device_offset_s` measured by the server; move the proposal to `accepted` or `rejected`; return
  `recorded` with both in `result.server`. A rejected proposal is retained with its decision, its
  decider and its time — nothing deletes it (FR-25).

Extend `lib/reconcile/apply.test.mjs` with the state-transition half. Assert:
- open on a fresh order records one segment with `source: "server"`;
- a second open under a different `client_id` returns `duplicate` and leaves one segment (FR-7, D-06);
- close ends the segment and `elapsed_s` is the sum over segments (FR-8);
- close with no open segment returns `not_open` and the detail is `CONFLICT_COPY.not_open.sentence`;
- close, then open, appends a second segment and the first is unchanged (FR-9);
- a queued `order_open` whose claim is before `issued_at` is clamped to the floor, `source` is
  `device_reconciled`, and both `device_claimed_opened_at` and `device_offset_s` are present — D-04's
  direct unit proof of the clamp, named so in the test;
- the same queued item with `readLastContact` stamped later than `issued_at` clamps to the later value;
- with no `last_contact` stamped, the floor is `issued_at` and nothing is reported — assert that no
  attempt entry carries a message about it;
- a claim far in the past and a claim far in the future both return `clock_skew`;
- a capture against an asset not on the order returns `asset_not_in_order`;
- a capture against a closed clock returns `order_closed` with EXPERIENCE.md's sentence — the reachable
  demonstration D-05 describes: close the clock, post a capture, read the refusal;
- a verify-purpose capture on `m-ap003` returns three proposals, each with a derived id, each `open`,
  and each carrying the `observation_id` its id was derived from, so a decision can be composed from the
  response alone; on `m-aa101` it returns a match and zero proposals;
- a decision naming a fabricated `proposal_id` and one naming a valid id derived under another account
  return **deep-equal** `unknown_proposal` results (FR-27);
- a decision naming a valid `proposal_id` with a wrong `observation_id`, and one omitting
  `capture_client_id` altogether, return results deep-equal to those two — every failure to reconstruct
  AD-5's triple is the same refusal by the same comparison, and no shape refusal escapes ahead of it;
- a decision naming a valid `proposal_id` with both identity fields correct is `recorded`, which is what
  proves the pair is read from the item rather than from the id;
- a decision on an `accepted` proposal returns `proposal_superseded`;
- a rejected proposal is still readable with its decision after the decision is recorded (FR-25);
- a body carrying `decided_by: "acc-vanwyk"` produces a decision whose `decided_by` is the acting
  account and the submitted value appears nowhere — search the whole stored record and the attempt
  entry for the string (FR-23's negative set at the writer);
- every record state written in the whole test file is a member of
  `["open","accepted","rejected","superseded"]` — collect and assert once at the end (FR-24).
  </action>
  <acceptance_criteria>
    - `node --test lib/reconcile/apply.test.mjs` exits 0
    - The test file contains a test naming D-04's clamp proof and asserting both `device_claimed_opened_at` and `device_offset_s` are present on the resulting segment
    - The test file contains a test asserting the fabricated-proposal and other-account-proposal results are deep-equal
    - The test file contains a test asserting a wrong `observation_id` and a missing `capture_client_id` produce results deep-equal to those two
    - `grep -n "parseProposalId" lib/reconcile/apply.ts` returns no match — the identity fields come from the item, never from the id
    - The test file contains a test asserting a submitted `decided_by` value appears nowhere in the stored record or the attempt entry
    - `grep -vn '^\s*\*\|^\s*//' lib/reconcile/apply.ts | grep -c "already_open"` returns 0
    - `node -e "import('./lib/data/types.ts').then(m=>0)" &amp;&amp; grep -c "CONFLICT_COPY\|REJECT_COPY" lib/reconcile/apply.ts` returns at least 6 — every sentence is imported, none is restated
    - `grep -n "\"The order was closed\|\"This order is not open\|\"That asset is not on" lib/reconcile/apply.ts` returns no match
    - `npx tsc --noEmit` exits 0, `node scripts/check-governed.mjs` exits 0 and `node scripts/claims-audit.mjs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test lib/reconcile/apply.test.mjs &amp;&amp; node scripts/check-governed.mjs &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>The clock opens idempotently, closes with a stated conflict, reopens without losing a prior segment, and clamps a queued claim to a floor it retains alongside the server's measured offset; a capture and a decision are both gated by the clock; and a forged actor field in a body reaches no record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the request body → the record | Everything in a body is attacker-shaped; the enumeration at parse is the only place a field can be dropped structurally rather than by review |
| the device's clock → the accrued record | A queued claim is the device's word; the clamp and the retained offset are what stop it from becoming the server's word |
| a client-supplied proposal id and its two identity fields → a decision on someone else's proposal | Re-derivation at the ownership position is what makes an unowned id and a fabricated id the same event; the account in the triple is the session's, so supplying another artisan's identity fields only produces a mismatch |
| one account's `client_id` namespace → another's | A `seen` map keyed globally would let one persona's retry return another's stored result |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-22 | Spoofing | a forged actor field (`decided_by`, `captured_by`, `account_id`) in a request body | mitigate | Task 1's `pick` drops every field outside the per-route enumeration and no enumeration contains an actor field; Task 3 assigns `captured_by` and `decided_by` only from `account.account_id`; the unit test searches the stored record and the attempt entry for a submitted value and requires no match. |
| T-3-23 | Tampering | client-supplied observation text, grade or provenance becoming a proposal | mitigate | Task 1's enumerations contain none of `observation`, `grade` or `provenance`; the unit test asserts it by iterating both records; plan 03-12's `check-accepted-fields.mjs` re-asserts it at build time against an independently written table (REQ-FR-61). |
| T-3-24 | Tampering | a device moving its own accrued start time backwards | mitigate | Task 3 clamps a queued `order_open` to the later of the session's `issued_at` and the account's last server contact, refuses outside `clock_skew`'s bounds, and retains the device's claim and the server's measured offset separately so neither replaces the other (D-04, D-07, FR-11). |
| T-3-25 | Information Disclosure | cross-account replay of an idempotency key | mitigate | Task 2 keys `seen` as `accountId:clientId` and the unit test asserts the same `client_id` under two accounts produces two independent `recorded` results (FR-34's server half). |
| T-3-26 | Elevation of Privilege | a decision recorded against another account's proposal | mitigate | Task 3 performs `proposalIdMatches` at the ownership position, before any state is read, re-deriving from the session's account and the item's two identity fields; a fabricated id, a valid id under another account, and an id with a wrong or missing identity field all return `unknown_proposal` and the unit test asserts the results are deep-equal (REQ-FR-27, AD-5). |
| T-3-27 | Denial of Service | an oversized thumbnail, declared byte count or note duration reaching the store | mitigate | Task 1 refuses `media_too_large` against `THUMB_MAX_ENCODED_BYTES`, `CAPTURE_MAX_DECLARED_BYTES` and `VOICE_MAX_DURATION_MS` before the write path is reached, and the unit test proves the boundary one character either side of the thumbnail cap (REQ-FR-19). |
| T-3-28 | Repudiation | a write that produces no retained entry, or a state outside the closed set | mitigate | Task 2 writes an `AttemptEntry` on every path including success, and asserts every record state it writes is a member of `{open, accepted, rejected, superseded}`; the unit test collects every state written across the file and asserts membership once (REQ-FR-24, AD-19). |
</threat_model>

<verification>
- `node --test lib/reconcile/validate.test.mjs` and `node --test lib/reconcile/apply.test.mjs` both exit 0.
- `grep -rn "writeCapture\|writeDecision\|writeClockSegment\|closeClockSegment\|writeSeen\|writeAttempt\|stampLastContact\|recordEviction" lib app --include=*.ts` names only `lib/store/memory.ts` and `lib/reconcile/apply.ts`.
- `node scripts/check-governed.mjs` and `node scripts/claims-audit.mjs` both exit 0.
- `npm run verify` exits 0 end to end.
- Each task is one commit; after `gsd-sdk query commit`, confirm with `git log -1 --format=%B` that the `Co-Authored-By` trailer survived, and amend if it did not.
</verification>

<success_criteria>
- `lib/reconcile/apply.ts` is the sole writer for the online routes and `/api/sync` alike, with the check order session → ownership → idempotency → shape → state, and refusals written as retained attempts by the same writer (roadmap success criterion 4, REQ-FR-24).
- Every capture-bearing path accepts exactly `{sha256, bytes, mime, duration_ms}` plus a bounded thumbnail (roadmap success criterion 4, REQ-FR-15, REQ-FR-16).
- The offline clock segment is clamped no earlier than the session's issue time and the device's last server contact, with claim and measured offset both retained (roadmap success criterion 5, REQ-FR-11).
- Opening is idempotent, closing a closed or never-opened order returns `not_open`, and reopening retains prior segments (REQ-FR-7, REQ-FR-8, REQ-FR-9).
</success_criteria>

<output>
Create `.planning/phases/03-server-seam/03-05-SUMMARY.md` when done
</output>
