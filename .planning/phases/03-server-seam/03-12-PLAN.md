---
phase: 03-server-seam
plan: 12
type: execute
wave: 5
depends_on: ["03-07", "03-08", "03-09", "03-10", "03-11"]
files_modified:
  - scripts/check-single-writer.mjs
  - scripts/check-single-writer.test.mjs
  - scripts/check-actor-field.mjs
  - scripts/check-actor-field.test.mjs
  - scripts/check-accepted-fields.mjs
  - scripts/check-accepted-fields.test.mjs
  - scripts/verify.mjs
  - scripts/verify.test.mjs
autonomous: true
requirements: [REQ-FR-10, REQ-FR-15, REQ-FR-16, REQ-FR-23, REQ-FR-24, REQ-FR-57, REQ-FR-61]

must_haves:
  truths:
    - "REQ-FR-24: a build rule asserts nothing but `lib/reconcile/apply.ts` writes to the record store, walked transitively over the import graph from `app/api` and `lib`"
    - "REQ-FR-23: a build rule asserts no module other than `lib/attribution` produces an actor field and no route schema contains one"
    - "REQ-FR-61 and AD-20: a build rule asserts every write route's accepted-field enumeration matches an independently written table, and that a declared-but-unexpected field is a failure as much as a missing one"
    - "REQ-FR-10: no enumeration anywhere carries an artisan-supplied duration or hour value, asserted by the same rule"
    - "REQ-FR-57: no route consults `rbac_tier` and no authorisation is decided outside `lib/access/scope.ts`, asserted by the actor-field rule's companion sweep"
    - "Each rule fails rather than warns, is not skippable, joins `verify.mjs` STEPS, and ships with a fixture proving it exits non-zero (Phase 1 D-20, D-23)"
  artifacts:
    - path: "scripts/check-single-writer.mjs"
      provides: "The AD-1 assertion: only apply.ts reaches the store's mutating exports"
      min_lines: 150
    - path: "scripts/check-actor-field.mjs"
      provides: "The AD-3 assertion: only lib/attribution produces an actor field, and no route schema names one"
      min_lines: 150
    - path: "scripts/check-accepted-fields.mjs"
      provides: "The AD-20 assertion: per-route accepted fields, compared against an independently written table"
      min_lines: 130
  key_links:
    - from: "scripts/verify.mjs STEPS"
      to: "the three new rules"
      via: "three source-side steps after check-named-packages, before next-build"
      pattern: "check-single-writer"
    - from: "scripts/check-accepted-fields.mjs"
      to: "lib/reconcile/validate.ts"
      via: "comparing the module's ACCEPTED_BODY_FIELDS against the script's own literal table, in both directions"
      pattern: "ACCEPTED_BODY_FIELDS"
---

<objective>
Turn the phase's three remaining architectural claims into build rules: one writer, one attribution
producer, one accepted-field enumeration per route.

Purpose: FR-23, FR-24 and FR-61 are all verified by analysis, and this repository's standing convention
is that an analysis claim which is not a check is a claim a future commit can quietly break. All three
rules follow patterns that already exist here — `check-register-isolation.mjs`'s import-graph walk and
`check-headers.mjs`'s expected-table comparison — so none of them is novel machinery.

Output: three check scripts, three fixture tests, three gate steps, and a step count of twenty-four.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-server-seam/03-CONTEXT.md
@.planning/phases/03-server-seam/03-RESEARCH.md
@.planning/phases/03-server-seam/03-PATTERNS.md
@.planning/phases/03-server-seam/03-VALIDATION.md
@.planning/phases/03-server-seam/03-05-SUMMARY.md
</context>

<interfaces>
scripts/check-register-isolation.mjs
- `toPosix`/`toRel` (53–61), `walkSourceFiles` (63–84), `loadAliasPrefix` (105–130, the tsconfig `@/*`
  mapping), `extractSpecifiers` (132–144, a regex extraction that states in a comment that it is not a
  full parser), `resolveImport` (146–175), `findViolation` (177–208, a BFS transitive walk, shortest
  chain first, per-root visited set so a cycle terminates)

scripts/check-headers.mjs
- `EXPECTED_BLOCKS` and the two-directional comparison at lines 130–145: a missing expected entry is a
  problem, and a declared-but-unexpected entry is also a problem. That second half is exactly AD-20.

scripts/check-governed.mjs lines 145–175 and 295–330
- `extractField`-style anchored regexes over source, and the whole-tree text sweep

scripts/lib/fixtures.mjs — `repoRoot()`, `withFixture(files, fn)`, `runCheck(script, { cwd })`

lib/store/memory.ts (03-02)
- mutating exports, all prefixed or named by convention: `writeCapture`, `writeProposals`,
  `writeDecision`, `writeClockSegment`, `closeClockSegment`, `writeSeen`, `writeAttempt`,
  `stampLastContact`, `recordEviction`, `sweep`
- read-only exports: `readClock`, `readClocksForAccount`, `readCaptures`, `readProposal`,
  `readProposalsForAsset`, `readDecisions`, `readSeen`, `readAttempts`, `readLastContact`,
  `wasEvicted`, `BOOT_ID`, `storeStats`, `uptimeSeconds`

lib/reconcile/validate.ts (03-05)
- `ACCEPTED_BODY_FIELDS` keyed `session`, `orders_open`, `orders_close`, `verify`, `captures`,
  `decisions`, `sync`; `ACCEPTED_PAYLOAD_FIELDS` keyed by `SyncItemKind`

scripts/verify.mjs / scripts/verify.test.mjs
- `STEPS` — twenty-one entries after plan 03-06; `check-named-packages` is the fourteenth and
  `next-build` follows it. `EXPECTED_ORDER` carries twenty-one ids; the `resolveSteps` length
  assertions are `19` for the `VERCEL: "1"` case and `21` for the three unset-or-distractor cases; the
  order test's name and the header comment carry the word "twenty-one".
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/check-single-writer.mjs with its fixture test</name>
  <files>scripts/check-single-writer.mjs, scripts/check-single-writer.test.mjs</files>
  <read_first>
    - scripts/check-register-isolation.mjs — the whole file; every helper is reused unchanged and only the roots and the forbidden set differ
    - scripts/check-register-isolation.test.mjs lines 29–116 — the fixture-test shape
    - scripts/lib/fixtures.mjs — `repoRoot`, `withFixture`, `runCheck`
    - lib/store/memory.ts — the exact mutating and read-only export names, and the header comment stating the naming convention
    - lib/reconcile/apply.ts — the one permitted importer
    - .planning/phases/03-server-seam/03-RESEARCH.md — §Code Examples 6, which states the shape and the honest limitation
    - .planning/phases/03-server-seam/03-PATTERNS.md — §`scripts/check-single-writer.mjs`
  </read_first>
  <action>
Create `scripts/check-single-writer.mjs`, reusing `check-register-isolation.mjs`'s helpers verbatim.

Declare two constants at the top of the file, each with a comment, so a future addition is a deliberate
edit rather than a silent widening:
- `MUTATING_EXPORTS` — the ten names `lib/store/memory.ts` exports as mutators, listed explicitly
  rather than derived from a prefix, because a prefix rule would let a new mutator named outside the
  convention slip through.
- `PERMITTED_IMPORTERS = ["lib/reconcile/apply.ts"]` — one entry. Comment that AD-1 is the reason there
  is exactly one, and that adding a second is an architectural change, not a maintenance edit.

Assert two things.

**Assertion 1 — the direct import.** Walk `app/api` and `lib` with `walkSourceFiles`. For each file that
is not a permitted importer and not `lib/store/memory.ts` itself, extract its specifiers and report a
problem when it imports `lib/store/memory.ts` **and** names any member of `MUTATING_EXPORTS` in its
import clause. Resolve the `@/*` alias with `loadAliasPrefix` so both spellings are caught. A file that
imports only read-only accessors is fine and must not be reported — say so in a comment, with the
reason: the responder needs `BOOT_ID` and the walk payload needs the readers, and a rule that banned
those would be a rule nobody could keep.

**Assertion 2 — the transitive reach.** Using `findViolation`'s BFS, report a problem when a file under
`app/api` reaches `lib/store/memory.ts` through any chain that does not pass through a permitted
importer. Report the shortest chain. Comment that a route reaching the store even read-only through a
helper other than the ones named above is worth flagging, because the next commit turns a read into a
write.

**Assertion 3 — the responder's monopoly.** Sweep `app/api/**` and `lib/**` for `NextResponse` and
`new Response(` outside `lib/http/respond.ts`, ignoring comment lines. AD-11 makes the responder the
only constructor, and AD-4's byte-identity is void by construction if a route can hand-write a 404.
Strip lines matching `^\s*(//|\*|/\*)` before counting, and say in a comment that a naive substring
count over a self-documenting file is self-invalidating, which is why the filter is there.

Carry `check-register-isolation.mjs`'s honest-caveat comment into the header in your own words: the
specifier extraction is a regex, not a parser, so a dynamic `import()` built from a runtime string and a
re-exported binding under a different name are both outside what this can catch.

Follow the `problems[]` and report-and-exit convention exactly. Resolve every path from `process.cwd()`.
No flag, no environment variable, no skip.

Create `scripts/check-single-writer.test.mjs` with `withFixture`/`runCheck`, each tree carrying a
minimal `tsconfig.json` plus the files the case needs. Cover:
- the real repository exits 0;
- a fixture where `app/api/captures/route.ts` imports `writeCapture` from `lib/store/memory.ts` exits
  non-zero and names the file;
- the same violation written through the `@/` alias exits non-zero;
- a fixture where a route imports a helper that imports `writeDecision` exits non-zero and names the
  two-hop chain;
- a fixture where a route imports only `BOOT_ID` and `readClock` exits **0** — read-only reach is not a
  violation, proved rather than assumed;
- a fixture where `app/api/orders/route.ts` calls `NextResponse.json` exits non-zero;
- a fixture where `NextResponse` appears only inside a comment exits 0 — the self-invalidation guard;
- a fixture with no `app/api` directory exits 0.
  </action>
  <acceptance_criteria>
    - `node scripts/check-single-writer.mjs` exits 0 against the real repository and prints `Problems: 0`
    - `node --test scripts/check-single-writer.test.mjs` exits 0
    - The test file contains a case proving read-only reach exits 0 and a case proving a two-hop write reach exits non-zero
    - The test file contains a comment-only `NextResponse` case that exits 0
    - `grep -n "PERMITTED_IMPORTERS" scripts/check-single-writer.mjs` shows exactly one entry
    - `grep -n "not a full parser\|dynamic" scripts/check-single-writer.mjs` returns a match
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-single-writer.mjs &amp;&amp; node --test scripts/check-single-writer.test.mjs</automated>
  </verify>
  <done>Nothing but the one writer can reach the store's mutators, nothing but the responder can construct a response, both are proved transitively and through the alias, and the rule states what its own regex cannot see.</done>
</task>

<task type="auto">
  <name>Task 2: Write scripts/check-actor-field.mjs with its fixture test</name>
  <files>scripts/check-actor-field.mjs, scripts/check-actor-field.test.mjs</files>
  <read_first>
    - scripts/check-single-writer.mjs — Task 1's script; this one reuses the same helpers and the same report convention
    - scripts/check-governed.mjs lines 145–175 — the anchored-regex-over-source style for the assignment sweep
    - lib/attribution/index.ts — the one permitted producer and its two exports
    - lib/reconcile/apply.ts — the one permitted assigner, and the exact right-hand side it uses
    - lib/reconcile/validate.ts — `ACCEPTED_BODY_FIELDS` and `ACCEPTED_PAYLOAD_FIELDS`, which must contain no actor field
    - lib/data/types.ts — the actor-bearing fields: `Capture.captured_by`, `Decision.decided_by`, `Referral.raised_by`, `Flag.raised_by`, `Session.account_id`
    - .planning/phases/03-server-seam/03-PATTERNS.md — §`scripts/check-actor-field.mjs`, which recommends combining the import walk with a regex assignment scan
    - .planning/REQUIREMENTS.md — REQ-FR-23 and REQ-FR-57 in full
  </read_first>
  <action>
Create `scripts/check-actor-field.mjs`. Declare the vocabulary at the top, each with a comment:
- `ACTOR_FIELDS = ["captured_by", "decided_by", "raised_by", "account_id"]`
- `PERMITTED_ASSIGNERS = ["lib/attribution/index.ts", "lib/reconcile/apply.ts"]` — two entries, with the
  reason: `lib/attribution` produces the acting account and `lib/reconcile/apply.ts` is the one writer
  that stamps it onto a record. Comment that a third entry is an architectural change.
- `PERMITTED_RHS` — the exact right-hand-side forms `lib/reconcile/apply.ts` may use when assigning an
  actor field: `account.account_id`, `acting.account_id` and `null`. Anything else in a permitted
  assigner is still a problem.

Assert four things.

**Assertion 1 — the assignment sweep.** Walk `app/api` and `lib`, strip comment lines, and match an
anchored regex for an object-literal assignment to any `ACTOR_FIELDS` member (`captured_by:` and the
rest). Report every match in a file outside `PERMITTED_ASSIGNERS`. Inside a permitted assigner, report a
match whose right-hand side is not one of `PERMITTED_RHS`. Quote the offending line in the problem.

**Assertion 2 — the route schemas.** Sweep every file under `app/api` for any `ACTOR_FIELDS` member
appearing at all outside a comment. A route may mention `decided_by` in prose explaining that the field
is dropped at parse; it may not name it in code. This is FR-23's "no route schema contains one".

**Assertion 3 — the enumerations.** Import `ACCEPTED_BODY_FIELDS` and `ACCEPTED_PAYLOAD_FIELDS` from
the working directory's own `lib/reconcile/validate.ts` with a dynamic `import()` of a `file://` URL
built from `process.cwd()` — the pattern `scripts/check-fixture-hash.mjs` already uses, and for the same
reason: a check that imports from a fixed path would pass every fixture and prove nothing. Report a
problem when any array contains an `ACTOR_FIELDS` member. Allow `claimed_account_id` explicitly, with
the reason in a comment: `lib/data/types.ts` says it is compared and never trusted, and it never becomes
an actor field on a record.

**Assertion 4 — the RBAC companion sweep (FR-57).** Report a problem when `rbac_tier` appears outside a
comment anywhere under `app/api` or `lib/access`, and when any file other than `lib/access/scope.ts`
imports `ORDER_IDS_BY_ARTISAN` from `lib/data/artisans.ts`. Comment that FR-57 is two claims — no route
consults RBAC tier, and one accessor decides authorisation — and that this is both of them.

Follow the `problems[]` and report-and-exit convention. Resolve every path from `process.cwd()`. No
flag, no environment variable, no skip. Carry the same honest-limitation comment: this is a text sweep
plus an import walk, so an actor field assigned through a computed key or a spread is outside what it
can see.

Create `scripts/check-actor-field.test.mjs` covering:
- the real repository exits 0;
- a fixture where `app/api/decisions/route.ts` assigns `decided_by:` exits non-zero and quotes the line;
- a fixture where `lib/walk/payload.ts` assigns `captured_by:` exits non-zero;
- a fixture where `lib/reconcile/apply.ts` assigns `decided_by: body.decided_by` exits non-zero — a
  permitted assigner with a forbidden right-hand side;
- a fixture where `lib/reconcile/apply.ts` assigns `decided_by: account.account_id` exits 0;
- a fixture where a route mentions `decided_by` only in a comment exits 0;
- a fixture whose `lib/reconcile/validate.ts` adds `decided_by` to `ACCEPTED_BODY_FIELDS.decisions`
  exits non-zero;
- a fixture whose enumeration contains `claimed_account_id` exits 0;
- a fixture where `app/api/orders/route.ts` reads `rbac_tier` exits non-zero;
- a fixture where a second module imports `ORDER_IDS_BY_ARTISAN` exits non-zero.
  </action>
  <acceptance_criteria>
    - `node scripts/check-actor-field.mjs` exits 0 against the real repository and prints `Problems: 0`
    - `node --test scripts/check-actor-field.test.mjs` exits 0
    - The test file contains a case where a permitted assigner uses a forbidden right-hand side and the check exits non-zero
    - The test file contains a case where the enumeration gains an actor field and the check exits non-zero
    - The test file contains an `rbac_tier` case and an `ORDER_IDS_BY_ARTISAN` case, both exiting non-zero
    - `grep -n "claimed_account_id" scripts/check-actor-field.mjs` shows it explicitly allowed with its reason
    - `grep -n "computed key\|spread" scripts/check-actor-field.mjs` returns a match — the limitation is stated
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-actor-field.mjs &amp;&amp; node --test scripts/check-actor-field.test.mjs</automated>
  </verify>
  <done>An actor field can be assigned in two named places and only from the acting account, no route names one in code, no enumeration carries one, and the two halves of FR-57 are checked beside them.</done>
</task>

<task type="auto">
  <name>Task 3: Write scripts/check-accepted-fields.mjs, its fixture test, and wire all three rules into verify</name>
  <files>scripts/check-accepted-fields.mjs, scripts/check-accepted-fields.test.mjs, scripts/verify.mjs, scripts/verify.test.mjs</files>
  <read_first>
    - scripts/check-headers.mjs — the whole file, especially `EXPECTED_BLOCKS` and the two-directional comparison at lines 130–145: a missing expected key is a problem and a declared-but-unexpected key is also a problem
    - lib/reconcile/validate.ts — `ACCEPTED_BODY_FIELDS` and `ACCEPTED_PAYLOAD_FIELDS` as plan 03-05 wrote them
    - app/api/session/route.ts, app/api/orders/[id]/open/route.ts, app/api/orders/[id]/close/route.ts, app/api/verify/route.ts, app/api/captures/route.ts, app/api/decisions/route.ts, app/api/sync/route.ts — each route's `pick(...)` call and which enumeration key it names
    - scripts/check-fixture-hash.mjs lines 55–80 — the dynamic `import()` of a `file://` URL built from `process.cwd()`, and the comment explaining why a fixed path would make the check vacuous
    - .planning/phases/03-server-seam/03-PATTERNS.md — §`scripts/check-accepted-fields.mjs`
    - scripts/verify.mjs and scripts/verify.test.mjs — `STEPS` and `EXPECTED_ORDER` at twenty-one entries
    - .planning/REQUIREMENTS.md — REQ-FR-10, REQ-FR-15, REQ-FR-16, REQ-FR-61
  </read_first>
  <action>
Create `scripts/check-accepted-fields.mjs`. Its core is `check-headers.mjs`'s shape with
`{ source, headers }` replaced by `{ routeKey, file, fields }`.

Write the expected table as a literal in this script — independently of
`lib/reconcile/validate.ts` — and say why in a comment: a check that imported its expected value from
the module it checks would pass every fixture and prove nothing, which is the lesson
`scripts/check-fixture-hash.mjs` already records. Two independent statements of the same table,
cross-checked, is the mechanism.

`EXPECTED_ROUTES` entries, one per write surface:
- `session` → `app/api/session/route.ts` → `["persona_id"]`
- `orders_open` → `app/api/orders/[id]/open/route.ts` → `["client_id"]`
- `orders_close` → `app/api/orders/[id]/close/route.ts` → `["client_id"]`
- `verify` → `app/api/verify/route.ts` → `["client_id","order_id","asset_id","kind","purpose","captured_at","sha256","bytes","mime","duration_ms","thumb"]`
- `captures` → `app/api/captures/route.ts` → the same list as `verify`
- `decisions` → `app/api/decisions/route.ts` → `["client_id","proposal_id","outcome","decided_at","decided_where_claimed","note"]`
- `sync` → `app/api/sync/route.ts` → `["items"]`

and `EXPECTED_PAYLOADS`, one per `SyncItemKind`:
`order_open` → `["device_claimed_opened_at"]`; `order_close` → `["device_claimed_closed_at"]`;
`capture` → `["asset_id","kind","purpose","captured_at","sha256","bytes","mime","duration_ms","thumb"]`;
`decision` → `["proposal_id","outcome","decided_at","decided_where_claimed","note"]`;
`referral` → `[]`.

Assert four things.

**Assertion 1 — both directions.** Import `ACCEPTED_BODY_FIELDS` and `ACCEPTED_PAYLOAD_FIELDS` from the
working directory's own `lib/reconcile/validate.ts` with a dynamic `import()` of a `file://` URL built
from `process.cwd()`. For every expected key, report a problem when the module's array is missing a
field; for every field the module declares, report a problem when the expected table does not carry it.
The second half is AD-20 as a rule: a declared-but-unexpected field is a failure as much as a missing
one.

**Assertion 2 — the route uses its own key.** Read each route file and require it to contain a `pick(`
call naming its `routeKey` from `ACCEPTED_BODY_FIELDS`. Report a problem when a route named in
`EXPECTED_ROUTES` does not call `pick`, and when a file exists under `app/api` with a `POST` export that
is not in `EXPECTED_ROUTES` at all — a new write route must join this table or fail the build.

**Assertion 3 — the forbidden vocabulary.** Report a problem when any array in either record, or either
expected table, contains `captured_by`, `decided_by`, `raised_by`, `account_id`, `observation`, `grade`,
`provenance`, `elapsed_s`, `hours` or `duration_s`. Comment the three requirements this covers: FR-23
(no actor field), FR-61 (no client-supplied observation, grade or provenance) and FR-10 (no
artisan-supplied duration or hour value). Note the deliberate distinction: `duration_ms` on a voice
capture is a media duration and is accepted; `duration_s` on a clock is not.

**Assertion 4 — the capture enumerations.** Assert `EXPECTED_PAYLOADS.capture` contains exactly
`sha256`, `bytes` and `mime` among its media fields plus `duration_ms` and `thumb`, and carries no field
whose name suggests a full-resolution image. FR-15 and FR-16 are the reason; write it in the comment.

Follow the `problems[]` and report-and-exit convention. Resolve every path from `process.cwd()`.

Create `scripts/check-accepted-fields.test.mjs` covering: the real repository exits 0; a fixture whose
`validate.ts` drops `sha256` from the capture enumeration exits non-zero; a fixture whose `validate.ts`
adds `decided_by` exits non-zero; a fixture whose `validate.ts` adds an unexpected but harmless field
exits non-zero (the declared-but-unexpected half); a fixture where `app/api/decisions/route.ts` has no
`pick(` call exits non-zero; a fixture that adds `app/api/referrals/route.ts` with a `POST` export exits
non-zero naming it as an unenumerated write route; a fixture with a clean tree exits 0.

Then wire all three of this plan's rules into `scripts/verify.mjs`'s `STEPS`, immediately after
`check-named-packages` and before `next-build`, in this order: `check-single-writer`,
`check-actor-field`, `check-accepted-fields`, each
`{ id, command: process.execPath, args: ["scripts/<name>.mjs"] }`. No `shell: true`, no
`vercelExcluded: true`.

Update `scripts/verify.test.mjs` in the same commit: insert the three ids at those positions, giving
twenty-four; change the order test's name and the header comment from "twenty-one" to "twenty-four";
change the `resolveSteps` length assertions from `19` to `22` for the `VERCEL: "1"` case and from `21` to
`24` for the three unset-or-distractor cases, including any test name that carries the word. Leave the
four structural tests unchanged. If `STEPS.length` is not 21 when you start, reconcile to the value you
find plus three and record the discrepancy in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `node scripts/check-accepted-fields.mjs` exits 0 against the real repository and prints `Problems: 0`
    - `node --test scripts/check-accepted-fields.test.mjs` exits 0
    - The test file contains a declared-but-unexpected-field case that exits non-zero, and an unenumerated-write-route case that exits non-zero
    - `grep -n "process.cwd()" scripts/check-accepted-fields.mjs` shows the dynamic import built from the working directory, not a fixed path
    - `node -e "import('./scripts/verify.mjs').then(m=>console.log(m.STEPS.length))"` prints `24`
    - `node -e "import('./scripts/verify.mjs').then(m=>{const i=m.STEPS.map(s=>s.id);console.log(i.slice(i.indexOf('check-named-packages'),i.indexOf('next-build')).join(','))})"` prints `check-named-packages,check-single-writer,check-actor-field,check-accepted-fields`
    - `node --test scripts/verify.test.mjs` exits 0 and `grep -n "twenty-one" scripts/verify.test.mjs` returns no match
    - `npm run verify` exits 0 end to end with twenty-four steps
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-accepted-fields.mjs &amp;&amp; node --test scripts/check-accepted-fields.test.mjs &amp;&amp; node --test scripts/verify.test.mjs</automated>
  </verify>
  <done>Every write route's accepted fields are stated twice and cross-checked in both directions, a new write route cannot ship without joining the table, the forbidden vocabulary is enumerated once, and all three of this plan's rules run inside the gate on both platforms.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a future commit → AD-1, AD-3 and AD-20 | Three architectural claims that survive only as long as a rule enforces them |
| a check's expected value → the thing it checks | A check that imports its expectation from its subject passes every fixture and proves nothing |
| a new write route → the enumeration | A route added later without an enumeration would be an unbounded parse surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-56 | Tampering | a second module writing to the record store | mitigate | Task 1 walks the import graph from `app/api` and `lib` for the store's ten named mutating exports with one permitted importer, transitively and through the `@/` alias, and proves read-only reach is not a violation so the rule is keepable. |
| T-3-57 | Spoofing | an actor field assigned outside `lib/attribution`'s output | mitigate | Task 2 sweeps for object-literal assignments to the four actor fields, allows only two files, and inside those allows only three right-hand sides; a permitted assigner using `body.decided_by` is a named failure with a fixture proving it. |
| T-3-58 | Tampering | a client-supplied observation, grade, provenance, duration or hour value reaching a record | mitigate | Task 3 asserts the forbidden vocabulary against both the module's records and the script's own table, and compares the two in both directions so a declared-but-unexpected field fails as hard as a missing one (REQ-FR-10, REQ-FR-61, AD-20). |
| T-3-59 | Elevation of Privilege | an authorisation decision taken outside `lib/access/scope.ts`, or a route consulting `rbac_tier` | mitigate | Task 2's companion sweep reports `rbac_tier` outside a comment under `app/api` or `lib/access`, and any import of `ORDER_IDS_BY_ARTISAN` outside the accessor (REQ-FR-57). |
| T-3-60 | Repudiation | a rule that cannot fail | mitigate | All three ship fixture tests with one case per violation class, including comment-only cases proving the text sweeps are neither self-tripping nor blind, and Task 3's expected table is written independently of the module it checks. |
| T-3-61 | Information Disclosure | a route hand-writing a response and so a second not-found producer | mitigate | Task 1's third assertion sweeps `app/api/**` and `lib/**` for `NextResponse` and `new Response(` outside `lib/http/respond.ts`, with a fixture proving it fails and a comment-only fixture proving it is not self-tripping (AD-4, AD-11). |
</threat_model>

<verification>
- `node scripts/check-single-writer.mjs`, `node scripts/check-actor-field.mjs` and `node scripts/check-accepted-fields.mjs` all exit 0 with `Problems: 0`.
- All three `.test.mjs` files and `scripts/verify.test.mjs` exit 0.
- `npm run verify` exits 0 end to end with twenty-four steps, locally and in the GitHub Actions `verify` job.
- Each task is one commit; after `gsd-sdk query commit`, confirm with `git log -1 --format=%B` that the `Co-Authored-By` trailer survived, and amend if it did not.
</verification>

<success_criteria>
- The single-writer rule, the actor-field rule and the accepted-field enumerations all run inside `npm run verify` (roadmap success criterion 3, REQ-FR-10, REQ-FR-23, REQ-FR-24, REQ-FR-61).
- Every capture-bearing route accepts exactly `{sha256, bytes, mime, duration_ms}` plus a bounded thumbnail, asserted against an independently written table (roadmap success criterion 4, REQ-FR-15, REQ-FR-16).
- Each rule ships with a fixture proving it exits non-zero (Phase 1 D-23).
</success_criteria>

<output>
Create `.planning/phases/03-server-seam/03-12-SUMMARY.md` when done
</output>
