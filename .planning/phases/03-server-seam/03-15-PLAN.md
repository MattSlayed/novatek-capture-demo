---
phase: 03-server-seam
plan: 15
type: execute
wave: 7
depends_on: ["03-13", "03-14"]
files_modified:
  - scripts/check-non-bypassability.mjs
  - scripts/check-non-bypassability.test.mjs
  - scripts/verify.mjs
  - scripts/verify.test.mjs
autonomous: true
requirements: [REQ-FR-24, REQ-FR-57]

must_haves:
  truths:
    - "D-12: a structure check in `verify` fails when any `app/api/**/route.ts`, any `process.env.<NAME>` read under `app/`, `lib/` or `next.config.ts`, or any module that writes to the store is absent from `docs/analysis/single-writer-non-bypassability.md` by path or name"
    - "D-12: every later phase that adds a route or an env read must extend the document or the build fails, which is how AD-19's enumeration is honoured without generating prose"
    - "The check reads the document as a literal string-membership sweep, matching the `check-governed` and `claims-audit` precedent, and states that limitation in its own header"
    - "The rule ships with a fixture proving it exits non-zero when a route is added and the document is not extended (Phase 1 D-23)"
  artifacts:
    - path: "scripts/check-non-bypassability.mjs"
      provides: "The completeness sweep over the non-bypassability document"
      min_lines: 130
    - path: "scripts/check-non-bypassability.test.mjs"
      provides: "Fixtures proving a missing route, a missing env name and a missing writer each exit non-zero"
      min_lines: 110
  key_links:
    - from: "scripts/verify.mjs STEPS"
      to: "scripts/check-non-bypassability.mjs"
      via: "a source-side step after check-accepted-fields, before next-build"
      pattern: "check-non-bypassability"
    - from: "scripts/check-non-bypassability.mjs"
      to: "docs/analysis/single-writer-non-bypassability.md"
      via: "a literal string-membership sweep over the document's text"
      pattern: "single-writer-non-bypassability"
---

<objective>
Close the loop on the enumeration: a build rule that fails whenever the codebase grows a route, an
environment read or a store-writing module that the non-bypassability document does not name.

Purpose: D-12 is explicit that the document is hand-written and the check is what keeps it honest over
time. AD-19 asks for an enumeration produced by the gate's own command; this is how that is satisfied
without a generator writing prose nobody reads.

Output: one check script, one fixture test, one gate step, and a step count of twenty-six — the last
automated addition this phase makes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-server-seam/03-CONTEXT.md
@.planning/phases/03-server-seam/03-RESEARCH.md
@.planning/phases/03-server-seam/03-PATTERNS.md
@.planning/phases/03-server-seam/03-VALIDATION.md
@.planning/phases/03-server-seam/03-13-SUMMARY.md
@.planning/phases/03-server-seam/03-14-SUMMARY.md
</context>

<interfaces>
scripts/check-structure.mjs
- The whole file (205 lines): the `FORBIDDEN_NAMES`/`FORBIDDEN_DIRS` must-be-absent loop at lines
  85–110, the `problems[]` convention, and the `--build-output` additive-mode pattern at lines 133–186
  ("additive, never a substitute"). D-12 names this script as the model and leaves it to this plan
  whether the rule extends it or ships beside it.

scripts/check-governed.mjs lines 295–330 and scripts/claims-audit.mjs
- The "sweep source as text" precedent this check follows, and the honest caveat each of them carries
  about being a string sweep

scripts/check-single-writer.mjs (03-12)
- `MUTATING_EXPORTS` and `PERMITTED_IMPORTERS`; this check needs the same notion of "a module that
  writes to the store" and must not restate it — import it or duplicate it deliberately, and say which
  and why

scripts/lib/fixtures.mjs — `repoRoot()`, `withFixture`, `runCheck`

docs/analysis/single-writer-non-bypassability.md (03-14)
- The document under sweep, with `## Routes`, `## Configuration`, `## The single writer`,
  `## The single accessor` and `## Open items`

scripts/verify.mjs / scripts/verify.test.mjs
- `STEPS` — twenty-five entries after plan 03-13. `check-accepted-fields` is the seventeenth and
  `next-build` follows it; `route-suite` sits after `check-register-isolation-bundle`. `EXPECTED_ORDER`
  carries twenty-five ids; the `resolveSteps` length assertions are `23` for the `VERCEL: "1"` case and
  `25` for the three unset-or-distractor cases; the order test's name and the header comment carry the
  word "twenty-five".
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/check-non-bypassability.mjs with its fixture test</name>
  <files>scripts/check-non-bypassability.mjs, scripts/check-non-bypassability.test.mjs</files>
  <read_first>
    - scripts/check-structure.mjs — the whole file, for the assertion style, the `problems[]` convention and the additive-mode pattern
    - scripts/check-structure.test.mjs lines 26–153 — the fixture-test shape: a real-repository-exits-0 test then one fixture per violation type, each asserting `code !== 0` and a specific `stdout` match
    - scripts/check-single-writer.mjs — `MUTATING_EXPORTS` and `PERMITTED_IMPORTERS` as plan 03-12 wrote them
    - scripts/check-governed.mjs lines 295–330 — the whole-tree text sweep
    - scripts/lib/fixtures.mjs — `repoRoot`, `withFixture`, `runCheck`
    - docs/analysis/single-writer-non-bypassability.md — the document this sweeps
    - .planning/phases/03-server-seam/03-CONTEXT.md — D-12 in full
    - .planning/phases/03-server-seam/03-RESEARCH.md — §Open Questions 2 and its recommendation of a literal string-membership sweep over a structured parse
  </read_first>
  <action>
Ship this as a new script, `scripts/check-non-bypassability.mjs`, rather than as an extension of
`check-structure.mjs`. Write the reason in the header: `check-structure.mjs` asserts things are absent
and this asserts things are present, the two failure messages read very differently to whoever is
debugging a red build, and a new script keeps each one's fixture set readable. D-12 leaves the choice
open and this is the choice.

Also state the limitation first, in the claims-audit's own voice: this is a literal string-membership
sweep over the document's text. It proves every route, every environment read and every store-writing
module is **named** in the document. It cannot prove the sentence beside the name is true, and it never
claims to.

Read `docs/analysis/single-writer-non-bypassability.md` once into a string. Report a named problem when
the file does not exist, rather than passing.

**Sweep 1 — routes.** Enumerate every `app/api/**/route.ts` with `globSync` (or the `walkSourceFiles`
helper, whichever reads better) and normalise every path to forward slashes so the check behaves the
same on Windows and Linux — say so in a comment; this repository has already been bitten by path
separators. Report a problem per route whose normalised path does not appear in the document, with a
message naming the path and telling the reader to add a `###` entry under `## Routes`.

**Sweep 2 — environment reads.** Scan `app/`, `lib/` and `next.config.ts` for `process.env.<NAME>`
matches with an anchored regex, ignoring comment lines so the document's own discussion of a variable
cannot satisfy its own requirement — strip lines matching `^\s*(//|\*|/\*)` before matching, and say why
in a comment. Collect the distinct names and report a problem per name absent from the document.

**Sweep 3 — store writers.** Determine which modules import any of `check-single-writer.mjs`'s
`MUTATING_EXPORTS` from `lib/store/memory.ts`. Import that constant from
`scripts/check-single-writer.mjs` rather than restating the ten names, and comment the reason: the two
rules must agree on what a write is, and two lists would drift. Report a problem per writing module
whose path is absent from the document, plus a problem when `lib/store/memory.ts` itself is absent.

**Sweep 4 — the required sections.** Report a problem when the document is missing any of
`## Scope`, `## The single writer`, `## The single accessor`, `## Routes`, `## Configuration`,
`## What a reviewer can run` or `## Open items`. A document that lost a section has lost the structure
the sweeps above assume.

Resolve every path from `process.cwd()` so `runCheck(script, { cwd })` exercises it against a fixture
tree. Follow the `problems[]` and report-and-exit convention. No flag, no environment variable, no skip.

Create `scripts/check-non-bypassability.test.mjs` with `withFixture`/`runCheck`. Because the fixtures
need a plausible tree, write a small local helper that builds a minimal one — a couple of route files, a
`lib/store/memory.ts` stub exporting one mutator, a `lib/reconcile/apply.ts` stub importing it, a
`next.config.ts` reading one variable, and a document naming all of them — then mutate exactly one thing
per case. Cover:
- the real repository exits 0;
- adding `app/api/referrals/route.ts` without touching the document exits non-zero and names the path;
- adding a `process.env.SOMETHING_NEW` read in a `lib/` file without touching the document exits
  non-zero and names `SOMETHING_NEW`;
- a `process.env.SOMETHING_NEW` appearing only inside a comment exits 0 — the comment filter, proved;
- a second module importing a mutating export without a document entry exits non-zero;
- a document missing `## Configuration` exits non-zero naming the section;
- a missing document file exits non-zero rather than passing;
- a document that names a route only inside a fenced code block still exits 0 — the sweep is literal and
  does not care where the string sits; say so in the test's name so the behaviour is deliberate rather
  than accidental;
- a clean tree exits 0.
  </action>
  <acceptance_criteria>
    - `node scripts/check-non-bypassability.mjs` exits 0 against the real repository and prints `Problems: 0`
    - `node --test scripts/check-non-bypassability.test.mjs` exits 0
    - The test file contains a missing-document case, a missing-section case, a comment-only env case that exits 0, and a new-route case that exits non-zero
    - `grep -n "MUTATING_EXPORTS" scripts/check-non-bypassability.mjs` shows it imported from `scripts/check-single-writer.mjs`, not restated
    - `grep -n "string sweep\|cannot prove" scripts/check-non-bypassability.mjs` returns a match — the limitation is stated in the script
    - `grep -n "replace(/\\\\\\\\/g" scripts/check-non-bypassability.mjs` or an equivalent path normalisation is present
  </acceptance_criteria>
  <verify>
    <automated>node scripts/check-non-bypassability.mjs &amp;&amp; node --test scripts/check-non-bypassability.test.mjs</automated>
  </verify>
  <done>Adding a route, an environment read or a second store writer without extending the enumeration now fails the build, the rule says plainly that it proves naming rather than truth, and a missing document is a failure rather than a silent pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the rule into verify and close the step list</name>
  <files>scripts/verify.mjs, scripts/verify.test.mjs</files>
  <read_first>
    - scripts/verify.mjs — the whole `STEPS` array as plan 03-13 left it, at twenty-five entries
    - scripts/verify.test.mjs — `EXPECTED_ORDER`, the order test, the header comment's step-count phrase, the `resolveSteps` length assertions and the four structural tests
    - .planning/phases/03-server-seam/03-PATTERNS.md — §`scripts/verify.mjs` (modified) and §`scripts/verify.test.mjs` (modified), the insertion contract and the warning that nothing else cross-checks the two files
    - .planning/phases/01-scaffold-conventions/01-CONTEXT.md — D-20, the fail-fast contract the step list must keep
  </read_first>
  <action>
Insert one step into `scripts/verify.mjs`'s `STEPS`, immediately after `check-accepted-fields` and
before `next-build`:
`{ id: "check-non-bypassability", command: process.execPath, args: ["scripts/check-non-bypassability.mjs"] }`.
No `shell: true`, no `vercelExcluded: true`. Write a comment above it naming D-12 and saying what it
guards: every later phase that adds a route or an environment read must extend
`docs/analysis/single-writer-non-bypassability.md` or this step fails.

Update `scripts/verify.test.mjs` in the same commit:
- insert `"check-non-bypassability"` into `EXPECTED_ORDER` at that position — twenty-six ids;
- change the order test's name and the header comment from "twenty-five" to "twenty-six";
- change the `resolveSteps` length assertions from `23` to `24` for the `VERCEL: "1"` case and from `25`
  to `26` for the three unset-or-distractor cases, including any test name that carries the word;
- leave the four structural tests and plan 03-13's two placement tests unchanged — all six still hold.

Then, in the same commit, add one closing test to `scripts/verify.test.mjs` that makes this phase's
whole contribution legible to a later reader: assert that every one of the six rules this phase added —
`check-fixture-inputs`, `check-named-packages`, `check-single-writer`, `check-actor-field`,
`check-accepted-fields`, `check-non-bypassability` — is present, runs before `next-build`, and is not
`vercelExcluded`, and that `route-suite` is present and runs after `next-build`. Name the test so a
reader sees at a glance what Phase 3 added to the gate. Comment that the six-plus-one shape is the
roadmap's success criterion 3 stated as an assertion.

If `STEPS.length` is not 25 when you start, reconcile to the value you find plus one rather than to the
literal 26, and record the discrepancy in the SUMMARY.

After the edit, run the full gate once and, separately, run it once with `VERCEL=1` in the environment
to confirm the exclusion still removes exactly the two `check-wcag` steps and nothing this phase added.
Record both exit codes and the excluded step ids in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `node -e "import('./scripts/verify.mjs').then(m=>console.log(m.STEPS.length))"` prints `26`
    - `node -e "import('./scripts/verify.mjs').then(m=>{const i=m.STEPS.map(s=>s.id);console.log(i[i.indexOf('check-accepted-fields')+1])})"` prints `check-non-bypassability`
    - `node -e "import('./scripts/verify.mjs').then(m=>{const i=m.STEPS.map(s=>s.id);const b=i.indexOf('next-build');for(const id of ['check-fixture-inputs','check-named-packages','check-single-writer','check-actor-field','check-accepted-fields','check-non-bypassability'])if(i.indexOf(id)===-1||i.indexOf(id)>b)process.exit(1);if(i.indexOf('route-suite')<b)process.exit(1)})"` exits 0
    - `node -e "import('./scripts/verify.mjs').then(m=>{const k=m.resolveSteps(m.STEPS,{VERCEL:'1'});if(k.length!==24)process.exit(1);const removed=m.STEPS.filter(s=>!k.includes(s)).map(s=>s.id);if(removed.join()!=='check-wcag-self-test,check-wcag')process.exit(1)})"` exits 0
    - `node --test scripts/verify.test.mjs` exits 0 and `grep -n "twenty-five" scripts/verify.test.mjs` returns no match
    - `npm run verify` exits 0 end to end with twenty-six steps
  </acceptance_criteria>
  <verify>
    <!-- The first two commands are the fast interim signal — the step list's insertion point and its
         whole shape, asserted in about a second. The full `npm run verify` stays because this task's
         claim is about the gate itself: twenty-six steps, in order, none of this phase's six rules
         excluded on Vercel. Only running it proves that. -->
    <automated>node -e "import('./scripts/verify.mjs').then(m=>{const i=m.STEPS.map(s=>s.id);if(i[i.indexOf('check-accepted-fields')+1]!=='check-non-bypassability')process.exit(1)})" &amp;&amp; node --test scripts/verify.test.mjs &amp;&amp; npm run verify</automated>
  </verify>
  <done>The gate runs twenty-six steps, the six source-side rules this phase added all run before the build and none is excluded on Vercel, the route suite runs after it, and a single test states that shape so the next phase inherits it as a contract rather than as folklore.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a later phase's new route → the enumeration | A route added in P4 or P9 without a document entry would silently hollow out AD-19's claim |
| a check's own text → the document it sweeps | A sweep that counts a variable named inside a comment would let the document satisfy its own requirement by discussing it |
| the step list → the two files that describe it | `verify.mjs` and `verify.test.mjs` drift apart silently; nothing but that test cross-checks them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-73 | Repudiation | a route, env read or store writer added later and never enumerated | mitigate | Task 1 sweeps all three on every build and fails naming the missing path or variable, with a fixture per class proving each non-zero exit (D-12, AD-19). |
| T-3-74 | Repudiation | the document satisfying its own requirement through a comment or its own prose | mitigate | Task 1 strips comment lines before collecting `process.env` names from source, and the test proves a comment-only env read exits 0 while a live one without a document entry exits non-zero. |
| T-3-75 | Repudiation | a missing document passing as "nothing to check" | mitigate | Task 1 reports a named problem when the file does not exist, and a fixture proves it; a missing enumeration is the loudest possible failure of AD-19, not a vacuous pass. |
| T-3-76 | Tampering | the step list and its test drifting apart | mitigate | Task 2 edits both in one commit and adds a closing assertion naming all six new source-side rules and the route suite with their relative positions, so a later removal fails rather than passing quietly (roadmap success criterion 3). |
</threat_model>

<verification>
- `node scripts/check-non-bypassability.mjs` exits 0 with `Problems: 0`.
- `node --test scripts/check-non-bypassability.test.mjs` and `node --test scripts/verify.test.mjs` both exit 0.
- `npm run verify` exits 0 end to end with twenty-six steps, and a `VERCEL=1` run keeps twenty-four, removing only the two `check-wcag` steps — record both in the SUMMARY.
- Each task is one commit; after `gsd-sdk query commit`, confirm with `git log -1 --format=%B` that the `Co-Authored-By` trailer survived, and amend if it did not.
</verification>

<success_criteria>
- The non-bypassability check runs inside `npm run verify` and fails when the enumeration is incomplete (D-12, REQ-FR-24, REQ-FR-57, AD-19).
- All six source-side rules this phase added run before `next-build`, none is excluded on Vercel, and the route suite runs after it (roadmap success criterion 3).
- Each rule ships with a fixture proving it exits non-zero (Phase 1 D-23).
</success_criteria>

<output>
Create `.planning/phases/03-server-seam/03-15-SUMMARY.md` when done
</output>
